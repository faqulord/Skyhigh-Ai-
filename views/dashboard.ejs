<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vezérlőpult | Rafinált Róka</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root { --primary: #FFD700; --dark: #0a0a0a; --dark-card: #161616; }
        body { background-color: var(--dark); color: white; font-family: 'Segoe UI', sans-serif; }
        .navbar { border-bottom: 1px solid rgba(255,255,255,0.1); background: rgba(0,0,0,0.8); backdrop-filter: blur(10px); }
        .card-custom { background: var(--dark-card); border: 1px solid rgba(255,255,255,0.05); border-radius: 15px; box-shadow: 0 10px 30px rgba(0,0,0,0.5); }
        
        /* --- ARANY BETŰ EFFEKT --- */
        .golden-text {
            background: linear-gradient(to right, #BF953F, #FCF6BA, #B38728, #FBF5B7, #AA771C);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            font-weight: 700;
            line-height: 1.6;
            text-shadow: 0px 0px 10px rgba(255, 215, 0, 0.2);
        }
        
        .tip-card { border-left: 5px solid var(--primary); padding: 20px; background: rgba(255, 215, 0, 0.05); border-radius: 10px; margin-top: 20px; }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark fixed-top">
        <div class="container">
            <a class="navbar-brand text-warning fw-bold" href="#"><i class="fas fa-fox me-2"></i>Rafinált Róka</a>
            <div class="d-flex">
                <% if(user.isAdmin) { %> <a href="/admin" class="btn btn-sm btn-outline-warning me-2">ADMIN</a> <% } %>
                <a href="/logout" class="btn btn-sm btn-outline-light">Kilépés</a>
            </div>
        </div>
    </nav>

    <div class="container" style="margin-top: 100px;">
        <div class="row mb-4">
            <div class="col-md-8">
                <h2 class="fw-bold">Szia, <%= user.fullname.split(' ')[0] %>! 🦊</h2>
                <p class="text-secondary"><%= randomQuote %></p>
            </div>
            <div class="col-md-4 text-end">
                <div class="badge bg-dark border border-warning p-2">
                    <i class="fas fa-wallet text-warning me-2"></i>Bankroll: <%= user.startingCapital %> Ft
                </div>
                <div class="mt-2 text-warning small">
                    <% if(streak > 0) { %> 🔥 Jelenlegi Széria: <%= streak %> WIN <% } %>
                </div>
            </div>
        </div>

        <div class="card card-custom mb-5">
            <div class="card-body p-4">
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <h5 class="text-uppercase text-secondary letter-spacing-2">Mai Zsákmány (<%= displayDate %>)</h5>
                    <% if(dailyTip && dailyTip.isReal) { %>
                        <span class="badge bg-success">✅ ÉLŐ API ADAT</span>
                    <% } else { %>
                        <span class="badge bg-warning text-dark">⚠️ DEMÓ</span>
                    <% } %>
                </div>

                <% if (dailyTip && dailyTip.isPublished) { %>
                    <div class="text-center py-3">
                        <h1 class="display-4 fw-bold text-white mb-0"><%= dailyTip.match %></h1>
                        <h3 class="text-warning mt-2"><%= dailyTip.prediction %> (@<%= dailyTip.odds %>)</h3>
                        <p class="text-secondary small"><%= dailyTip.league %> | <%= dailyTip.bookmaker %></p>
                    </div>

                    <div class="tip-card">
                        <p class="golden-text mb-0" style="font-size: 1.1rem; white-space: pre-line;">
                            <%= dailyTip.memberMessage %>
                        </p>
                    </div>

                    <div class="row mt-4 text-center">
                        <div class="col-6">
                            <div class="p-3 bg-dark rounded border border-secondary">
                                <small class="text-secondary text-uppercase">Kezdés</small>
                                <h4 class="text-white mb-0"><%= dailyTip.matchTime %></h4>
                            </div>
                        </div>
                        <div class="col-6">
                            <div class="p-3 bg-dark rounded border border-warning">
                                <small class="text-warning text-uppercase">Ajánlott Tét</small>
                                <h4 class="text-white mb-0"><%= dailyTip.recommendedStake %></h4>
                                <small class="text-secondary">(Kb. <%= Math.floor(user.startingCapital * (parseInt(dailyTip.recommendedStake)/100)) %> Ft)</small>
                            </div>
                        </div>
                    </div>

                <% } else { %>
                    <div class="text-center py-5">
                        <i class="fas fa-hourglass-half fa-3x text-secondary mb-3"></i>
                        <h3>A Róka még számol...</h3>
                        <p class="text-secondary">Nézz vissza később! Az elemzés hamarosan elkészül.</p>
                    </div>
                <% } %>
            </div>
        </div>
        
        <div class="text-center text-secondary small mb-5">
            &copy; 2026 Rafinált Róka System.
        </div>
    </div>
</body>
</html>