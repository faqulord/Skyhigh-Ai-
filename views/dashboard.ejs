<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Skyhigh AI | Az Öreg Róka</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        body { background: #000; color: #fff; font-family: 'Inter', sans-serif; }
        .fox-card { background: #0a0a0a; border-radius: 2rem; border-top: 4px solid #f97316; }
    </style>
</head>
<body class="p-4 md:p-8 flex justify-center min-h-screen">
    <div class="max-w-md w-full pb-12">
        <header class="mb-8 pt-4 flex justify-between items-center">
            <div>
                <p class="text-[10px] text-orange-500 font-black uppercase tracking-[3px] mb-1">Neural Analyst</p>
                <h1 class="text-xl font-bold italic"><%= displayDate %></h1>
            </div>
            <% if (user.isAdmin) { %><a href="/admin" class="bg-orange-600 px-3 py-1 rounded-full text-[9px] font-bold">ADMIN</a><% } %>
        </header>

        <div class="fox-card p-6 shadow-2xl border border-white/5 relative">
            <div class="flex items-center gap-3 mb-6">
                <div class="text-2xl">👴🦊</div>
                <h2 class="text-[10px] font-black uppercase text-zinc-500 tracking-widest italic">A Veterán mai meglátása</h2>
            </div>

            <% if (dailyTip) { %>
                <p class="text-zinc-400 text-xs italic mb-6 leading-relaxed">"<%= dailyTip.reasoning %>"</p>

                <div class="bg-orange-600/10 border-l-4 border-orange-500 p-5 mb-6">
                    <p class="text-[9px] text-orange-500 font-black uppercase mb-1"><%= dailyTip.league %></p>
                    <h3 class="text-xl font-black uppercase mb-1"><%= dailyTip.match %></h3>
                    <p class="text-lg font-black text-white italic underline underline-offset-4 decoration-orange-500"><%= dailyTip.prediction %></p>
                </div>

                <div class="grid grid-cols-2 gap-4 mb-6">
                    <div class="bg-white/5 p-4 rounded-2xl text-center">
                        <p class="text-[8px] text-zinc-500 font-bold uppercase mb-1 tracking-widest text-orange-500">Mikor?</p>
                        <p class="text-base font-black italic underline"><%= dailyTip.matchTime %></p>
                    </div>
                    <div class="bg-white/5 p-4 rounded-2xl text-center">
                        <p class="text-[8px] text-zinc-500 font-bold uppercase mb-1 tracking-widest">Szorzó:</p>
                        <p class="text-base font-black text-white">@<%= dailyTip.odds %></p>
                    </div>
                </div>

                <div class="bg-zinc-900/80 p-5 rounded-3xl border border-white/5 text-center">
                    <p class="text-[9px] text-zinc-500 font-bold uppercase mb-1 tracking-widest italic">Javasolt Tét (Kőkemény 10%):</p>
                    <p class="text-3xl font-black text-green-500 italic"><%= recommendedStake.toLocaleString() %> Ft</p>
                    <div class="mt-4 flex justify-between items-center text-[10px] border-t border-white/10 pt-3">
                        <span class="text-zinc-500 uppercase font-bold tracking-widest">Hol rakd meg?</span>
                        <span class="text-white font-black uppercase tracking-widest"><%= dailyTip.bookmaker %></span>
                    </div>
                </div>

            <% } else { %>
                <div class="py-16 text-center text-zinc-600 text-[10px] font-black uppercase tracking-[0.4em] animate-pulse italic">Az Öreg Róka épp a statisztikákat elemzi...</div>
            <% } %>
        </div>

        <div class="mt-8 flex justify-between items-center px-4">
            <p class="text-[10px] text-zinc-500 uppercase font-bold tracking-widest italic tracking-widest">Bankroll: <b><%= user.startingCapital.toLocaleString() %> Ft</b></p>
        </div>
    </div>
</body>
</html>