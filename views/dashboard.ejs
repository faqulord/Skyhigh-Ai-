<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Vezérlőpult | Rafinált Róka</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&display=swap" rel="stylesheet">
    <style>
        /* --- PRÉMIUM STÍLUSOK --- */
        body { 
            background-color: #050505; 
            /* Finom technológiai rács háttér */
            background-image: linear-gradient(rgba(255, 255, 255, 0.03) 1px, transparent 1px),
            linear-gradient(90deg, rgba(255, 255, 255, 0.03) 1px, transparent 1px);
            background-size: 40px 40px;
            color: #fff; 
            font-family: 'Inter', sans-serif; 
        }

        /* ÜVEGHATÁSÚ KÁRTYA (Glassmorphism) */
        .glass-card {
            background: rgba(20, 20, 20, 0.7);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.08);
            border-radius: 24px;
            box-shadow: 0 20px 50px rgba(0,0,0,0.5);
        }

        /* ARANY GRADIENS SZÖVEG (A TIPPHEZ) */
        .gold-gradient {
            background: linear-gradient(135deg, #FFD700 0%, #FDB931 50%, #D4AF37 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            filter: drop-shadow(0 0 15px rgba(253, 185, 49, 0.3));
        }

        /* PEZSGŐ SZÍNŰ MAGYARÁZAT (OLVASHATÓ, ELEGÁNS) */
        .champagne-text {
            color: #F3E5AB; 
            line-height: 1.7;
            font-weight: 400;
            letter-spacing: 0.3px;
        }

        /* ARANY SZEGÉLY A BAL OLDALON */
        .gold-border-left {
            border-left: 3px solid #FFD700;
        }

        .status-badge {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(4px);
        }
    </style>
</head>
<body class="p-4 md:p-8 max-w-2xl mx-auto min-h-screen flex flex-col">

    <header class="flex justify-between items-center mb-10">
        <div class="flex items-center gap-3">
            <div class="text-2xl">🦊</div>
            <div>
                <h1 class="text-sm font-bold text-white tracking-widest uppercase">Rafinált Róka</h1>
                <div class="flex items-center gap-2">
                    <span class="w-1.5 h-1.5 rounded-full bg-green-500 animate-pulse"></span>
                    <p class="text-[10px] text-zinc-400 font-medium">System Online</p>
                </div>
            </div>
        </div>
        
        <div class="flex gap-2">
            <% if (user.isAdmin || user.email === 'stylefaqu@gmail.com') { %>
                <a href="/admin" class="status-badge text-yellow-500 px-3 py-1.5 rounded-lg text-[10px] font-bold hover:bg-white/10 transition">ADMIN</a>
            <% } %>
            <a href="/logout" class="status-badge text-zinc-400 px-3 py-1.5 rounded-lg text-[10px] font-bold hover:text-white transition">KILÉPÉS</a>
        </div>
    </header>

    <div class="mb-8 px-2">
        <p class="text-zinc-500 text-xs font-bold uppercase mb-1 tracking-widest"><%= displayDate %></p>
        <h2 class="text-3xl font-light text-white mb-2">Üdv a Falkában, <span class="font-bold text-yellow-500"><%= user.fullname.split(' ')[0] %></span>.</h2>
    </div>

    <% if (dailyTip && dailyTip.isPublished) { %>
        <div class="glass-card p-0 overflow-hidden relative mb-8">
            <div class="px-6 py-4 border-b border-white/5 flex justify-between items-center bg-white/5">
                <span class="text-[10px] font-bold text-zinc-400 uppercase tracking-widest"><%= dailyTip.league %></span>
                <% if(dailyTip.isReal) { %>
                    <span class="text-[10px] font-bold text-green-400 bg-green-900/20 px-2 py-1 rounded border border-green-500/20">ÉLŐ ADAT</span>
                <% } else { %>
                    <span class="text-[10px] font-bold text-yellow-400 bg-yellow-900/20 px-2 py-1 rounded border border-yellow-500/20">DEMÓ</span>
                <% } %>
            </div>

            <div class="p-8 text-center">
                <h3 class="text-xl font-medium text-white mb-6"><%= dailyTip.match %></h3>
                
                <div class="inline-block relative">
                    <div class="absolute inset-0 bg-yellow-500/10 blur-xl rounded-full"></div>
                    <div class="relative bg-black/40 border border-yellow-500/30 px-8 py-4 rounded-2xl backdrop-blur-md">
                        <p class="text-xs text-zinc-500 uppercase font-bold mb-1 tracking-widest">CÉLPONT</p>
                        <p class="text-3xl font-black gold-gradient mb-1"><%= dailyTip.prediction %></p>
                        <p class="text-sm font-medium text-zinc-400">Odds: <span class="text-white font-bold"><%= dailyTip.odds %></span></p>
                    </div>
                </div>
            </div>

            <div class="bg-black/20 p-6 border-t border-white/5">
                <div class="gold-border-left pl-4">
                    <p class="text-[10px] text-zinc-500 font-bold uppercase mb-2 flex items-center gap-2">
                        <span>💬</span> ELEMZÉS
                    </p>
                    <p class="text-sm champagne-text italic">
                        "<%= dailyTip.memberMessage %>"
                    </p>
                </div>
            </div>

            <div class="bg-black/40 px-6 py-4 flex justify-between items-center border-t border-white/5">
                <div class="text-center">
                    <p class="text-[9px] text-zinc-500 font-bold uppercase">KEZDÉS</p>
                    <p class="text-sm font-bold text-white"><%= dailyTip.matchTime %></p>
                </div>
                <div class="h-6 w-px bg-white/10"></div>
                <div class="text-center">
                    <p class="text-[9px] text-yellow-600 font-bold uppercase">JAVASOLT TÉT</p>
                    <p class="text-sm font-bold text-yellow-500"><%= dailyTip.recommendedStake %></p>
                </div>
            </div>
        </div>
    <% } else { %>
        <div class="glass-card p-10 text-center mb-8">
            <div class="w-16 h-16 bg-white/5 rounded-full flex items-center justify-center mx-auto mb-6 text-3xl animate-pulse ring-1 ring-white/10">⏳</div>
            <h3 class="text-xl font-bold text-white mb-2">Adatok feldolgozása...</h3>
            <p class="text-sm text-zinc-500">A Róka éppen a piacot elemzi. Hamarosan érkezik a prémium tipp.</p>
        </div>
    <% } %>

    <div class="grid grid-cols-2 gap-4 mt-auto">
        <div class="status-badge p-4 rounded-xl text-center">
            <p class="text-[10px] text-zinc-500 font-bold uppercase">BANKROLL</p>
            <p class="text-lg font-bold text-white"><%= user.startingCapital %> Ft</p>
        </div>
        <div class="status-badge p-4 rounded-xl text-center">
            <p class="text-[10px] text-zinc-500 font-bold uppercase">WIN STREAK</p>
            <p class="text-lg font-bold text-green-400">
                <% if(streak > 0) { %> 🔥 <%= streak %> <% } else { %> - <% } %>
            </p>
        </div>
    </div>

    <div class="text-center mt-8 pb-4">
        <p class="text-[10px] text-zinc-700 font-bold uppercase">&copy; 2026 RAFINÁLT RÓKA AI SYSTEM</p>
    </div>

</body>
</html>