<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Skyhigh AI | Vezérlőpult</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-[#050505] text-white p-4 font-sans">
    <div class="max-w-md mx-auto">
        <header class="flex justify-between items-center mb-8 pt-4">
            <div>
                <h1 class="text-xl font-black italic text-purple-500 uppercase tracking-tighter">Skyhigh AI</h1>
                <p class="text-[9px] text-zinc-500 uppercase font-bold tracking-[3px]">Neural Network v4.0</p>
            </div>
            <a href="/logout" class="text-zinc-600 hover:text-white transition"><svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" /></svg></a>
        </header>

        <div class="bg-zinc-900/50 p-6 rounded-3xl border border-white/5 mb-6">
            <p class="text-[10px] text-zinc-500 uppercase font-bold mb-1">Üdvözlünk, <%= user.fullname %></p>
            <div class="flex justify-between items-end">
                <h2 class="text-2xl font-black italic">Saját Bankroll</h2>
                <span class="text-purple-500 font-mono font-bold text-xl">€<%= user.startingCapital %></span>
            </div>
        </div>

        <div class="bg-gradient-to-br from-purple-900/30 to-black p-6 rounded-[2rem] border border-purple-500/30 mb-6 shadow-2xl shadow-purple-500/10">
            <div class="flex justify-between items-start mb-6">
                <span class="bg-purple-600 text-[9px] px-3 py-1 rounded-full font-black uppercase tracking-widest text-white">Mai Master Tipp</span>
                <span class="text-zinc-500 text-[10px] font-mono"><%= new Date().toLocaleDateString('hu-HU') %></span>
            </div>

            <% if (dailyTip) { %>
                <div class="mb-6">
                    <h3 class="text-xl font-black mb-1 uppercase tracking-tight text-white"><%= dailyTip.match %></h3>
                    <p class="text-purple-400 font-bold text-lg italic"><%= dailyTip.prediction %></p>
                </div>

                <div class="grid grid-cols-2 gap-4 mb-6">
                    <div class="bg-black/40 p-3 rounded-2xl border border-white/5">
                        <p class="text-[9px] text-zinc-500 uppercase font-bold">Odds</p>
                        <p class="text-lg font-black"><%= dailyTip.odds %></p>
                    </div>
                    <div class="bg-black/40 p-3 rounded-2xl border border-white/5">
                        <p class="text-[9px] text-zinc-500 uppercase font-bold">Várható Profit</p>
                        <p class="text-lg font-black text-green-500">+<%= dailyTip.profitPercent %>%</p>
                    </div>
                </div>

                <div class="bg-white/5 p-4 rounded-2xl border border-white/5">
                    <p class="text-[10px] text-purple-400 uppercase font-black mb-2 tracking-widest">Matematikai Elemzés (10 év H2H)</p>
                    <p class="text-xs text-zinc-300 leading-relaxed italic">"<%= dailyTip.reasoning %>"</p>
                </div>
            <% } else { %>
                <div class="text-center py-10">
                    <div class="animate-spin h-8 w-8 border-4 border-purple-500 border-t-transparent rounded-full mx-auto mb-4"></div>
                    <p class="text-zinc-500 text-xs uppercase font-bold">A robot jelenleg elemzi a 10 éves adatbázist...</p>
                </div>
            <% } %>
        </div>

        <div class="bg-zinc-900/30 p-6 rounded-3xl border border-white/5">
            <h3 class="text-[10px] text-zinc-500 uppercase font-black mb-4 tracking-widest">Utolsó Tippek Eredménye</h3>
            <div class="space-y-3">
                <% history.forEach(h => { %>
                    <div class="flex justify-between items-center text-[11px] border-b border-white/5 pb-2">
                        <span class="text-zinc-400 font-mono"><%= h.date %></span>
                        <span class="font-bold truncate w-32 px-2"><%= h.match %></span>
                        <span class="text-green-500 font-black">+<%= h.profitPercent %>%</span>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>