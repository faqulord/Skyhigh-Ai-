<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard | Rafinált Robot Róka</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@800&family=Inter:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body { background: #050505; color: #fff; font-family: 'Inter', sans-serif; }
        .fox-card { background: #0a0a0a; border-radius: 2rem; border-top: 4px solid #8b5cf6; }
        .next-tip-badge { background: rgba(139, 92, 246, 0.1); border: 1px solid rgba(139, 92, 246, 0.3); }
    </style>
</head>
<body class="p-4 md:p-8 flex justify-center min-h-screen">
    <div class="max-w-md w-full">
        <header class="mb-8 pt-4 flex justify-between items-center border-b border-white/5 pb-6">
            <div>
                <p style="font-family: 'Orbitron';" class="text-[9px] text-purple-500 uppercase tracking-[4px] mb-1">Neural Access</p>
                <h1 class="text-lg font-bold italic"><%= displayDate %></h1>
            </div>
            <% if (user.isAdmin) { %><a href="/admin" class="bg-purple-600 px-3 py-1.5 rounded-full text-[9px] font-black uppercase">Admin Panel</a><% } %>
        </header>

        <div class="next-tip-badge p-3 rounded-2xl mb-6 flex justify-between items-center">
            <span class="text-[10px] text-purple-400 font-black uppercase tracking-widest italic">Következő Master Tipp:</span>
            <span class="text-[10px] text-white font-black"><%= nextTipText %></span>
        </div>

        <div class="fox-card p-8 shadow-2xl relative mb-10 overflow-hidden border-b border-white/5">
            <div class="absolute -top-10 -right-10 w-32 h-32 bg-purple-600/10 blur-3xl"></div>
            <div class="flex items-center gap-4 mb-8">
                <div class="text-4xl">🦊💜</div>
                <div>
                    <h2 class="text-[10px] font-black uppercase text-purple-400 tracking-[3px]">Rafinált Robot Róka</h2>
                    <p class="text-[9px] text-zinc-500 font-bold uppercase italic">Cél: 30 napos profit</p>
                </div>
            </div>

            <% if (dailyTip) { %>
                <p class="text-zinc-400 text-[13px] italic mb-8 leading-relaxed">"<%= dailyTip.reasoning %>"</p>
                <div class="bg-purple-600/10 border-l-4 border-purple-500 p-6 mb-8 rounded-r-xl">
                    <p class="text-[10px] text-purple-400 font-black uppercase mb-1"><%= dailyTip.league %></p>
                    <h3 class="text-xl font-black uppercase text-white mb-2 leading-tight"><%= dailyTip.match %></h3>
                    <p class="text-lg font-black italic text-purple-300 uppercase tracking-tighter"><%= dailyTip.prediction %></p>
                </div>
                <div class="grid grid-cols-2 gap-3 mb-8">
                    <div class="bg-white/5 p-4 rounded-2xl text-center border border-white/5 shadow-inner">
                        <p class="text-[8px] text-zinc-500 uppercase font-black">Kezdés</p>
                        <p class="text-base font-black"><%= dailyTip.matchTime %></p>
                    </div>
                    <div class="bg-white/5 p-4 rounded-2xl text-center border border-white/5 shadow-inner">
                        <p class="text-[8px] text-zinc-500 uppercase font-black">Odds</p>
                        <p class="text-base font-black text-purple-400">@<%= dailyTip.odds %></p>
                    </div>
                </div>
                <div class="bg-zinc-900/80 p-6 rounded-[2rem] text-center shadow-xl border border-white/5">
                    <p class="text-[10px] text-zinc-500 font-bold uppercase mb-2 tracking-[2px]">Tétjavaslat a tőkédhez:</p>
                    <p class="text-3xl font-black text-green-500 italic"><%= recommendedStake.toLocaleString() %> Ft</p>
                </div>
            <% } else { %>
                <div class="py-20 text-center text-zinc-600 text-[10px] font-black uppercase animate-pulse italic leading-relaxed">
                    A Róka épp a statisztikákat elemzi...<br>
                    <span class="text-purple-500 text-[8px] tracking-[2px]">Várható érkezés: <%= nextTipText %></span>
                </div>
            <% } %>
        </div>

        <div class="px-6 flex justify-between items-center opacity-40">
            <p class="text-[10px] text-zinc-500 uppercase font-bold italic">Tőkéd: <%= user.startingCapital.toLocaleString() %> Ft</p>
            <a href="/logout" class="text-[10px] text-zinc-500 uppercase font-bold hover:text-white">Kilépés</a>
        </div>
    </div>
</body>
</html>