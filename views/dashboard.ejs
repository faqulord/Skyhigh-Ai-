<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard | Skyhigh AI</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>body { background: #050505; color: #fff; } .fox-card { background: #0a0a0a; border-radius: 2rem; border-top: 4px solid #8b5cf6; }</style>
</head>
<body class="p-4 md:p-8 flex justify-center">
    <div class="max-w-md w-full">
        <header class="flex justify-between mb-8 pb-4 border-b border-white/5"><div><p class="text-[9px] text-purple-500 font-bold uppercase tracking-widest">Neural Access</p><h1 class="text-lg font-bold italic"><%= displayDate %></h1></div><% if (user.isAdmin) { %><a href="/admin" class="bg-purple-600 px-3 py-1.5 rounded-full text-[9px] font-black uppercase">Admin HQ</a><% } %></header>
        <div class="bg-purple-600/10 p-3 rounded-2xl mb-8 text-[10px] text-purple-400 font-black text-center uppercase">Várható következő elemzés: <%= nextTipText %></div>
        <div class="fox-card p-10 shadow-2xl relative">
            <% if (dailyTip) { %><p class="text-zinc-400 text-xs italic mb-6 leading-relaxed">"<%= dailyTip.reasoning %>"</p>
            <div class="bg-purple-600/10 border-l-4 border-purple-500 p-5 mb-6"><h3 class="text-xl font-black uppercase"><%= dailyTip.match %></h3><p class="text-lg text-purple-300 italic"><%= dailyTip.prediction %></p></div>
            <div class="bg-zinc-900 p-6 rounded-[2rem] text-center"><p class="text-[10px] text-zinc-500 uppercase">Tétjavaslat:</p><p class="text-3xl font-black text-green-500 italic"><%= recommendedStake.toLocaleString() %> Ft</p></div><% } else { %>
            <div class="py-20 text-center text-zinc-600 text-[10px] animate-pulse italic uppercase tracking-widest">A Róka elemzi az adatokat...</div><% } %>
        </div>
    </div>
</body>
</html>