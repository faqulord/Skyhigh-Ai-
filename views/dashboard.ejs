<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Skyhigh | Neural Access</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;700;900&display=swap');
        body { background-color: #000; color: #fff; font-family: 'Inter', sans-serif; }
        .neon-border { border: 1px solid rgba(139, 92, 246, 0.3); box-shadow: 0 0 20px rgba(139, 92, 246, 0.1); }
    </style>
</head>
<body class="p-4 md:p-8">
    <div class="max-w-lg mx-auto">
        <header class="flex justify-between items-center mb-10">
            <div>
                <h1 class="text-2xl font-black italic tracking-tighter text-purple-500">SKYHIGH AI</h1>
                <p class="text-[8px] uppercase tracking-[4px] text-zinc-500 font-bold">Neural Trading Platform</p>
            </div>
            <div class="flex gap-4">
                <% if (user.isAdmin) { %>
                    <a href="/admin" class="bg-purple-600 text-white px-4 py-1.5 rounded-full text-[10px] font-black uppercase shadow-lg shadow-purple-600/20">Admin</a>
                <% } %>
                <a href="/logout" class="text-zinc-500 text-[10px] font-bold uppercase py-1.5">Kilépés</a>
            </div>
        </header>

        <div class="bg-zinc-900/40 neon-border p-6 rounded-3xl mb-8 flex justify-between items-center">
            <div>
                <p class="text-[9px] text-zinc-500 uppercase font-bold mb-1">Elérhető Tőke</p>
                <p class="text-2xl font-black italic">€<%= user.startingCapital %></p>
            </div>
            <div class="text-right">
                <p class="text-[9px] text-green-500 uppercase font-bold mb-1">Státusz</p>
                <p class="text-xs font-bold uppercase tracking-widest text-white">Aktív Access</p>
            </div>
        </div>

        <div class="bg-gradient-to-b from-zinc-900 to-black neon-border rounded-[2.5rem] p-8 mb-8">
            <div class="flex justify-between items-center mb-8">
                <span class="bg-white text-black text-[9px] px-3 py-1 rounded-full font-black uppercase">Master Tip</span>
                <span class="text-[10px] text-zinc-500 font-mono font-bold"><%= new Date().toLocaleDateString('hu-HU') %></span>
            </div>

            <% if (dailyTip) { %>
                <h2 class="text-2xl font-black uppercase mb-2 leading-none"><%= dailyTip.match %></h2>
                <p class="text-purple-500 font-bold italic text-lg mb-8 uppercase tracking-tight"><%= dailyTip.prediction %></p>

                <div class="grid grid-cols-2 gap-4 mb-8">
                    <div class="bg-zinc-800/50 p-4 rounded-2xl">
                        <p class="text-[9px] text-zinc-500 uppercase font-bold mb-1">Odds</p>
                        <p class="text-xl font-black"><%= dailyTip.odds %></p>
                    </div>
                    <div class="bg-zinc-800/50 p-4 rounded-2xl text-green-500">
                        <p class="text-[9px] text-zinc-500 uppercase font-bold mb-1">Várható Profit</p>
                        <p class="text-xl font-black">+<%= dailyTip.profitPercent %>%</p>
                    </div>
                </div>

                <div class="border-t border-white/5 pt-6">
                    <p class="text-[10px] text-purple-400 uppercase font-black mb-3 tracking-widest">Matematikai Elemzés (10Y H2H)</p>
                    <p class="text-xs text-zinc-400 leading-relaxed italic"><%= dailyTip.reasoning %></p>
                </div>
            <% } else { %>
                <div class="py-10 text-center">
                    <p class="text-zinc-600 text-xs font-bold uppercase animate-pulse">Neural robot elemzés folyamatban...</p>
                </div>
            <% } %>
        </div>

        <div class="bg-zinc-900/20 rounded-3xl p-6">
            <h3 class="text-[10px] text-zinc-600 uppercase font-black mb-6 tracking-widest">Utolsó 10 elemzés naplója</h3>
            <div class="space-y-4">
                <% history.forEach(h => { %>
                    <div class="flex justify-between items-center text-[11px] border-b border-white/5 pb-3 last:border-0">
                        <span class="text-zinc-600 font-mono"><%= h.date %></span>
                        <span class="font-bold uppercase truncate w-32 px-2"><%= h.match %></span>
                        <span class="text-green-500 font-black">+<%= h.profitPercent %>%</span>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>