<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Dashboard | Rafinált Róka</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@800&family=Inter:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        body { background: #050505; color: #fff; font-family: 'Inter', sans-serif; }
        .fox-card { background: #0a0a0a; border-radius: 2rem; border-top: 4px solid #8b5cf6; }
    </style>
</head>
<body class="p-4 flex justify-center min-h-[100dvh]">
    <div class="w-full max-w-md">
        <header class="flex justify-between items-center mb-6 pt-2 border-b border-white/5 pb-4">
            <div><p style="font-family: 'Orbitron';" class="text-[8px] text-purple-500 uppercase tracking-widest">Neural Access</p><h1 class="text-sm font-bold italic"><%= displayDate %></h1></div>
            <% if (user.isAdmin) { %><a href="/admin" class="bg-purple-600 px-3 py-1.5 rounded-full text-[9px] font-black uppercase">Admin HQ</a><% } %>
        </header>

        <div class="bg-purple-600/10 p-2.5 rounded-xl mb-6 text-[9px] font-black uppercase text-purple-400 text-center tracking-widest">Következő elemzés: <%= nextTipText %></div>

        <div class="fox-card p-6 shadow-2xl relative mb-8">
            <div class="flex items-center gap-3 mb-6">
                <div class="text-3xl">🦊💜</div>
                <div><h2 class="text-[9px] font-black uppercase text-purple-400">Rafinált Robot Róka</h2><p class="text-[8px] text-zinc-500 italic">30 napos ciklus aktív</p></div>
            </div>

            <% if (dailyTip) { %>
                <p class="text-zinc-400 text-[11px] italic mb-6 leading-relaxed">"<%= dailyTip.reasoning %>"</p>
                <div class="bg-purple-600/10 border-l-4 border-purple-500 p-4 mb-6 rounded-r-xl">
                    <p class="text-[8px] text-purple-400 font-black uppercase mb-1"><%= dailyTip.league %></p>
                    <h3 class="text-lg font-black uppercase mb-1 leading-tight"><%= dailyTip.match %></h3>
                    <p class="text-base font-black italic text-purple-300 uppercase underline decoration-purple-500/30 underline-offset-4"><%= dailyTip.prediction %></p>
                </div>
                <div class="grid grid-cols-2 gap-3 mb-6 text-center">
                    <div class="bg-white/5 p-3 rounded-xl border border-white/5"><p class="text-[7px] text-zinc-500 uppercase">Kezdés</p><p class="text-sm font-black"><%= dailyTip.matchTime %></p></div>
                    <div class="bg-white/5 p-3 rounded-xl border border-white/5"><p class="text-[7px] text-zinc-500 uppercase">Szorzó</p><p class="text-sm font-black text-purple-400">@<%= dailyTip.odds %></p></div>
                </div>
                <div class="bg-zinc-900/80 p-5 rounded-[1.5rem] text-center border border-white/5">
                    <p class="text-[9px] text-zinc-500 font-bold uppercase mb-1">Ezt rakd meg:</p>
                    <p class="text-2xl font-black text-green-500 italic"><%= recommendedStake.toLocaleString() %> Ft</p>
                    <p class="text-[7px] text-zinc-600 mt-2 uppercase">Iroda: <span class="text-white font-bold"><%= dailyTip.bookmaker %></span></p>
                </div>
            <% } else { %>
                <div class="py-16 text-center text-zinc-600 text-[9px] font-black uppercase animate-pulse italic">A Róka elemzést végez...</div>
            <% } %>
        </div>

        <div class="flex justify-between items-center opacity-40 text-[8px] font-bold uppercase tracking-widest px-4">
            <p>Tőke: <%= user.startingCapital.toLocaleString() %> Ft</p>
            <a href="/logout">Kilépés</a>
        </div>
    </div>
</body>
</html>