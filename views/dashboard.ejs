<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Skyhigh AI | Neural Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;900&family=Inter:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body { background: #020202; color: #e5e7eb; font-family: 'Inter', sans-serif; }
        .neon-box { background: #0a0a0a; border: 1px solid rgba(139, 92, 246, 0.3); box-shadow: 0 0 15px rgba(139, 92, 246, 0.1); }
        .orbitron { font-family: 'Orbitron', sans-serif; }
    </style>
</head>
<body class="p-4">
    <div class="max-w-md mx-auto">
        <header class="flex justify-between items-center mb-8 pt-4">
            <h1 class="orbitron text-xl font-black text-purple-500 italic">SKYHIGH AI</h1>
            <div class="flex gap-3">
                <% if (user.isAdmin) { %><a href="/admin" class="bg-purple-600 px-3 py-1 rounded-lg text-[10px] font-black uppercase">Admin</a><% } %>
                <a href="/logout" class="text-zinc-500 text-[10px] uppercase font-bold py-1">Kilépés</a>
            </div>
        </header>

        <div class="neon-box rounded-3xl p-6 mb-6">
            <p class="text-[9px] uppercase text-zinc-500 font-bold mb-1">Elérhető Bankroll</p>
            <p class="orbitron text-2xl font-black"><%= user.startingCapital.toLocaleString() %> Ft</p>
        </div>

        <div class="bg-gradient-to-b from-zinc-900 to-black rounded-[2.5rem] border border-purple-500/50 p-6 mb-8">
            <div class="flex justify-between items-center mb-6">
                <span class="bg-purple-600 text-[9px] px-3 py-1 rounded-full font-black uppercase">Master Tip</span>
                <span class="text-[10px] text-zinc-500 font-mono"><%= new Date().toLocaleDateString('hu-HU') %></span>
            </div>

            <% if (dailyTip) { %>
                <h2 class="text-xl font-black uppercase mb-2"><%= dailyTip.match %></h2>
                <p class="text-purple-400 font-bold mb-6 italic uppercase tracking-widest"><%= dailyTip.prediction %></p>
                <div class="grid grid-cols-2 gap-4 mb-6">
                    <div class="bg-zinc-800/40 p-4 rounded-2xl border border-white/5 text-center">
                        <p class="text-[9px] text-zinc-500 uppercase font-bold mb-1">Odds</p>
                        <p class="text-lg font-black orbitron"><%= dailyTip.odds %></p>
                    </div>
                    <div class="bg-zinc-800/40 p-4 rounded-2xl border border-white/5 text-center">
                        <p class="text-[9px] text-zinc-500 uppercase font-bold mb-1 text-green-500">Profit</p>
                        <p class="text-lg font-black text-green-500 orbitron">+<%= dailyTip.profitPercent %>%</p>
                    </div>
                </div>
                <div class="bg-black/60 p-4 rounded-2xl border border-white/5">
                    <p class="text-[10px] text-purple-500 font-black mb-2 uppercase tracking-widest">Matematikai Elemzés</p>
                    <p class="text-[11px] text-zinc-400 leading-relaxed italic"><%= dailyTip.reasoning %></p>
                </div>
            <% } else { %>
                <div class="py-12 text-center text-zinc-600 text-[10px] font-black uppercase tracking-widest animate-pulse italic">Neural elemzés folyamatban...</div>
            <% } %>
        </div>

        <div class="bg-zinc-900/20 rounded-3xl p-6 border border-white/5">
            <h3 class="text-[10px] text-zinc-600 uppercase font-black mb-6 tracking-widest italic text-center">Korábbi Eredmények</h3>
            <div class="space-y-4">
                <% history.forEach(h => { %>
                    <div class="flex justify-between items-center text-[10px] border-b border-white/5 pb-3">
                        <span class="text-zinc-500 font-mono w-20"><%= h.date %></span>
                        <span class="font-bold uppercase truncate flex-1 text-zinc-300 px-2"><%= h.match %></span>
                        <span class="text-green-500 font-black orbitron">+<%= h.profitPercent %>%</span>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>