<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Skyhigh AI | Neural Terminal</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { background: #020202; color: #fff; font-family: 'Inter', sans-serif; }
        .glass-panel { background: rgba(15, 15, 15, 0.8); border: 1px solid rgba(139, 92, 246, 0.2); border-radius: 24px; }
        .neon-text { text-shadow: 0 0 10px rgba(139, 92, 246, 0.5); }
    </style>
</head>
<body class="p-6 md:p-12">
    <div class="max-w-5xl mx-auto">
        <div class="flex justify-between items-center mb-12">
            <div>
                <h1 class="text-2xl font-black italic neon-text text-purple-500 uppercase">Skyhigh AI</h1>
                <p class="text-[9px] text-zinc-600 font-bold uppercase tracking-[4px]">Neural Betting Interface</p>
            </div>
            <% if (user.isAdmin) { %>
                <a href="/admin" class="bg-red-600/20 text-red-500 border border-red-500/30 px-6 py-2 rounded-full text-[10px] font-black uppercase tracking-widest">Admin Control</a>
            <% } %>
        </div>

        <div class="glass-panel p-4 mb-8 flex justify-between items-center px-10 border-purple-500/40">
            <span class="text-[10px] text-zinc-500 font-black uppercase tracking-widest italic">Next AI Update In:</span>
            <div id="timer" class="text-2xl font-mono font-bold text-white italic">00:00:00</div>
        </div>

        <div class="glass-panel p-10 relative overflow-hidden border-purple-500/30 shadow-2xl">
            <% if (dailyTip) { %>
                <span class="text-[9px] bg-green-500/20 text-green-500 px-3 py-1 rounded-full font-bold uppercase mb-6 inline-block">Algorithm Validated</span>
                <h2 class="text-4xl md:text-6xl font-black italic uppercase mb-6"><%= dailyTip.match %></h2>
                <div class="flex items-center gap-6 mb-8">
                    <div class="bg-purple-600 text-white px-8 py-3 rounded-2xl text-3xl font-black italic shadow-lg shadow-purple-600/40"><%= dailyTip.odds %></div>
                    <div class="text-xl font-bold italic text-zinc-400">TIP: <span class="text-white uppercase"><%= dailyTip.prediction %></span></div>
                </div>
                <div class="bg-black/50 p-6 rounded-2xl border border-white/5 italic">
                    <p class="text-[10px] text-purple-500 font-black uppercase mb-3 tracking-widest">Neural Reasoning (AI Elemzés)</p>
                    <p class="text-sm text-zinc-300 leading-relaxed">"<%= dailyTip.reasoning %>"</p>
                </div>
            <% } else { %>
                <div class="text-center py-20 text-zinc-700 italic uppercase text-xs tracking-widest">A robot elemzi a mai piacot... Várj 08:00-ig</div>
            <% } %>
        </div>
    </div>

    <script>
        function updateTimer() {
            const now = new Date();
            const target = new Date(); target.setHours(8, 0, 0, 0);
            if (now > target) target.setDate(target.getDate() + 1);
            const diff = target - now;
            const h = Math.floor(diff/3600000).toString().padStart(2,'0');
            const m = Math.floor((diff%3600000)/60000).toString().padStart(2,'0');
            const s = Math.floor((diff%60000)/1000).toString().padStart(2,'0');
            document.getElementById('timer').innerText = `${h}:${m}:${s}`;
        }
        setInterval(updateTimer, 1000); updateTimer();
    </script>
</body>
</html>