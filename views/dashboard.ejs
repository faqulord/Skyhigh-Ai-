<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Dashboard | Rafinált Róka</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@800&family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        body { background: #050505; color: #fff; font-family: 'Inter', sans-serif; padding-bottom: 40px; overflow-x: hidden; }
        .startup-card { background: #111; border: 1px solid #27272a; border-radius: 20px; box-shadow: 0 10px 30px -10px rgba(0,0,0,0.5); }
        
        .gradient-text { 
            background: linear-gradient(to right, #FCD34D, #F59E0B, #D97706); 
            -webkit-background-clip: text; 
            -webkit-text-fill-color: transparent; 
            filter: drop-shadow(0 0 10px rgba(245, 158, 11, 0.3));
        }
        
        /* LILA-RÓZSASZÍN KERET */
        .purple-pink-border {
            border-width: 2px;
            border-style: solid;
            border-image: linear-gradient(to right, #a855f7, #ec4899) 1;
            box-shadow: 0 0 15px rgba(168, 85, 247, 0.2);
        }

        /* ÚJ: NARANCSSÁRGA SZÖVEG (Vakító!) */
        .orange-neon-text {
            color: #FF9F43; /* Élénk Narancs */
            text-shadow: 0 0 8px rgba(255, 159, 67, 0.6); /* Neon ragyogás */
            font-weight: 700;
            letter-spacing: 0.5px;
        }

        /* ÚSZÓ ANIMÁCIÓ (MARQUEE) */
        .marquee-container {
            overflow: hidden;
            white-space: nowrap;
            position: relative;
            mask-image: linear-gradient(to right, transparent, black 10%, black 90%, transparent); /* Szebb szélek */
        }
        .marquee-text {
            display: inline-block;
            padding-left: 100%; 
            animation: marquee 20s linear infinite; /* Lassítottam kicsit, hogy olvashatóbb legyen */
        }
        @keyframes marquee {
            0% { transform: translateX(0); }
            100% { transform: translateX(-100%); }
        }

        .fox-pulse { animation: pulse-fox 2s infinite; }
        @keyframes pulse-fox { 0% { transform: scale(1); opacity: 1; } 50% { transform: scale(1.1); opacity: 0.8; } 100% { transform: scale(1); opacity: 1; } }

        .fox-glow { box-shadow: 0 0 25px rgba(168, 85, 247, 0.2); }
        .toast { position: fixed; bottom: -100px; left: 50%; transform: translateX(-50%); background: #7c3aed; color: white; padding: 12px 24px; border-radius: 50px; font-size: 12px; font-weight: bold; box-shadow: 0 10px 20px rgba(124, 58, 237, 0.4); z-index: 100; transition: bottom 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275); display: flex; align-items: center; gap: 10px; width: 90%; max-width: 350px; justify-content: center; }
        .toast.show { bottom: 20px; }
    </style>
</head>
<body class="p-5 max-w-lg mx-auto">

    <header class="flex justify-between items-center mb-8 pt-2">
        <div class="flex items-center gap-3">
            <div class="w-10 h-10 bg-gradient-to-br from-purple-600 to-pink-600 rounded-xl flex items-center justify-center text-xl shadow-lg fox-glow">🦊</div>
            <div>
                <h1 class="text-sm font-bold text-white tracking-wide">RAFINÁLT RÓKA</h1>
                <p class="text-[10px] text-zinc-400 font-medium">Privát Csoport</p>
            </div>
        </div>
        
        <div class="flex gap-2">
            <a href="/stats" class="bg-zinc-900 hover:bg-zinc-800 text-zinc-400 px-3 py-2 rounded-lg text-[10px] font-bold transition flex items-center gap-1"><span>📊</span> STATS</a>
            <% if (user.isAdmin || user.email === 'stylefaqu@gmail.com') { %>
                <a href="/admin" class="bg-purple-900/50 hover:bg-purple-600 text-purple-200 border border-purple-500/30 px-3 py-2 rounded-lg text-[10px] font-bold transition flex items-center gap-1"><span>⚡</span> HQ</a>
            <% } %>
            <a href="/logout" class="bg-zinc-900 hover:bg-zinc-800 text-zinc-400 px-4 py-2 rounded-full text-[10px] font-bold transition flex items-center">EXIT</a>
        </div>
    </header>

    <div class="mb-8">
        <p class="text-zinc-500 text-xs font-bold uppercase mb-1"><%= displayDate %></p>
        <h2 class="text-2xl font-bold text-white mb-4">Szia, <span class="text-purple-400"><%= user.fullname.split(' ')[0] %></span>! 👋</h2>
        
        <div class="bg-[#0a0a0a] p-3 rounded-xl purple-pink-border w-full overflow-hidden relative">
            <div class="flex items-center gap-2 mb-2 border-b border-purple-500/20 pb-2">
                <div class="text-xl fox-pulse">🦊</div> <p class="text-[10px] font-bold text-purple-300 uppercase tracking-widest">Bejövő Róka Üzenet...</p>
            </div>

            <div class="marquee-container">
                <p id="dynamicQuote" class="text-sm orange-neon-text italic marquee-text">
                    "<%= foxQuotes[0] %>"
                </p>
            </div>
            
            <div class="absolute top-0 left-0 w-full h-full bg-gradient-to-b from-transparent via-purple-500/5 to-transparent opacity-30 pointer-events-none animate-scan"></div>
        </div>
    </div>

    <% if (dailyTip && dailyTip.isPublished) { %>
        <div class="startup-card p-6 mb-8 relative overflow-hidden border-t-2 border-t-purple-500">
            <div class="absolute top-0 right-0 bg-purple-600/20 text-purple-300 text-[9px] font-bold px-3 py-1 rounded-bl-xl border-l border-b border-purple-500/30">MAI FIX</div>
            <div class="text-center mb-6 mt-2">
                <p class="text-[10px] text-zinc-500 font-bold uppercase tracking-widest mb-2"><%= dailyTip.league %></p>
                <h3 class="text-lg font-black text-white leading-tight mb-4"><%= dailyTip.match %></h3>
                <div class="bg-zinc-900/80 inline-block px-6 py-3 rounded-2xl border border-zinc-800 w-full">
                    <p class="text-[10px] text-zinc-500 uppercase font-bold mb-1">A Róka Tippje</p>
                    <p class="text-xl font-black gradient-text"><%= dailyTip.prediction %></p>
                    <p class="text-xs font-bold text-white mt-1">Odds: <span class="text-purple-400"><%= dailyTip.odds %></span></p>
                </div>
            </div>
            <div class="bg-purple-900/10 border border-purple-500/20 p-4 rounded-xl relative">
                <span class="absolute -top-3 left-4 bg-[#111] px-2 text-xl">🦊</span>
                <p class="text-xs text-[#F3E5AB] italic leading-relaxed mt-2 font-medium">
                    "<%= dailyTip.memberMessage ? dailyTip.memberMessage : 'A Róka épp fogalmazza az üzenetet... (Admin: Nyomj a Szöveg Újragenerálásra!)' %>"
                </p>
            </div>
            <div class="mt-4 flex justify-between items-center border-t border-white/5 pt-4">
                <span class="text-[10px] text-zinc-500 font-bold uppercase">Ajánlott Tét</span>
                <span class="text-sm font-bold text-white bg-zinc-800 px-3 py-1 rounded-lg"><%= dailyTip.recommendedStake %></span>
            </div>
        </div>
    <% } else { %>
        <div class="startup-card p-8 text-center mb-8 border-dashed border border-zinc-800">
            <div class="w-16 h-16 bg-zinc-900 rounded-full flex items-center justify-center mx-auto mb-4 text-3xl animate-pulse">⏳</div>
            <h3 class="text-lg font-bold text-white mb-2">A Róka még dolgozik...</h3>
            <p class="text-xs text-zinc-500 leading-relaxed">Az AI éppen a piacot elemzi. Hamarosan jön a fix profit!</p>
        </div>
    <% } %>

    <div class="mt-8 text-center">
        <a href="/stats" class="text-xs font-bold text-zinc-500 hover:text-white transition uppercase border-b border-zinc-700 pb-1">Megnézem a teljes statisztikát -></a>
    </div>

    <div id="toast" class="toast"><span>🚀</span><span>Csúcstechnológia. Fix Profit.</span></div>
    
    <script>
        window.addEventListener('load', () => {
            const toast = document.getElementById('toast');
            setTimeout(() => { toast.classList.add('show'); }, 500);
            setTimeout(() => { toast.classList.remove('show'); }, 4000);

            const quotes = <%- JSON.stringify(foxQuotes) %>;
            const quoteElement = document.getElementById('dynamicQuote');
            let index = 0;

            // Amikor az animáció véget ér (kimegy a képből), cseréljük a szöveget
            quoteElement.addEventListener('animationiteration', () => {
                index = (index + 1) % quotes.length;
                quoteElement.innerText = '"' + quotes[index] + '"';
            });
        });
    </script>
    <style>
        /* Extra szkennelés animáció a doboz hátterében */
        @keyframes scan { 0% { transform: translateY(-100%); } 100% { transform: translateY(100%); } }
        .animate-scan { animation: scan 4s linear infinite; }
    </style>
</body>
</html>