<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Skyhigh AI | Vezérlőpult</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { background-color: #000; color: white; font-family: 'Inter', sans-serif; }
        .tech-card { background: #050505; border: 1px solid #141414; border-radius: 24px; }
        .master-glow { border: 1px solid rgba(124, 58, 237, 0.3); background: linear-gradient(145deg, #0a0514 0%, #000 100%); }
        .chat-input { background: #080808; border: 1px solid #1a1a1a; }
        ::-webkit-scrollbar { width: 4px; }
        ::-webkit-scrollbar-thumb { background: #1a1a1a; border-radius: 10px; }
    </style>
</head>
<body class="p-4 md:p-8 max-w-7xl mx-auto">

    <header class="flex justify-between items-center mb-10">
        <div>
            <h1 class="text-2xl font-black italic tracking-tighter uppercase">Skyhigh<span class="text-purple-600">.AI</span></h1>
            <p class="text-[9px] text-zinc-600 font-bold uppercase tracking-widest mt-1">Operator: <%= user.fullname %></p>
        </div>
        <div class="flex gap-3">
            <% if (isAdmin) { %>
                <a href="/admin" class="bg-red-600/10 text-red-500 border border-red-600/20 px-4 py-2 rounded-xl text-[10px] font-black uppercase tracking-widest">Admin</a>
            <% } %>
            <a href="/logout" class="bg-zinc-900 text-zinc-400 px-4 py-2 rounded-xl text-[10px] font-black uppercase tracking-widest hover:text-white transition">Kilépés</a>
        </div>
    </header>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-8">
        <div class="tech-card p-6 flex flex-col items-center">
            <p class="text-[9px] text-zinc-600 uppercase font-black tracking-widest mb-1">Licenc Jog</p>
            <p class="text-lg font-black <%= user.hasLicense ? 'text-green-500' : 'text-red-500 animate-pulse' %> uppercase">
                <%= user.hasLicense ? 'Hitelesítve' : 'Zárolva' %>
            </p>
        </div>
        <div class="tech-card p-6 flex flex-col items-center border-purple-900/20">
            <p class="text-[9px] text-zinc-600 uppercase font-black tracking-widest mb-1">Következő Elemzés</p>
            <div id="countdown" class="text-2xl font-mono font-black text-white">00:00:00</div>
        </div>
        <div class="tech-card p-6 flex flex-col items-center">
            <p class="text-[9px] text-zinc-600 uppercase font-black tracking-widest mb-1">Aktív Tőke</p>
            <p class="text-xl font-mono font-bold text-purple-500 tracking-tighter"><%= (user.startingCapital || 0).toLocaleString() %> Ft</p>
        </div>
    </div>

    <div class="mb-8">
        <div class="tech-card p-8 md:p-16 master-glow relative overflow-hidden text-center min-h-[300px] flex flex-col justify-center">
            <% if (!user.hasLicense) { %>
                <div class="absolute inset-0 bg-black/95 z-20 flex flex-col items-center justify-center p-6">
                    <i class="fas fa-lock text-4xl mb-6 text-zinc-800"></i>
                    <h3 class="text-2xl font-black uppercase italic mb-2 tracking-tighter">A Master Elemzés Zárolva</h3>
                    <p class="text-xs text-zinc-600 mb-8 italic">Csak aktív licencel rendelkező operátorok számára.</p>
                    <button onclick="openPricing()" class="bg-white text-black px-12 py-4 rounded-2xl font-black uppercase text-xs tracking-widest hover:bg-purple-600 hover:text-white transition shadow-xl">Licenc Aktiválása</button>
                </div>
            <% } %>

            <h2 class="text-[10px] font-black text-purple-500 uppercase tracking-[12px] mb-10">Napi Stratégiai Protokoll</h2>
            
            <div id="waiting" class="py-10 text-zinc-700 italic tracking-widest text-sm">ANALÍZIS FOLYAMATBAN: 08:00...</div>
            
            <div id="activeTip" class="hidden animate-in fade-in duration-1000">
                <h3 class="text-4xl md:text-7xl font-black mb-6 uppercase tracking-tighter italic"><%= dailyTip ? dailyTip.match : '' %></h3>
                <div class="flex flex-wrap justify-center gap-4 mb-10">
                    <div class="bg-purple-600 text-white px-8 py-2 rounded-full font-black text-sm tracking-widest shadow-lg shadow-purple-900/40">
                        TIP: <%= dailyTip ? dailyTip.prediction : '' %> @ <%= dailyTip ? dailyTip.odds : '' %>
                    </div>
                    <div class="bg-zinc-900 border border-zinc-800 text-green-500 px-8 py-2 rounded-full font-black text-sm tracking-widest">
                        TÉT: <%= (user.startingCapital * 0.05).toLocaleString() %> FT
                    </div>
                </div>
                <p class="text-zinc-500 text-sm md:text-base italic max-w-3xl mx-auto border-t border-zinc-900 pt-8 leading-relaxed">
                    "<%= dailyTip ? dailyTip.reasoning : '' %>"
                </p>
            </div>
        </div>
    </div>

    <div class="grid grid-cols-1 gap-6">
        <div class="tech-card flex flex-col h-[500px] overflow-hidden">
            <div id="chatBox" class="flex-1 p-6 overflow-y-auto space-y-6 bg-black">
                <div class="flex justify-start">
                    <div class="bg-zinc-900/50 p-4 rounded-3xl rounded-tl-none border border-zinc-800 max-w-[85%] text-sm leading-relaxed text-zinc-300">
                        <strong class="text-purple-500 block text-[9px] uppercase mb-2 tracking-[3px]">Skyhigh Core AI 🧠</strong>
                        Üdvözlöm az Operatív Törzsben, <%= user.fullname.split(' ')[0] %>! A rendszerem készen áll a 6 hónapos ciklus vezénylésére. Hogyan segíthetek?
                    </div>
                </div>
            </div>
            <form id="chatForm" class="p-4 bg-zinc-950 border-t border-zinc-900 flex gap-3">
                <input type="text" id="userInput" placeholder="Üzenet a Core-nak..." class="flex-1 chat-input rounded-2xl px-6 py-4 text-sm outline-none focus:border-purple-600 transition">
                <button class="bg-purple-600 w-14 h-14 rounded-2xl flex items-center justify-center shadow-lg shadow-purple-900/20"><i class="fas fa-paper-plane"></i></button>
            </form>
        </div>
    </div>

    <div id="pricingModal" class="hidden fixed inset-0 z-[100] bg-black/95 backdrop-blur-xl flex items-center justify-center p-4">
        <div class="max-w-4xl w-full grid grid-cols-1 md:grid-cols-2 gap-6">
            <div class="tech-card p-10 text-center flex flex-col items-center">
                <h4 class="text-xs font-bold text-zinc-500 uppercase tracking-widest mb-4">Havi Hozzáférés</h4>
                <p class="text-5xl font-black mb-10">20.000 <span class="text-xs text-zinc-600">Ft</span></p>
                <button onclick="buy('monthly')" class="w-full bg-zinc-900 py-4 rounded-2xl font-black uppercase text-[10px] tracking-widest hover:bg-white hover:text-black transition">Kiválasztás</button>
            </div>
            <div class="tech-card p-10 border-purple-500/50 bg-purple-500/5 text-center flex flex-col items-center">
                <h4 class="text-xs font-bold text-purple-400 uppercase tracking-widest mb-4 italic">Éves Licenc</h4>
                <p class="text-5xl font-black mb-10 text-gradient text-white">180.000 <span class="text-xs text-zinc-600">Ft</span></p>
                <button onclick="buy('yearly')" class="w-full bg-purple-600 py-4 rounded-2xl font-black uppercase text-[10px] tracking-widest text-white shadow-xl shadow-purple-900/30">Aktiválás Most</button>
            </div>
            <button onclick="document.getElementById('pricingModal').classList.add('hidden')" class="md:col-span-2 text-[10px] text-zinc-700 font-bold uppercase tracking-widest">Bezárás</button>
        </div>
    </div>

    <% if (!user.startingCapital || user.startingCapital == 0) { %>
    <div id="onboarding" class="fixed inset-0 z-[110] bg-black flex items-center justify-center p-6 text-center">
        <div class="max-w-sm w-full">
            <h2 class="text-4xl font-black mb-4 italic uppercase tracking-tighter">Szoftver <span class="text-purple-600">Inicializálása</span></h2>
            <p class="text-zinc-500 text-xs mb-10 italic uppercase tracking-widest">Adja meg az induló tőkeegyenleget</p>
            <input type="number" id="customCap" placeholder="Összeg (Ft)" class="w-full bg-zinc-900 border border-zinc-800 p-5 rounded-2xl text-center text-xl font-mono mb-6 outline-none focus:border-purple-600">
            <button onclick="saveCap()" class="w-full bg-white text-black py-5 rounded-2xl font-black uppercase text-xs tracking-widest hover:bg-purple-600 hover:text-white transition duration-500">Rendszer Indítása</button>
        </div>
    </div>
    <% } %>

    <script>
        function updateUI() {
            const now = new Date(); const h = now.getHours();
            if(h >= 8) { document.getElementById('activeTip')?.classList.remove('hidden'); document.getElementById('waiting')?.classList.add('hidden'); }
            const target = new Date(); target.setHours(8,0,0,0); if(now > target) target.setDate(target.getDate()+1);
            const diff = target - now;
            const hours = Math.floor(diff/3600000).toString().padStart(2,'0');
            const mins = Math.floor((diff%3600000)/60000).toString().padStart(2,'0');
            const secs = Math.floor((diff%60000)/1000).toString().padStart(2,'0');
            document.getElementById('countdown').innerText = `${hours}:${mins}:${secs}`;
        }
        setInterval(updateUI, 1000); updateUI();

        function openPricing() { document.getElementById('pricingModal').classList.remove('hidden'); }
        async function buy(plan) {
            const res = await fetch('/api/activate-license', { method:'POST', headers:{'Content-Type':'application/json'}, body:JSON.stringify({plan}) });
            if((await res.json()).success) location.reload();
        }
        async function saveCap() {
            const capital = document.getElementById('customCap').value;
            if(!capital) return;
            await fetch('/api/set-capital', { method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({capital}) });
            location.reload();
        }

        const chatBox = document.getElementById('chatBox');
        document.getElementById('chatForm').onsubmit = async (e) => {
            e.preventDefault();
            const msg = document.getElementById('userInput').value;
            if(!msg) return;
            document.getElementById('userInput').value = '';
            chatBox.innerHTML += `<div class="flex justify-end mb-2"><div class="bg-purple-600 px-5 py-3 rounded-3xl rounded-tr-none text-sm font-bold shadow-lg shadow-purple-900/20">${msg}</div></div>`;
            chatBox.scrollTop = chatBox.scrollHeight;
            const res = await fetch('/api/chat', { method:'POST', headers:{'Content-Type':'application/json'}, body:JSON.stringify({message:msg}) });
            const data = await res.json();
            chatBox.innerHTML += `<div class="flex justify-start mb-2"><div class="bg-zinc-900/50 p-4 rounded-3xl rounded-tl-none border border-zinc-800 text-sm italic text-zinc-400"><strong class="text-purple-500 block text-[9px] uppercase mb-1 tracking-widest">Skyhigh Core</strong>${data.reply}</div></div>`;
            chatBox.scrollTop = chatBox.scrollHeight;
        };
    </script>
</body>
</html>