<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Skyhigh AI | Neural Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { background-color: #050505; color: white; font-family: 'Inter', sans-serif; }
        .glass-panel { background: rgba(15, 15, 15, 0.7); border: 1px solid rgba(139, 92, 246, 0.2); backdrop-filter: blur(10px); border-radius: 24px; }
        .neon-glow { box-shadow: 0 0 20px rgba(139, 92, 246, 0.2); }
    </style>
</head>
<body class="min-h-screen p-4 md:p-10">

    <div class="max-w-5xl mx-auto">
        <header class="flex justify-between items-center mb-10">
            <div>
                <h1 class="text-2xl font-black italic tracking-tighter text-purple-500">SKYHIGH AI</h1>
                <p class="text-[9px] uppercase tracking-[4px] text-zinc-500 font-bold">Neural Trading Interface</p>
            </div>
            <div class="flex items-center gap-4">
                <% if (user.isAdmin) { %>
                    <a href="/admin" class="bg-red-600/20 text-red-500 border border-red-500/30 px-4 py-2 rounded-full text-[10px] font-black uppercase tracking-widest">Admin Panel</a>
                <% } %>
                <div class="text-right">
                    <p class="text-[9px] text-zinc-600 uppercase">Üdvözöljük</p>
                    <p class="font-bold text-sm text-white"><%= user.fullname %></p>
                </div>
            </div>
        </header>

        <div class="glass-panel p-4 mb-8 flex justify-between items-center px-8 border-purple-500/40">
            <div class="flex items-center gap-3">
                <div class="w-2 h-2 rounded-full bg-green-500 animate-pulse"></div>
                <span class="text-[10px] text-green-500 font-black uppercase tracking-widest">Rendszer Aktív</span>
            </div>
            <div id="countdown" class="text-lg font-mono font-bold text-purple-400">00:00:00</div>
        </div>

        <div class="glass-panel p-8 md:p-12 mb-10 relative overflow-hidden neon-glow border-purple-500/30">
            <div class="absolute top-0 right-0 p-6 opacity-5 text-8xl font-black italic">AI</div>
            
            <p class="text-[10px] font-black text-purple-500 uppercase tracking-[5px] mb-6">Today's Master Prediction</p>

            <% if (dailyTip) { %>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-10 relative z-10">
                    <div>
                        <h2 class="text-4xl md:text-5xl font-black italic uppercase leading-none mb-4"><%= dailyTip.match %></h2>
                        <div class="flex items-center gap-4 mb-6">
                            <span class="bg-purple-600 text-white px-6 py-2 rounded-xl text-3xl font-black italic shadow-lg shadow-purple-600/40 tracking-tighter"><%= dailyTip.odds %></span>
                            <div class="text-xl font-bold italic text-zinc-400">TIP: <span class="text-white uppercase"><%= dailyTip.prediction %></span></div>
                        </div>
                    </div>
                    <div class="bg-black/40 p-6 rounded-2xl border border-white/5">
                        <p class="text-[10px] text-purple-500 font-black uppercase mb-3 tracking-widest">Matematikai Indoklás (AI)</p>
                        <p class="text-sm italic text-zinc-300 leading-relaxed">"<%= dailyTip.reasoning %>"</p>
                    </div>
                </div>
            <% } else { %>
                <div class="text-center py-10">
                    <p class="text-zinc-600 italic uppercase text-xs tracking-widest animate-pulse">A robot jelenleg elemzi a piacot... Master tipp érkezik 08:00-kor.</p>
                </div>
            <% } %>
        </div>

        <div class="mt-12">
            <h3 class="text-[10px] font-black text-zinc-600 uppercase tracking-[4px] mb-6 flex items-center gap-2">
                <i class="fas fa-history"></i> Utolsó 30 Nap Eredményei
            </h3>
            <div class="grid grid-cols-1 gap-3">
                <% if (history && history.length > 0) { %>
                    <% history.forEach(item => { %>
                        <div class="glass-panel p-4 flex justify-between items-center bg-zinc-900/30 hover:bg-zinc-900/50 transition border-white/5">
                            <span class="text-[11px] font-bold text-zinc-500 font-mono"><%= item.date %></span>
                            <span class="text-sm font-bold uppercase italic"><%= item.match %></span>
                            <div class="flex gap-4">
                                <span class="text-[11px] font-black text-purple-500 uppercase"><%= item.prediction %></span>
                                <span class="text-[11px] font-bold text-zinc-600"><%= item.odds %></span>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <p class="text-zinc-700 text-xs italic">Még nincsenek korábbi adatok.</p>
                <% } %>
            </div>
        </div>

        <footer class="mt-16 text-center">
            <a href="/logout" class="text-[10px] text-zinc-700 font-bold uppercase tracking-widest hover:text-red-500 transition">Kijelentkezés a terminálból</a>
        </footer>
    </div>

    <script>
        function updateTimer() {
            const now = new Date();
            const target = new Date();
            target.setHours(8, 0, 0, 0);
            if (now > target) target.setDate(target.getDate() + 1);
            
            const diff = target - now;
            const h = Math.floor(diff / 3600000).toString().padStart(2, '0');
            const m = Math.floor((diff % 3600000) / 60000).toString().padStart(2, '0');
            const s = Math.floor((diff % 60000) / 1000).toString().padStart(2, '0');
            
            document.getElementById('countdown').innerText = `NEXT MASTER TIP: ${h}:${m}:${s}`;
        }
        setInterval(updateTimer, 1000);
        updateTimer();
    </script>
</body>
</html>