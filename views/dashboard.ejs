<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Dashboard | Skyhigh AI</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@800&family=Inter:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body { background: #050505; color: #fff; font-family: 'Inter', sans-serif; }
        .fox-card { background: #0a0a0a; border-radius: 2rem; border-top: 4px solid #8b5cf6; }
    </style>
</head>
<body class="p-4 md:p-8 flex justify-center min-h-[100dvh]">
    <div class="max-w-md w-full">
        <header class="mb-10 pt-4 flex justify-between items-center border-b border-white/5 pb-6">
            <div>
                <p style="font-family: 'Orbitron';" class="text-[9px] text-purple-500 uppercase tracking-[4px]">Neural Access</p>
                <h1 class="text-lg font-bold italic"><%= displayDate %></h1>
            </div>
            <% if (user.isAdmin) { %><a href="/admin" class="bg-purple-600 px-3 py-1.5 rounded-full text-[9px] font-black uppercase">Admin HQ</a><% } %>
        </header>

        <div class="bg-purple-600/10 p-3 rounded-2xl mb-8 text-[10px] font-black uppercase text-purple-400 text-center tracking-widest">Várható elemzés: <%= nextTipText %></div>

        <div class="fox-card p-8 shadow-2xl relative mb-10 overflow-hidden">
            <div class="flex items-center gap-4 mb-8">
                <div class="text-4xl">🦊💜</div>
                <div><h2 class="text-[10px] font-black uppercase text-purple-400">Rafinált Robot Róka</h2><p class="text-[9px] text-zinc-500 italic">30 napos stratégia aktív</p></div>
            </div>

            <% if (dailyTip) { %>
                <p class="text-zinc-400 text-xs italic mb-8 leading-relaxed text-left">"<%= dailyTip.reasoning %>"</p>
                <div class="bg-purple-600/10 border-l-4 border-purple-500 p-6 mb-8 text-left rounded-r-xl">
                    <p class="text-[9px] text-purple-400 font-black uppercase mb-1"><%= dailyTip.league %></p>
                    <h3 class="text-xl font-black uppercase text-white mb-2"><%= dailyTip.match %></h3>
                    <p class="text-lg font-black italic text-purple-300 uppercase"><%= dailyTip.prediction %></p>
                </div>
                <div class="grid grid-cols-2 gap-4 mb-8">
                    <div class="bg-white/5 p-4 rounded-2xl text-center"><p class="text-[8px] text-zinc-500 font-bold uppercase">Idő</p><p class="text-base font-black"><%= dailyTip.matchTime %></p></div>
                    <div class="bg-white/5 p-4 rounded-2xl text-center"><p class="text-[8px] text-zinc-500 font-bold uppercase">Odds</p><p class="text-base font-black text-purple-400">@<%= dailyTip.odds %></p></div>
                </div>
                <div class="bg-zinc-900/80 p-6 rounded-[2rem] text-center border border-white/5 shadow-xl">
                    <p class="text-[10px] text-zinc-500 font-bold uppercase mb-2">Tétjavaslat:</p>
                    <p class="text-3xl font-black text-green-500 italic"><%= recommendedStake.toLocaleString() %> Ft</p>
                </div>
            <% } else { %>
                <div class="py-20 text-center text-zinc-600 text-[10px] font-black uppercase animate-pulse">A Róka elemzést végez...</div>
            <% } %>
        </div>

        <div class="px-6 flex justify-between items-center opacity-40 text-[9px] font-bold uppercase italic">
            <p>Tőke: <%= user.startingCapital.toLocaleString() %> Ft</p>
            <a href="/logout">Kilépés</a>
        </div>
    </div>
</body>
</html>