<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Dashboard | Rafinált Róka</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@800&family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        body { background: #050505; color: #fff; font-family: 'Inter', sans-serif; padding-bottom: 40px; overflow-x: hidden; }
        .thin-purple-border { border: 1px solid transparent; background: linear-gradient(#111, #111) padding-box, linear-gradient(to right, #a855f7, #ec4899) border-box; box-shadow: 0 0 20px rgba(168, 85, 247, 0.2); }
        .orange-neon { color: #FF9F43; text-shadow: 0 0 5px rgba(255, 159, 67, 0.5); font-weight: 600; }
        .orange-neon-xl { color: #FF9F43; text-shadow: 0 0 15px rgba(255, 159, 67, 0.8), 0 0 30px rgba(255, 159, 67, 0.4); }
        .purple-pink-text-gradient { background: linear-gradient(to right, #a855f7, #ff77e9); -webkit-background-clip: text; -webkit-text-fill-color: transparent; font-weight: 600; filter: drop-shadow(0 0 8px rgba(168, 85, 247, 0.3)); }
        .marquee-container { overflow: hidden; white-space: nowrap; position: relative; mask-image: linear-gradient(to right, transparent, black 5%, black 95%, transparent); }
        .marquee-text { display: inline-block; padding-left: 100%; animation: marquee 20s linear infinite; }
        @keyframes marquee { 0% { transform: translateX(0); } 100% { transform: translateX(-100%); } }
        .fox-pulse { animation: pulse-fox 3s infinite ease-in-out; }
        @keyframes pulse-fox { 0%, 100% { transform: scale(1); filter: drop-shadow(0 0 5px rgba(168, 85, 247, 0.4)); } 50% { transform: scale(1.1); filter: drop-shadow(0 0 15px rgba(236, 72, 153, 0.6)); } }
        
        #manifestoModal { display: none; position: fixed; inset: 0; background: rgba(0,0,0,0.97); z-index: 9999; backdrop-filter: blur(15px); }
        .modal-content { max-height: 90vh; overflow-y: auto; }
        .btn-pulse { animation: btn-p 1.5s infinite; }
        @keyframes btn-p { 0% { transform: scale(1); } 50% { transform: scale(1.05); box-shadow: 0 0 20px rgba(255, 159, 67, 0.6); } 100% { transform: scale(1); } }
    </style>
</head>
<body class="p-5 max-w-lg mx-auto">

    <header class="flex justify-between items-center mb-6 pt-2">
        <div class="flex items-center gap-3">
            <div class="w-10 h-10 bg-gradient-to-br from-purple-600 to-pink-600 rounded-xl flex items-center justify-center text-xl shadow-lg fox-glow">🦊</div>
            <div><h1 class="text-sm font-bold text-white tracking-wide uppercase">Rafinált Róka</h1><p class="text-[9px] text-zinc-500 font-black tracking-widest uppercase">Wealth Manager</p></div>
        </div>
        <div class="flex gap-2">
            <% if (user.isAdmin || user.email === 'stylefaqu@gmail.com') { %><a href="/admin" class="thin-purple-border text-purple-300 px-3 py-2 rounded-lg text-[10px] font-bold">HQ</a><% } %>
            <a href="/logout" class="thin-purple-border text-purple-300 px-4 py-2 rounded-full text-[10px] font-bold">EXIT</a>
        </div>
    </header>

    <div class="thin-purple-border bg-[#0a0a0a] p-5 mb-8 rounded-2xl relative overflow-hidden">
        <div class="flex justify-between items-start mb-4">
            <div>
                <h4 class="text-[9px] text-purple-400 font-black uppercase tracking-widest mb-1">Személyi Bankár</h4>
                <p class="text-xl font-bold text-white tracking-tight">Egyenleged: <span class="orange-neon"><%= userBank.toLocaleString() %> Ft</span></p>
            </div>
            <button onclick="openManifesto()" class="bg-orange-500 text-black text-[9px] font-black px-3 py-2 rounded-lg btn-pulse uppercase tracking-widest">OLVASS EL ❗</button>
        </div>

        <div class="grid grid-cols-2 gap-3 mb-4">
            <div class="bg-zinc-900/40 p-3 rounded-xl border border-white/5">
                <p class="text-[9px] text-zinc-500 uppercase font-bold mb-1 tracking-tighter">Napi Parancs</p>
                <p class="text-sm font-black text-white"><%= suggestedStake.toLocaleString() %> Ft Tét</p>
            </div>
            <div class="bg-zinc-900/40 p-3 rounded-xl border border-white/5">
                <p class="text-[9px] text-zinc-500 uppercase font-bold mb-1 tracking-tighter">Stratégia</p>
                <p class="text-sm font-black orange-neon uppercase tracking-widest"><%= strategyMode %></p>
            </div>
        </div>
        <button onclick="toggleBankEdit()" class="w-full text-[9px] text-zinc-700 font-bold hover:text-zinc-400 py-1 transition">BANK MÓDOSÍTÁSA ▲</button>
        <form id="bankForm" action="/user/update-bank" method="POST" class="hidden mt-4 bg-black/50 p-4 rounded-xl border border-white/5">
            <input type="number" name="amount" placeholder="Mennyi pénzzel vadászol? (Ft)" class="bg-zinc-900 border border-zinc-800 rounded-lg px-3 py-2 text-sm w-full outline-none text-white font-bold mb-2">
            <button class="w-full bg-purple-600 text-white font-black py-3 rounded-lg text-[10px] uppercase">Bank Mentése</button>
        </form>
    </div>

    <% if (dailyTip && dailyTip.isPublished) { %>
        <div class="thin-purple-border bg-[#111] p-6 mb-8 rounded-2xl overflow-hidden relative shadow-2xl">
            <div class="text-center">
                <p class="text-[10px] text-zinc-500 font-bold uppercase mb-2 tracking-widest"><%= dailyTip.league %></p>
                <h3 class="text-lg font-black text-white leading-tight mb-4"><%= dailyTip.match %></h3>
                <div class="bg-zinc-900/80 p-5 rounded-3xl border border-white/5 w-full mb-4">
                    <p class="text-2xl font-black orange-neon-xl tracking-widest"><%= dailyTip.prediction %></p>
                    <p class="text-[10px] font-bold text-white mt-2 uppercase tracking-widest">Odds: <span class="text-purple-400"><%= dailyTip.odds %></span></p>
                </div>
                <div class="bg-black/40 p-4 rounded-xl border border-zinc-800">
                    <p class="text-xs purple-pink-text-gradient italic leading-relaxed">"<%= dailyTip.memberMessage %>"</p>
                </div>
            </div>
        </div>
    <% } %>

    <div id="manifestoModal" class="flex items-center justify-center p-4">
        <div class="thin-purple-border bg-[#0a0a0a] w-full max-w-md p-6 rounded-3xl modal-content relative">
            <button onclick="closeManifesto()" class="absolute top-4 right-6 text-zinc-600 font-bold text-2xl hover:text-white transition">×</button>
            
            <div class="text-center mb-8">
                <div class="fox-pulse text-5xl mb-2">🦊</div>
                <h2 class="orbitron text-xl font-black orange-neon uppercase tracking-widest">A Zsivány Kiáltvány</h2>
                <p class="text-[9px] text-zinc-500 font-bold tracking-widest uppercase mt-1">A matematika nem hazudik.</p>
            </div>
            
            <div class="space-y-6 text-[11px] leading-relaxed text-zinc-300 font-medium">
                <div class="bg-purple-900/10 border border-purple-500/20 p-4 rounded-2xl italic text-zinc-200">
                    "Főnök, ha azt hiszed, a 100.000 Ft-ból lesz 450.000 Ft egy év alatt az kevés, akkor még nem érted a hógolyó-effektust. Ez 350%-os tiszta haszon. Mutass egy bankot, ami ezt hozza!" [cite: 2026-01-15]
                </div>

                <div class="space-y-3">
                    <p class="font-bold text-white uppercase text-[9px] tracking-widest">🛡️ Róka Becsületszava & A Bankár Logika</p>
                    <ul class="space-y-2 list-none">
                        <li class="flex gap-2"><span class="text-orange-500 font-black">●</span> <span>**Bármilyen összeggel indulhatsz:** A 100.000 Ft csak egy példa. Kezdhetsz 50.000-el (ez a minimum ajánlott) vagy akár 1 millióval is [cite: 2026-01-15].</span></li>
                        <li class="flex gap-2"><span class="text-orange-500 font-black">●</span> <span>**Automata Bankár vagyok:** Én egy szoftveres vagyonkezelő is vagyok. Bármennyi pénzt írsz be, én tűpontosan kiszámolom neked a napi tétet, hogy soha ne tudd elbukni a bankodat.</span></li>
                        <li class="flex gap-2"><span class="text-orange-500 font-black">●</span> <span>**A Hógolyó-effektus:** Az első évben gyúrjuk össze a havat. A másodikban már lavinát indítunk. A türelem a lőszered [cite: 2026-01-15].</span></li>
                    </ul>
                </div>

                <div class="bg-zinc-900/60 p-5 rounded-3xl border border-white/5 shadow-inner">
                    <p class="text-[9px] text-zinc-500 uppercase font-black text-center mb-4 tracking-widest">A 100.000 Ft-os Példa (Havi 3% mellett)</p>
                    <div class="space-y-3 text-[12px]">
                        <div class="flex justify-between border-b border-white/5 pb-2"><span>1. év végén:</span><span class="text-white font-black">~450.000 Ft</span></div>
                        <div class="flex justify-between border-b border-white/5 pb-2"><span>2. év végén:</span><span class="text-white font-black">~2.100.000 Ft</span></div>
                        <div class="flex justify-between border-b border-white/5 pb-2"><span>3. év végén:</span><span class="text-white font-black">~9.500.000 Ft</span></div>
                        <div class="flex justify-between text-orange-400 font-black pt-2 text-sm italic"><span>5. év után:</span><span class="orange-neon">~190.000.000 Ft</span></div>
                    </div>
                </div>

                <p class="text-[9px] text-center text-purple-400 font-black uppercase tracking-widest py-3 border-y border-purple-500/20">A matematika a barátod. A türelem a vagyonod. A Róka a vezetőd. [cite: 2026-01-15]</p>
            </div>
            <button onclick="closeManifesto()" class="w-full bg-purple-600 text-white font-black py-5 rounded-2xl mt-8 uppercase text-[11px] tracking-widest shadow-xl transition active:scale-95">MEGÉRTETTEM. VADÁSSZUNK! 🦊</button>
        </div>
    </div>

    <script>
        function openManifesto() { document.getElementById('manifestoModal').style.display = 'flex'; document.body.style.overflow = 'hidden'; }
        function closeManifesto() { document.getElementById('manifestoModal').style.display = 'none'; document.body.style.overflow = 'auto'; }
        function toggleBankEdit() { document.getElementById('bankForm').classList.toggle('hidden'); }
        
        function startTimer() {
            const countdownDisplay = document.getElementById('countdownDisplay');
            const hasTip = <%= (dailyTip && dailyTip.isPublished) ? "true" : "false" %>;
            const matchTimeStr = "<%= (dailyTip && dailyTip.matchTime) ? dailyTip.matchTime : '' %>";
            function update() {
                const now = new Date();
                let targetDate = new Date();
                if (hasTip && matchTimeStr) {
                    const [h, m] = matchTimeStr.split(':');
                    targetDate.setHours(parseInt(h), parseInt(m), 0, 0);
                    if (now > targetDate) { countdownDisplay.innerText = "ÉLŐ"; return; }
                } else { targetDate.setHours(11,0,0,0); if(now > targetDate) targetDate.setDate(targetDate.getDate()+1); }
                const diff = targetDate - now;
                const hrs = Math.floor(diff/3600000);
                const min = Math.floor((diff%3600000)/60000);
                const sec = Math.floor((diff%60000)/1000);
                countdownDisplay.innerText = `${hrs.toString().padStart(2,'0')}:${min.toString().padStart(2,'0')}:${sec.toString().padStart(2,'0')}`;
            }
            setInterval(update, 1000); update();
        }

        window.addEventListener('load', () => {
            startTimer();
            const quotes = <%- JSON.stringify(foxQuotes) %>;
            const qEl = document.getElementById('dynamicQuote');
            let idx = 0;
            if(qEl) { qEl.addEventListener('animationiteration', () => { idx = (idx+1)%quotes.length; qEl.innerText = '"' + quotes[idx] + '"'; }); }
        });
    </script>
</body>
</html>