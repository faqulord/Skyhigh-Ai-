<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vezérlőpult | Rafinált Róka</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* --- A KLASSZIKUS STÍLUS (ADMIN PANELHEZ PASSZOL) --- */
        :root { 
            --primary: #FFD700; /* Róka Arany */
            --dark-bg: #1a1a1a; 
            --dark-card: #212121;
            --text-light: #e0e0e0;
            --border-color: #333;
        }

        body { 
            background-color: #000000; /* Teljesen fekete háttér, ahogy kérted */
            color: var(--text-light); 
            font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; 
        }

        .navbar { 
            border-bottom: 1px solid var(--border-color); 
            background: #111; 
        }

        /* Kártya stílus - Letisztult, szögletesebb, profi */
        .card-custom { 
            background: var(--dark-card); 
            border: 1px solid var(--border-color); 
            border-radius: 8px; 
            box-shadow: 0 4px 15px rgba(0,0,0,0.5); 
        }

        .card-header-custom {
            background: rgba(255, 215, 0, 0.05);
            border-bottom: 1px solid var(--border-color);
            padding: 15px 20px;
            border-radius: 8px 8px 0 0;
        }

        /* A Róka Üzenete - NEM kék, hanem elegáns */
        .fox-message-box {
            background: #111;
            border-left: 4px solid var(--primary); /* Csak egy arany csík */
            padding: 20px;
            border-radius: 4px;
            margin-top: 25px;
            font-style: italic;
        }

        .fox-message-text {
            color: #fff; /* Tiszta fehér szöveg */
            font-size: 1.1rem;
            line-height: 1.6;
            white-space: pre-line;
        }

        .stat-badge {
            background: #000;
            border: 1px solid var(--border-color);
            padding: 10px;
            border-radius: 6px;
            text-align: center;
        }
        
        .section-title {
            color: var(--primary);
            text-transform: uppercase;
            letter-spacing: 1px;
            font-size: 0.8rem;
            margin-bottom: 5px;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark fixed-top">
        <div class="container">
            <a class="navbar-brand fw-bold text-warning" href="#"><i class="fas fa-fox me-2"></i>Rafinált Róka</a>
            <div class="d-flex">
                <% if(user.isAdmin) { %> <a href="/admin" class="btn btn-sm btn-outline-warning me-2">ADMIN</a> <% } %>
                <a href="/logout" class="btn btn-sm btn-outline-secondary">Kilépés</a>
            </div>
        </div>
    </nav>

    <div class="container" style="margin-top: 100px;">
        
        <div class="d-flex justify-content-between align-items-center mb-4 border-bottom border-secondary pb-3">
            <div>
                <h3 class="mb-0 text-white">Vezérlőpult</h3>
                <small class="text-secondary">Üdvözöllek, <%= user.fullname %></small>
            </div>
            <div class="text-end">
                <div class="text-warning fw-bold fs-5"><%= user.startingCapital %> Ft</div>
                <small class="text-secondary">Egyenleg</small>
            </div>
        </div>

        <% if(streak > 0) { %> 
            <div class="alert alert-dark border-warning d-flex align-items-center mb-4 text-warning">
                <i class="fas fa-fire me-2"></i> Aktív Nyerő Széria: <strong>&nbsp;<%= streak %> WIN</strong>
            </div>
        <% } %>

        <div class="card card-custom mb-5">
            <div class="card-header-custom d-flex justify-content-between align-items-center">
                <div class="fw-bold text-warning"><i class="fas fa-calendar-day me-2"></i>MAI TIPP (<%= displayDate %>)</div>
                <% if(dailyTip && dailyTip.isReal) { %>
                    <span class="badge bg-success">ÉLŐ ADAT</span>
                <% } else { %>
                    <span class="badge bg-secondary">DEMÓ</span>
                <% } %>
            </div>

            <div class="card-body p-4">
                <% if (dailyTip && dailyTip.isPublished) { %>
                    
                    <div class="row mb-4">
                        <div class="col-md-8">
                            <div class="section-title">MÉRKŐZÉS</div>
                            <h2 class="fw-bold text-white mb-1"><%= dailyTip.match %></h2>
                            <span class="badge bg-secondary"><%= dailyTip.league %></span>
                            <span class="badge bg-dark border border-secondary"><%= dailyTip.bookmaker %></span>
                        </div>
                        <div class="col-md-4 text-md-end mt-3 mt-md-0">
                            <div class="section-title">TIPP & ODDS</div>
                            <div class="fs-4 text-white fw-bold"><%= dailyTip.prediction %></div>
                            <div class="fs-3 text-warning fw-bold">@<%= dailyTip.odds %></div>
                        </div>
                    </div>

                    <div class="row g-2 mb-4">
                        <div class="col-6">
                            <div class="stat-badge">
                                <div class="section-title"><i class="far fa-clock"></i> KEZDÉS</div>
                                <div class="fw-bold text-white"><%= dailyTip.matchTime %></div>
                            </div>
                        </div>
                        <div class="col-6">
                            <div class="stat-badge" style="border-color: #ffd700;">
                                <div class="section-title"><i class="fas fa-coins"></i> TÉT</div>
                                <div class="fw-bold text-warning"><%= dailyTip.recommendedStake %></div>
                            </div>
                        </div>
                    </div>

                    <hr style="border-color: #333;">

                    <div class="fox-message-box">
                        <div class="d-flex">
                            <i class="fas fa-quote-left text-warning fs-3 me-3 opacity-50"></i>
                            <div>
                                <strong class="text-warning d-block mb-2">A RÓKA ELEMZÉSE:</strong>
                                <p class="fox-message-text mb-0">
                                    <%= dailyTip.memberMessage %>
                                </p>
                            </div>
                        </div>
                    </div>

                <% } else { %>
                    <div class="text-center py-5">
                        <div class="spinner-border text-warning mb-3" role="status"></div>
                        <h4 class="text-white">Elemzés folyamatban...</h4>
                        <p class="text-secondary">A rendszer éppen a legjobb lehetőségeket kutatja.</p>
                    </div>
                <% } %>
            </div>
        </div>
        
        <div class="text-center text-secondary small mb-5 opacity-50">
            &copy; 2