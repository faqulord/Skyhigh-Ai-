<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Skyhigh AI | Az Öreg Róka</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        body { background: #000; color: #fff; font-family: 'Inter', sans-serif; }
        .message-bubble { background: #0a0a0a; border: 1px solid #1f1f1f; border-radius: 24px; }
        .fox-accent { border-left: 4px solid #f97316; background: rgba(249, 115, 22, 0.05); }
    </style>
</head>
<body class="p-4 md:p-8 flex justify-center min-h-screen">
    <div class="max-w-md w-full pb-12">
        <div class="flex justify-between items-center mb-10">
            <h1 class="text-xl font-black italic text-orange-500 uppercase tracking-tighter">Skyhigh AI</h1>
            <% if (user.isAdmin) { %><a href="/admin" class="text-[10px] bg-zinc-900 px-3 py-1 rounded-full uppercase font-bold text-zinc-500">Admin</a><% } %>
        </div>

        <div class="flex items-center gap-4 mb-8">
            <div class="w-14 h-14 bg-zinc-800 rounded-full flex items-center justify-center shadow-lg border-2 border-orange-500/30 text-3xl">👴🦊</div>
            <div>
                <h2 class="font-bold text-sm uppercase tracking-widest">Az Öreg Róka | Master Elemző</h2>
                <p class="text-[9px] text-orange-500 font-bold uppercase animate-pulse">Elemzés a pályák széléről...</p>
            </div>
        </div>

        <div class="message-bubble p-6 shadow-2xl relative border-t-4 border-orange-500">
            <p class="text-zinc-600 text-[9px] mb-4 uppercase font-black tracking-widest border-b border-white/5 pb-2">Friss üzenet: <%= new Date().toLocaleTimeString('hu-HU', {hour:'2-digit', minute:'2-digit'}) %></p>
            
            <% if (dailyTip) { %>
                <p class="text-zinc-300 text-sm leading-relaxed mb-6 italic italic">"Ide figyelj barátom. Átfésültem a mai kínálatot, és a 10 éves statisztikák nem hazudnak. Ez itt a nap tutija, ezt kell megraknod:"</p>

                <div class="fox-accent p-5 mb-8">
                    <p class="text-[9px] text-orange-400 font-black uppercase mb-2 tracking-widest">A Nap Mérkőzése</p>
                    <h3 class="text-2xl font-black uppercase mb-1 leading-tight"><%= dailyTip.match %></h3>
                    <p class="text-xl font-black text-white italic underline decoration-orange-500 decoration-4 underline-offset-4 uppercase tracking-tighter"><%= dailyTip.prediction %></p>
                </div>

                <div class="grid grid-cols-2 gap-4 mb-8">
                    <div class="bg-black border border-white/5 p-4 rounded-2xl">
                        <p class="text-[8px] text-zinc-500 uppercase font-black mb-1 tracking-widest">Ennyivel rakd meg (10%):</p>
                        <p class="text-2xl font-black text-green-500"><%= recommendedStake.toLocaleString() %> Ft</p>
                    </div>
                    <div class="bg-black border border-white/5 p-4 rounded-2xl">
                        <p class="text-[8px] text-zinc-500 uppercase font-black mb-1 tracking-widest">Szorzó (Odds):</p>
                        <p class="text-2xl font-black"><%= dailyTip.odds %></p>
                    </div>
                </div>

                <div class="flex justify-between text-[10px] text-zinc-500 mb-8 border-t border-white/5 pt-4 uppercase font-bold">
                    <span>Kezdés: <b class="text-white"><%= dailyTip.matchTime %></b></span>
                    <span>Iroda: <b class="text-white"><%= dailyTip.bookmaker %></b></span>
                </div>

                <div class="bg-zinc-900/50 p-5 rounded-2xl border border-white/5">
                    <h4 class="text-[10px] font-black text-orange-500 uppercase mb-3 tracking-widest">Miért ezt választottam?</h4>
                    <p class="text-[11px] text-zinc-400 leading-relaxed italic">"<%= dailyTip.reasoning %>"</p>
                </div>

            <% } else { %>
                <div class="py-12 text-center">
                    <p class="text-zinc-600 text-[10px] font-black uppercase tracking-[0.3em] animate-pulse">Az Öreg Róka épp a statisztikákat bújja...</p>
                </div>
            <% } %>
        </div>

        <div class="mt-8 flex justify-between items-center px-4">
            <p class="text-[10px] text-zinc-600 uppercase font-bold tracking-widest italic">Tőkéd: <b><%= user.startingCapital.toLocaleString() %> Ft</b></p>
            <a href="/logout" class="text-[10px] text-red-500/50 hover:text-red-500 transition uppercase font-black">Kilépés</a>
        </div>
    </div>
</body>
</html>