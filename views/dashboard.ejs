<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Skyhigh | Az Öreg Róka</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        body { background: #000; color: #fff; font-family: 'Inter', sans-serif; }
        .message-bubble { background: #0a0a0a; border: 1px solid #1f1f1f; border-radius: 24px; border-top: 4px solid #f97316; }
        .highlight { background: rgba(249, 115, 22, 0.1); border-left: 4px solid #f97316; }
    </style>
</head>
<body class="p-4 md:p-8 flex justify-center min-h-screen">
    <div class="max-w-md w-full pb-12">
        <header class="flex justify-between items-center mb-10">
            <h1 class="text-xl font-black italic text-orange-500 uppercase">Skyhigh AI</h1>
            <div class="flex gap-3">
                <% if (user.isAdmin) { %><a href="/admin" class="bg-zinc-800 px-3 py-1 rounded-full text-[9px] font-bold text-zinc-400">ADMIN</a><% } %>
                <a href="/logout" class="text-zinc-600 text-[9px] font-bold">KILÉPÉS</a>
            </div>
        </header>

        <div class="flex items-center gap-4 mb-6">
            <div class="w-14 h-14 bg-zinc-800 rounded-full flex items-center justify-center border-2 border-orange-500/30 text-3xl shadow-lg shadow-orange-500/10">👴</div>
            <div>
                <h2 class="font-bold text-xs uppercase tracking-widest">Az Öreg Róka Tanácsa</h2>
                <p class="text-[9px] text-orange-500 font-bold uppercase animate-pulse">Azonnali elemzés kész</p>
            </div>
        </div>

        <div class="message-bubble p-6 shadow-2xl">
            <% if (dailyTip) { %>
                <p class="text-zinc-400 text-xs italic mb-6 leading-relaxed">"<%= dailyTip.reasoning %>"</p>

                <div class="highlight p-5 mb-6">
                    <p class="text-[9px] text-orange-500 font-black uppercase mb-1 tracking-widest"><%= dailyTip.league %></p>
                    <h3 class="text-xl font-black uppercase mb-1"><%= dailyTip.match %></h3>
                    <p class="text-lg font-black text-white italic underline underline-offset-4 decoration-orange-500"><%= dailyTip.prediction %></p>
                </div>

                <div class="grid grid-cols-2 gap-4 mb-6 text-center">
                    <div class="bg-black/50 p-4 rounded-2xl border border-white/5">
                        <p class="text-[8px] text-zinc-500 uppercase font-black mb-1">Mikor?</p>
                        <p class="text-base font-black"><%= dailyTip.matchTime %></p>
                    </div>
                    <div class="bg-black/50 p-4 rounded-2xl border border-white/5">
                        <p class="text-[8px] text-zinc-500 uppercase font-black mb-1">Odds</p>
                        <p class="text-base font-black"><%= dailyTip.odds %></p>
                    </div>
                </div>

                <div class="bg-zinc-900/50 p-5 rounded-3xl border border-white/5 mb-6">
                    <div class="flex justify-between items-center mb-4">
                        <p class="text-[10px] text-zinc-400 font-bold uppercase">Javasolt Tét:</p>
                        <p class="text-xl font-black text-green-500"><%= recommendedStake.toLocaleString() %> Ft</p>
                    </div>
                    <div class="flex justify-between items-center border-t border-white/5 pt-4">
                        <p class="text-[10px] text-zinc-400 font-bold uppercase">Hol rakd meg?</p>
                        <p class="text-xs font-black text-white uppercase"><%= dailyTip.bookmaker %></p>
                    </div>
                </div>

            <% } else { %>
                <div class="py-10 text-center text-zinc-600 text-xs font-bold uppercase animate-pulse">Az Öreg Róka épp a statisztikákat bújja...</div>
            <% } %>
        </div>

        <div class="mt-8 flex justify-between items-center px-4">
            <p class="text-[10px] text-zinc-500 uppercase font-bold tracking-widest italic">Bankroll: <b><%= user.startingCapital.toLocaleString() %> Ft</b></p>
        </div>
    </div>
</body>
</html>