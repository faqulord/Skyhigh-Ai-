<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Skyhigh AI | Neural Access</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@700&family=Inter:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        body { background: #050505; color: #fff; font-family: 'Inter', sans-serif; }
        .orbitron { font-family: 'Orbitron', sans-serif; }
        .card-glass { background: rgba(15, 15, 15, 0.9); border: 1px solid rgba(139, 92, 246, 0.2); }
        .neon-glow { text-shadow: 0 0 10px rgba(139, 92, 246, 0.5); }
    </style>
</head>
<body class="p-4 md:p-8">
    <div class="max-w-md mx-auto">
        <header class="flex justify-between items-center mb-8">
            <h1 class="orbitron text-xl font-black text-purple-500 italic neon-glow">SKYHIGH AI</h1>
            <% if (user.isAdmin) { %><a href="/admin" class="bg-purple-600/20 text-purple-400 border border-purple-500/30 px-3 py-1 rounded-full text-[9px] font-black uppercase">Admin</a><% } %>
        </header>

        <div class="card-glass p-5 rounded-3xl mb-6 flex justify-between items-center shadow-xl">
            <div>
                <p class="text-[9px] text-zinc-500 uppercase font-black tracking-widest">Saját Bankroll</p>
                <p class="text-xl font-black"><%= user.startingCapital.toLocaleString() %> Ft</p>
            </div>
            <div class="text-right">
                <p class="text-[9px] text-zinc-500 uppercase font-black tracking-widest">Havi Cél</p>
                <p class="text-sm font-bold text-green-500">+45%</p>
            </div>
        </div>

        <div class="bg-gradient-to-br from-zinc-900 to-black border border-purple-500/40 rounded-[2.5rem] p-6 mb-8 relative overflow-hidden">
            <div class="absolute -top-10 -left-10 w-32 h-32 bg-purple-600/5 blur-3xl"></div>
            
            <div class="flex justify-between items-center mb-6">
                <span class="bg-purple-600 text-[9px] px-3 py-1 rounded-full font-black uppercase tracking-widest">Nap Fix Tippje</span>
                <span class="text-[10px] text-zinc-500 font-mono font-bold"><%= new Date().toLocaleDateString('hu-HU') %></span>
            </div>

            <% if (dailyTip) { %>
                <div class="mb-8">
                    <h2 class="text-2xl font-black uppercase leading-tight mb-1"><%= dailyTip.match %></h2>
                    <p class="text-purple-400 font-bold italic text-sm"><%= dailyTip.prediction %></p>
                </div>

                <div class="grid grid-cols-3 gap-3 mb-8">
                    <div class="bg-white/5 p-3 rounded-2xl border border-white/5 text-center">
                        <p class="text-[8px] text-zinc-500 uppercase font-bold mb-1">Kezdés</p>
                        <p class="text-sm font-black"><%= dailyTip.matchTime %></p>
                    </div>
                    <div class="bg-white/5 p-3 rounded-2xl border border-white/5 text-center">
                        <p class="text-[8px] text-zinc-500 uppercase font-bold mb-1">Szorzó</p>
                        <p class="text-sm font-black"><%= dailyTip.odds %></p>
                    </div>
                    <div class="bg-white/5 p-3 rounded-2xl border border-white/5 text-center">
                        <p class="text-[8px] text-zinc-500 uppercase font-bold mb-1 italic text-purple-400">Profit</p>
                        <p class="text-sm font-black text-green-500">+<%= dailyTip.profitPercent %>%</p>
                    </div>
                </div>

                <div class="bg-purple-600/10 border border-purple-500/20 p-4 rounded-2xl mb-6 flex justify-between items-center">
                    <p class="text-[10px] font-bold uppercase text-zinc-300">Ajánlott iroda:</p>
                    <p class="text-xs font-black text-white uppercase tracking-widest"><%= dailyTip.bookmaker %></p>
                </div>

                <div class="bg-black/40 p-5 rounded-2xl border border-white/5">
                    <p class="text-[10px] text-purple-400 font-black mb-3 tracking-[2px] uppercase">Neural 10Y Deep Analysis</p>
                    <p class="text-xs text-zinc-300 leading-relaxed italic"><%= dailyTip.reasoning %></p>
                </div>
            <% } else { %>
                <div class="py-12 text-center">
                    <div class="animate-spin h-8 w-8 border-4 border-purple-500 border-t-transparent rounded-full mx-auto mb-4"></div>
                    <p class="text-zinc-600 text-[10px] font-black uppercase tracking-widest animate-pulse">Neural elemzés folyamatban...</p>
                </div>
            <% } %>
        </div>

        <div class="card-glass rounded-3xl p-6">
            <h3 class="text-[10px] text-zinc-500 uppercase font-black mb-6 tracking-widest text-center italic">Neural Napló (Last 10)</h3>
            <div class="space-y-4">
                <% history.forEach(h => { %>
                    <div class="flex justify-between items-center text-[10px] border-b border-white/5 pb-3 last:border-0">
                        <span class="text-zinc-500 w-20 font-mono"><%= h.date %></span>
                        <span class="font-bold flex-1 px-4 truncate"><%= h.match %></span>
                        <span class="text-green-500 font-black">+<%= h.profitPercent %>%</span>
                    </div>
                <% }) %>
            </div>
        </div>

        <footer class="mt-8 text-center">
            <p class="text-[8px] text-zinc-700 uppercase font-bold tracking-[3px]">Secure Neural Connection Active</p>
        </footer >
    </div>
</body>
</html>