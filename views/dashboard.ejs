<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Dashboard | Rafinált Róka</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@800&family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        body { background: #000000; color: #fff; font-family: 'Inter', sans-serif; padding-bottom: 40px; }
        
        /* Kártya Stílus (Ugyanaz a profi tech look) */
        .startup-card { 
            background: #111; 
            border: 1px solid #333; 
            border-radius: 20px; 
            box-shadow: 0 10px 30px -10px rgba(0,0,0,0.5); 
            position: relative;
            overflow: hidden;
        }
        
        .gradient-text { 
            background: linear-gradient(to right, #a855f7, #ec4899); 
            -webkit-background-clip: text; 
            -webkit-text-fill-color: transparent; 
        }
        
        .fox-glow { box-shadow: 0 0 25px rgba(168, 85, 247, 0.2); }
        
        /* TOAST ÉRTESÍTÉS */
        .toast { 
            position: fixed; bottom: -100px; left: 50%; transform: translateX(-50%); 
            background: #7c3aed; color: white; 
            padding: 12px 24px; border-radius: 50px; 
            font-size: 12px; font-weight: bold; 
            box-shadow: 0 10px 20px rgba(124, 58, 237, 0.4); 
            z-index: 100; transition: bottom 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275); 
            display: flex; align-items: center; gap: 10px; width: 90%; max-width: 350px; justify-content: center; 
        }
        .toast.show { bottom: 20px; }
        
        /* Admin Gomb Stílus (Ha te nézed) */
        .admin-btn {
            background: rgba(234, 179, 8, 0.1);
            color: #fbbf24;
            border: 1px solid rgba(234, 179, 8, 0.3);
        }
    </style>
</head>
<body class="p-5 max-w-lg mx-auto min-h-screen flex flex-col">

    <header class="flex justify-between items-center mb-8 pt-2">
        <div class="flex items-center gap-3">
            <div class="w-10 h-10 bg-gradient-to-br from-purple-600 to-pink-600 rounded-xl flex items-center justify-center text-xl shadow-lg fox-glow">🦊</div>
            <div>
                <h1 class="text-sm font-bold text-white tracking-wide" style="font-family: 'Orbitron'">RAFINÁLT RÓKA</h1>
                <p class="text-[10px] text-zinc-400 font-medium">Prémium Csoport</p>
            </div>
        </div>
        
        <div class="flex gap-2">
            <% if (user.isAdmin || user.email === 'stylefaqu@gmail.com') { %>
                <a href="/admin" class="admin-btn px-3 py-2 rounded-lg text-[10px] font-bold transition flex items-center gap-1">
                    ⚡ HQ
                </a>
            <% } %>
            
            <a href="/stats" class="bg-zinc-900 hover:bg-zinc-800 text-zinc-400 border border-zinc-800 px-3 py-2 rounded-lg text-[10px] font-bold transition flex items-center gap-1">
                📊 STATS
            </a>
            
            <a href="/logout" class="bg-zinc-900 hover:bg-zinc-800 text-zinc-400 px-3 py-2 rounded-full text-[10px] font-bold transition flex items-center">EXIT</a>
        </div>
    </header>

    <div class="mb-8">
        <div class="flex justify-between items-end mb-2">
            <div>
                <p class="text-zinc-500 text-xs font-bold uppercase mb-1"><%= displayDate %></p>
                <h2 class="text-2xl font-bold text-white">Szia, <span class="text-purple-400"><%= user.fullname.split(' ')[0] %></span>! 👋</h2>
            </div>
        </div>
        
        <div class="bg-zinc-900/50 p-3 rounded-xl border border-white/5 inline-block w-full">
            <p class="text-xs text-zinc-300 italic text-center">"<%= randomQuote %>"</p>
        </div>
    </div>

    <% if (dailyTip && dailyTip.isPublished) { %>
        <div class="startup-card p-6 mb-8 border-t-2 border-t-purple-500 hover:scale-[1.01] transition duration-300">
            <div class="absolute top-0 right-0 bg-purple-600/20 text-purple-300 text-[9px] font-bold px-3 py-1 rounded-bl-xl border-l border-b border-purple-500/30">
                MAI FIX
            </div>
            
            <div class="text-center mb-6 mt-4">
                <p class="text-[10px] text-zinc-500 font-bold uppercase tracking-widest mb-2"><%= dailyTip.league %></p>
                <h3 class="text-xl font-black text-white leading-tight mb-4"><%= dailyTip.match %></h3>
                
                <div class="bg-zinc-900/80 inline-block px-8 py-4 rounded-2xl border border-zinc-800 w-full relative overflow-hidden">
                    <div class="absolute top-0 left-0 w-1 h-full bg-gradient-to-b from-purple-500 to-pink-500"></div>
                    <p class="text-[10px] text-zinc-500 uppercase font-bold mb-1">A Róka Tippje</p>
                    <p class="text-2xl font-black gradient-text"><%= dailyTip.prediction %></p>
                    <div class="mt-2 inline-flex items-center gap-2 bg-black/50 px-3 py-1 rounded-lg border border-white/10">
                        <span class="text-[10px] text-zinc-400 uppercase">Odds</span>
                        <span class="text-sm font-bold text-white"><%= dailyTip.odds %></span>
                    </div>
                </div>
            </div>

            <div class="bg-purple-900/10 border border-purple-500/20 p-4 rounded-xl relative mt-6">
                <span class="absolute -top-3 left-4 bg-[#111] px-2 text-xl">🦊</span>
                <p class="text-xs text-zinc-300 italic leading-relaxed mt-2 text-justify">
                    "<%= dailyTip.memberMessage || 'Készül a stratégia... Figyeld az értesítéseket!' %>"
                </p>
            </div>

            <div class="mt-6 flex justify-between items-center border-t border-white/5 pt-4">
                <div class="flex flex-col">
                    <span class="text-[9px] text-zinc-500 font-bold uppercase">Stratégia</span>
                    <span class="text-[10px] text-zinc-300">Fix Tét (10%)</span>
                </div>
                <span class="text-sm font-bold text-white bg-zinc-800 px-4 py-2 rounded-lg border border-zinc-700">
                    <%= recommendedStake %> Ft
                </span>
            </div>
        </div>
    <% } else { %>
        <div class="startup-card p-10 text-center mb-8 border-dashed border border-zinc-800 flex flex-col items-center justify-center min-h-[300px]">
            <div class="w-20 h-20 bg-zinc-900 rounded-full flex items-center justify-center mb-6 text-4xl animate-pulse relative">
                🧠
                <div class="absolute inset-0 rounded-full border-2 border-purple-500/30 animate-ping"></div>
            </div>
            <h3 class="text-lg font-bold text-white mb-2">A Róka még számol...</h3>
            <p class="text-xs text-zinc-500 leading-relaxed max-w-[250px]">
                Az AI éppen a mai kínálatot elemzi a legjobb Value Bet után kutatva.
                <br><br>
                <span class="text-purple-400 font-bold">Nézz vissza később (kb. 09:00)!</span>
            </p>
        </div>
    <% } %>

    <div class="mt-auto text-center pt-8 border-t border-zinc-900">
        <a href="/stats" class="text-xs font-bold text-zinc-600 hover:text-white transition uppercase tracking-widest mb-2 block">
            Eredmények Megtekintése ->
        </a>
        <p class="text-[9px] text-zinc-700">© 2024 Rafinált Róka AI</p>
    </div>

    <div id="toast" class="toast">
        <span>🚀</span>
        <span>Üdv a klubban! Ma kaszálunk?</span>
    </div>

    <script>
        // Toast megjelenítése belépéskor
        window.addEventListener('load', () => {
            const toast = document.getElementById('toast');
            setTimeout(() => { toast.classList.add('show'); }, 500);
            setTimeout(() => { toast.classList.remove('show'); }, 4000);
        });
    </script>
</body>
</html>