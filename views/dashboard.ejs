<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Skyhigh AI | Neural Assistant</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@700&family=Inter:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        body { background: #000; color: #fff; font-family: 'Inter', sans-serif; }
        .orbitron { font-family: 'Orbitron', sans-serif; }
        .ticket { background: #111; border-left: 4px solid #8b5cf6; border-radius: 0 20px 20px 0; }
        .neon-glow { text-shadow: 0 0 15px rgba(139, 92, 246, 0.6); }
    </style>
</head>
<body class="p-4 md:p-8">
    <div class="max-w-md mx-auto">
        <header class="flex justify-between items-center mb-10 pt-4">
            <h1 class="orbitron text-xl font-black text-purple-500 italic neon-glow">SKYHIGH AI</h1>
            <div class="flex gap-2">
                <% if (user.isAdmin) { %><a href="/admin" class="bg-purple-600/20 text-purple-400 border border-purple-500/30 px-3 py-1 rounded-full text-[9px] font-black uppercase">Admin Panel</a><% } %>
                <a href="/logout" class="text-zinc-600 text-[10px] font-bold uppercase py-1">Kilépés</a>
            </div>
        </header>

        <div class="bg-zinc-900/40 p-5 rounded-3xl mb-8 flex justify-between items-center border border-white/5">
            <div>
                <p class="text-[9px] text-zinc-500 uppercase font-black">Bankroll Státusz</p>
                <p class="text-xl font-black"><%= user.startingCapital.toLocaleString() %> Ft</p>
            </div>
            <div class="bg-green-500/10 text-green-500 px-3 py-1 rounded-full text-[10px] font-black uppercase tracking-widest">Active</div>
        </div>

        <div class="ticket p-6 mb-8 shadow-2xl shadow-purple-900/20 relative">
            <div class="flex justify-between items-center mb-6 border-b border-white/5 pb-4">
                <span class="text-[10px] font-black uppercase text-purple-500 tracking-widest">Neural Master Ticket</span>
                <span class="text-[10px] text-zinc-500 font-mono"><%= new Date().toLocaleDateString('hu-HU') %></span>
            </div>

            <% if (dailyTip) { %>
                <div class="mb-6">
                    <p class="text-[10px] text-zinc-500 uppercase font-bold mb-1">Mérkőzés és kezdés</p>
                    <h2 class="text-xl font-black uppercase tracking-tight"><%= dailyTip.match %></h2>
                    <p class="text-purple-400 font-black text-sm italic">Kezdés: <%= dailyTip.matchTime %></p>
                </div>

                <div class="bg-black/50 p-4 rounded-2xl mb-6">
                    <p class="text-[10px] text-zinc-500 uppercase font-bold mb-1">Kimenetel</p>
                    <p class="text-lg font-black text-white italic uppercase tracking-wider"><%= dailyTip.prediction %></p>
                </div>

                <div class="grid grid-cols-3 gap-4 mb-6">
                    <div class="text-center">
                        <p class="text-[8px] text-zinc-500 uppercase font-bold">Odds</p>
                        <p class="text-base font-black orbitron text-purple-500"><%= dailyTip.odds %></p>
                    </div>
                    <div class="text-center">
                        <p class="text-[8px] text-zinc-500 uppercase font-bold">Profit</p>
                        <p class="text-base font-black orbitron text-green-500">+<%= dailyTip.profitPercent %>%</p>
                    </div>
                    <div class="text-center">
                        <p class="text-[8px] text-zinc-500 uppercase font-bold">Iroda</p>
                        <p class="text-[10px] font-black uppercase text-white truncate"><%= dailyTip.bookmaker %></p>
                    </div>
                </div>

                <div class="bg-white/5 p-4 rounded-xl border border-white/5">
                    <p class="text-[9px] text-purple-400 font-black mb-2 uppercase tracking-widest">AI Matematikai Indoklás</p>
                    <p class="text-[11px] text-zinc-400 leading-relaxed italic"><%= dailyTip.reasoning %></p>
                </div>
            <% } else { %>
                <div class="py-10 text-center text-zinc-600 text-[10px] font-black uppercase tracking-widest animate-pulse italic">A robot jelenleg a 10 éves adatbázist elemzi...</div>
            <% } %>
        </div>

        <div class="bg-zinc-900/20 p-6 rounded-3xl border border-white/5">
            <h3 class="text-[9px] text-zinc-500 uppercase font-black mb-6 tracking-widest text-center italic">Neural Napló (Utolsó 10 tipp)</h3>
            <div class="space-y-4">
                <% history.forEach(h => { %>
                    <div class="flex justify-between items-center text-[10px] border-b border-white/5 pb-3">
                        <span class="text-zinc-600 font-mono w-24"><%= h.date %></span>
                        <span class="font-bold uppercase truncate flex-1 px-2"><%= h.match %></span>
                        <span class="text-green-500 font-black">+<%= h.profitPercent %>%</span>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>