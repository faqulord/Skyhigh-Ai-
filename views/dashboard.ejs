<body class="p-4 md:p-10 max-w-7xl mx-auto bg-black text-white">

    <header class="flex flex-col md:flex-row justify-between items-center mb-10 gap-4">
        <div>
            <h1 class="text-2xl font-black italic uppercase tracking-tighter">Skyhigh<span class="text-purple-500">.AI</span></h1>
            <p class="text-[10px] text-zinc-500 font-bold uppercase tracking-widest mt-1">
                Üdvözlöm, <span class="text-white"><%= user.fullname %></span>! 👋
            </p>
        </div>
        </header>

    <div class="tech-card flex flex-col h-[550px] shadow-2xl">
        <div id="chatBox" class="flex-1 p-6 overflow-y-auto space-y-4 text-sm bg-[#080808]">
            <div class="flex justify-start">
                <div class="bg-zinc-900 p-4 rounded-2xl rounded-tl-none border border-zinc-800 text-zinc-300">
                    <strong class="text-purple-500 block text-[9px] uppercase mb-2 tracking-widest">Skyhigh Core AI 🧠</strong>
                    Üdvözlöm a fedélzeten, <%= user.fullname.split(' ')[0] %>! 🤝 <br><br>
                    A rendszerem készen áll a mai piaci adatok feldolgozására. 
                    <% if (!user.hasLicense) { %>
                        Jelenleg demo módban van. A Master Tippek megtekintéséhez aktiválja a licencét! 🔓
                    <% } else { %>
                        A licence aktív. Maradjon fegyelmezett, a profit a türelmeseké. 📈
                    <% } %>
                </div>
            </div>
        </div>
        <form id="chatForm" class="p-4 border-t border-zinc-800 flex gap-2">
            <input type="text" id="userInput" placeholder="Kérdezzen az AI-tól..." class="flex-1 bg-black border border-zinc-800 rounded-xl px-4 outline-none focus:border-purple-600">
            <button class="bg-purple-600 w-12 h-12 rounded-xl flex items-center justify-center"><i class="fas fa-paper-plane"></i></button>
        </form>
    </div>

    <script>
        // ... (visszaszámláló marad) ...

        async function buy(plan) {
            const res = await fetch('/api/activate-license', { 
                method: 'POST', 
                headers: {'Content-Type': 'application/json'}, 
                body: JSON.stringify({ plan }) 
            });
            const data = await res.json();
            if(data.success) {
                // SIKERES AKTIVÁLÁS ÜZENET
                const chatBox = document.getElementById('chatBox');
                chatBox.innerHTML += `
                    <div class="flex justify-start my-4">
                        <div class="bg-green-600/20 p-4 rounded-2xl border border-green-500/50 text-green-400">
                            <strong>RENSZERÜZENET:</strong> ${data.message}
                        </div>
                    </div>`;
                setTimeout(() => location.reload(), 2000);
            }
        }
        
        // ... (chat küldés script marad) ...
    </script>
</body>