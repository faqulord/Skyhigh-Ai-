<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Skyhigh AI | Neural Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Syncopate:wght@400;700&family=Inter:wght@300;400;700;900&display=swap" rel="stylesheet">
    <style>
        :root { --neon-purple: #8b5cf6; --neon-blue: #06b6d4; }
        body { background-color: #020202; color: #e2e8f0; font-family: 'Inter', sans-serif; overflow-x: hidden; }
        .font-sync { font-family: 'Syncopate', sans-serif; }
        .glass-panel { background: rgba(15, 15, 20, 0.7); border: 1px solid rgba(139, 92, 246, 0.2); backdrop-filter: blur(12px); border-radius: 28px; }
        .neon-border { border: 1px solid var(--neon-purple); box-shadow: 0 0 15px rgba(139, 92, 246, 0.3); }
        .glow-text { text-shadow: 0 0 10px rgba(139, 92, 246, 0.5); }
        .gradient-btn { background: linear-gradient(90deg, #7c3aed, #2563eb); transition: 0.4s; }
        .gradient-btn:hover { box-shadow: 0 0 25px rgba(124, 58, 237, 0.6); transform: translateY(-2px); }
    </style>
</head>
<body class="min-h-screen p-4 md:p-10">

    <div class="max-w-6xl mx-auto">
        <nav class="flex justify-between items-center mb-12">
            <div class="flex flex-col">
                <span class="font-sync text-2xl font-bold tracking-tighter glow-text italic text-white">SKYHIGH <span class="text-purple-500">AI</span></span>
                <span class="text-[9px] uppercase tracking-[4px] text-zinc-500 font-bold">V1.0 Neural Interface</span>
            </div>
            
            <div class="flex items-center gap-6">
                <% if (user.isAdmin) { %>
                    <a href="/admin" class="hidden md:flex items-center gap-2 border border-red-500/50 text-red-500 px-4 py-2 rounded-full text-[10px] font-black uppercase tracking-widest hover:bg-red-500 hover:text-white transition-all">
                        <i class="fas fa-user-shield"></i> Admin Panel
                    </a>
                <% } %>
                
                <div class="text-right">
                    <p class="text-[9px] text-zinc-600 uppercase font-black">Rendszer Operátor</p>
                    <p class="text-sm font-bold text-white"><%= user.fullname %></p>
                </div>
            </div>
        </nav>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            
            <div class="lg:col-span-1 space-y-6">
                <div class="glass-panel p-8 relative overflow-hidden">
                    <div class="absolute -right-4 -top-4 w-24 h-24 bg-purple-600/10 rounded-full blur-3xl"></div>
                    <p class="text-[10px] text-zinc-500 uppercase font-black tracking-widest mb-2">Összesített Egyenleg</p>
                    <h2 class="text-4xl font-black text-white tabular-nums mb-4"><%= user.startingCapital.toLocaleString() %> <span class="text-sm text-zinc-500">FT</span></h2>
                    <div class="flex items-center gap-2 text-green-400 text-[11px] font-bold">
                        <i class="fas fa-chart-line"></i>
                        <span>STRATÉGIA: 5% RISK MANAGEMENT</span>
                    </div>
                </div>

                <div class="glass-panel p-6 border-l-4 border-l-purple-600">
                    <p class="text-[10px] text-zinc-500 uppercase font-black mb-1">Mai Javasolt Tét</p>
                    <p class="text-2xl font-black text-purple-400 italic"><%= (user.startingCapital * 0.05).toLocaleString() %> FT</p>
                </div>
            </div>

            <div class="lg:col-span-2">
                <div class="glass-panel p-8 h-full border border-purple-500/30 relative">
                    <div class="flex justify-between items-start mb-8">
                        <div>
                            <h3 class="text-xs font-black text-purple-500 uppercase tracking-[4px] mb-1">Live Master Tip</h3>
                            <p class="text-[10px] text-zinc-600 uppercase">Frissítve: <%= new Date().toLocaleDateString('hu-HU') %></p>
                        </div>
                        <div class="flex gap-2">
                            <span class="w-2 h-2 rounded-full bg-green-500 animate-pulse"></span>
                            <span class="text-[9px] text-green-500 font-bold uppercase tracking-widest">Live Feed</span>
                        </div>
                    </div>

                    <% if (user.hasLicense && dailyTip) { %>
                        <div class="mb-8">
                            <div class="flex flex-col md:flex-row md:items-center justify-between gap-4 mb-6">
                                <h4 class="text-3xl font-black italic uppercase text-white tracking-tighter"><%= dailyTip.match %></h4>
                                <div class="bg-purple-600 text-white px-6 py-2 rounded-xl font-black text-xl shadow-[0_0_20px_rgba(139,92,246,0.4)]">
                                    <%= dailyTip.odds %>
                                </div>
                            </div>
                            
                            <div class="bg-black/40 border border-white/5 p-6 rounded-2xl italic">
                                <p class="text-[10px] text-purple-400 uppercase font-black mb-3 tracking-widest">Neural Reasoning (AI Elemzés)</p>
                                <p class="text-zinc-300 leading-relaxed text-sm">"<%= dailyTip.reasoning %>"</p>
                            </div>
                        </div>
                        
                        <div class="grid grid-cols-2 gap-4">
                            <div class="bg-zinc-900/50 p-4 rounded-xl text-center">
                                <p class="text-[9px] text-zinc-600 uppercase font-bold">Kimenetel</p>
                                <p class="font-bold text-white"><%= dailyTip.prediction %></p>
                            </div>
                            <div class="bg-zinc-900/50 p-4 rounded-xl text-center">
                                <p class="text-[9px] text-zinc-600 uppercase font-bold">Biztonsági Index</p>
                                <p class="font-bold text-green-500">89.4%</p>
                            </div>
                        </div>
                    <% } else { %>
                        <div class="text-center py-12">
                            <i class="fas fa-lock text-4xl text-zinc-800 mb-6"></i>
                            <p class="text-zinc-500 uppercase text-xs font-bold tracking-widest mb-6">A hozzáféréshez aktív előfizetés szükséges</p>
                            <button class="gradient-btn text-white px-10 py-4 rounded-full font-black uppercase text-[10px] tracking-widest">Licenc Aktiválása</button>
                        </div>
                    <% } %>
                </div>
            </div>
        </div>

        <footer class="mt-12 flex flex-col md:flex-row justify-between items-center gap-6 border-t border-white/5 pt-8">
            <p class="text-[9px] text-zinc-700 uppercase tracking-widest font-bold">© 2026 Skyhigh AI Neural Systems. All Rights Reserved.</p>
            <div class="flex gap-8">
                <% if (user.isAdmin) { %>
                    <a href="/admin" class="md:hidden text-[10px] text-red-500 uppercase font-bold tracking-widest">Admin</a>
                <% } %>
                <a href="/logout" class="text-[10px] text-zinc-500 uppercase font-bold tracking-widest hover:text-white transition">Kijelentkezés</a>
            </div>
        </footer>
    </div>

</body>
</html>