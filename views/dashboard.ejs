<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Skyhigh AI | Vezérlőpult</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body { background-color: #000; color: white; font-family: sans-serif; padding: 20px; }
        .card { background: #111; padding: 20px; border-radius: 15px; border: 1px solid #333; margin-bottom: 20px; }
        .admin-btn { display: block; width: 100%; background: gold; color: black; font-weight: bold; text-align: center; padding: 15px; border-radius: 10px; text-transform: uppercase; margin-bottom: 20px; }
    </style>
</head>
<body>

    <div class="flex justify-between items-center mb-8">
        <h2 class="text-xl font-bold uppercase text-green-500">Vezérlőpult</h2>
        <a href="/logout" class="text-xs text-red-500 border border-red-500 px-3 py-1 rounded">KILÉPÉS</a>
    </div>

    <% if (isAdmin) { %>
        <a href="/admin" class="admin-btn">👑 BELÉPÉS AZ ADMIN FELÜLETRE</a>
    <% } %>

    <div class="card">
        <h3 class="text-lg font-bold mb-2">Szia, <%= user.fullname %>!</h3>
        <p class="text-sm text-gray-400">Üdvözöllek a Skyhigh AI belső felületén.</p>
        <hr class="border-gray-800 my-4">
        
        <p class="text-xs text-gray-500 uppercase font-bold">Státuszod:</p>
        <p class="text-xl font-bold <%= user.hasLicense ? 'text-green-500' : 'text-red-500' %>">
            <%= user.hasLicense ? 'AKTÍV LICENC' : 'NINCS LICENC' %>
        </p>
    </div>

</body>
</html>