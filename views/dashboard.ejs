<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Skyhigh AI | Vezérlőpult</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-[#050505] text-white p-4 font-sans">
    <div class="max-w-md mx-auto">
        <header class="flex justify-between items-center mb-8 pt-4">
            <h1 class="text-xl font-black italic text-purple-500 uppercase tracking-tighter">Skyhigh AI</h1>
            <a href="/logout" class="text-zinc-600 font-bold text-xs uppercase">Kilépés</a>
        </header>

        <div class="bg-zinc-900/80 p-5 rounded-3xl border border-white/5 mb-6">
            <p class="text-[10px] text-zinc-500 uppercase font-bold mb-1">Üdvözlünk, <%= user.fullname %></p>
            <div class="flex justify-between items-center">
                <h2 class="text-lg font-bold">Aktuális Bankroll:</h2>
                <span class="text-purple-500 font-mono font-bold text-xl">€<%= user.startingCapital %></span>
            </div>
        </div>

        <div class="bg-gradient-to-br from-purple-900/20 to-black p-6 rounded-[2.5rem] border border-purple-500/40 mb-6">
            <h3 class="bg-purple-600 w-fit text-[9px] px-3 py-1 rounded-full font-black uppercase mb-4">Mai Nap Master Tippje</h3>
            
            <% if (dailyTip) { %>
                <div class="mb-6">
                    <h4 class="text-xl font-black uppercase"><%= dailyTip.match %></h4>
                    <p class="text-purple-400 font-bold italic"><%= dailyTip.prediction %></p>
                </div>

                <div class="grid grid-cols-2 gap-4 mb-6 text-center">
                    <div class="bg-white/5 p-3 rounded-2xl">
                        <p class="text-[9px] text-zinc-500 uppercase font-bold">Szorzó</p>
                        <p class="text-lg font-black"><%= dailyTip.odds %></p>
                    </div>
                    <div class="bg-white/5 p-3 rounded-2xl">
                        <p class="text-[9px] text-zinc-500 uppercase font-bold">Profit</p>
                        <p class="text-lg font-black text-green-500">+<%= dailyTip.profitPercent %>%</p>
                    </div>
                </div>

                <div class="bg-black/50 p-4 rounded-2xl border border-white/5">
                    <p class="text-[10px] text-purple-400 uppercase font-black mb-2 tracking-widest">Matematikai Elemzés (10 év statisztika)</p>
                    <p class="text-xs text-zinc-400 leading-relaxed italic"><%= dailyTip.reasoning %></p>
                </div>
            <% } else { %>
                <p class="text-center text-zinc-600 text-xs py-10 uppercase font-bold">A mai elemzés hamarosan érkezik...</p>
            <% } %>
        </div>

        <div class="bg-zinc-900/30 p-5 rounded-3xl border border-white/5">
            <h3 class="text-[10px] text-zinc-500 uppercase font-black mb-4">Múltbéli Eredmények</h3>
            <div class="space-y-2">
                <% history.forEach(h => { %>
                    <div class="flex justify-between text-[11px] border-b border-white/5 pb-2">
                        <span class="text-zinc-600"><%= h.date %></span>
                        <span class="font-bold truncate w-32"><%= h.match %></span>
                        <span class="text-green-500 font-black">+<%= h.profitPercent %>%</span>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>