<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Skyhigh AI | Neural Platform</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body { background: #050505; color: #fff; font-family: 'Inter', sans-serif; }
        .glass { background: rgba(20, 20, 20, 0.8); border: 1px solid rgba(139, 92, 246, 0.2); backdrop-filter: blur(12px); }
        .neon-text { text-shadow: 0 0 10px rgba(139, 92, 246, 0.5); }
    </style>
</head>
<body class="min-h-screen p-4">
    <div class="max-w-md mx-auto pb-10">
        <header class="flex justify-between items-center py-6">
            <div>
                <h1 class="text-2xl font-black italic text-purple-500 tracking-tighter neon-text">SKYHIGH AI</h1>
                <p class="text-[9px] text-zinc-500 uppercase tracking-[3px] font-bold">Neural Engine v4.0</p>
            </div>
            <div class="flex items-center gap-3">
                <% if (user.isAdmin) { %>
                    <a href="/admin" class="bg-purple-600/20 text-purple-400 border border-purple-500/30 px-3 py-1 rounded-full text-[10px] font-black uppercase tracking-widest">Admin</a>
                <% } %>
                <a href="/logout" class="text-zinc-500 hover:text-white transition uppercase text-[10px] font-bold">Kilépés</a>
            </div>
        </header>

        <div class="glass p-6 rounded-[2rem] mb-6 shadow-xl shadow-purple-900/10">
            <p class="text-[10px] text-zinc-500 uppercase font-black mb-1">Felhasználó: <%= user.fullname %></p>
            <div class="flex justify-between items-end">
                <p class="text-sm font-bold text-zinc-300 italic">Saját Bankroll:</p>
                <p class="text-2xl font-black text-white italic">€<%= user.startingCapital %></p>
            </div>
        </div>

        <div class="bg-gradient-to-br from-purple-900/30 to-black p-6 rounded-[2.5rem] border border-purple-500/40 mb-8 relative overflow-hidden">
            <div class="absolute -top-10 -right-10 w-32 h-32 bg-purple-600/10 blur-3xl"></div>
            <div class="flex justify-between items-center mb-6">
                <span class="bg-purple-600 text-[9px] px-3 py-1 rounded-full font-black uppercase tracking-widest">MAI MASTER TIPP</span>
                <span class="text-[10px] text-zinc-500 font-mono"><%= new Date().toLocaleDateString('hu-HU') %></span>
            </div>

            <% if (dailyTip) { %>
                <div class="mb-6">
                    <h3 class="text-xl font-black uppercase leading-tight"><%= dailyTip.match %></h3>
                    <p class="text-purple-400 font-bold italic text-lg tracking-tight"><%= dailyTip.prediction %></p>
                </div>

                <div class="grid grid-cols-2 gap-3 mb-6 text-center">
                    <div class="bg-white/5 p-4 rounded-3xl border border-white/5">
                        <p class="text-[9px] text-zinc-500 uppercase font-bold mb-1 tracking-widest">Odds</p>
                        <p class="text-xl font-black"><%= dailyTip.odds %></p>
                    </div>
                    <div class="bg-white/5 p-4 rounded-3xl border border-white/5">
                        <p class="text-[9px] text-zinc-500 uppercase font-bold mb-1 tracking-widest">Várható Profit</p>
                        <p class="text-xl font-black text-green-500">+<%= dailyTip.profitPercent %>%</p>
                    </div>
                </div>

                <div class="bg-black/40 p-5 rounded-3xl border border-white/5 shadow-inner">
                    <p class="text-[10px] text-purple-400 uppercase font-black mb-3 tracking-[2px]">Matematikai Analízis (10 év H2H)</p>
                    <p class="text-xs text-zinc-400 leading-relaxed italic"><%= dailyTip.reasoning %></p>
                </div>
            <% } else { %>
                <div class="py-12 text-center">
                    <div class="animate-pulse flex flex-col items-center">
                        <div class="w-8 h-8 border-4 border-purple-500 border-t-transparent rounded-full animate-spin mb-4"></div>
                        <p class="text-zinc-600 text-[10px] uppercase font-bold tracking-widest">A robot elemzi a 10 éves adatbázist...</p>
                    </div>
                </div>
            <% } %>
        </div>

        <div class="glass p-6 rounded-[2.5rem]">
            <h3 class="text-[10px] text-zinc-500 uppercase font-black mb-6 tracking-widest">Múltbéli Eredmények</h3>
            <div class="space-y-4">
                <% history.forEach(h => { %>
                    <div class="flex justify-between items-center border-b border-white/5 pb-3">
                        <div class="w-20">
                            <p class="text-[9px] text-zinc-600 font-mono"><%= h.date %></p>
                        </div>
                        <p class="text-[11px] font-bold truncate flex-1 px-4"><%= h.match %></p>
                        <p class="text-[11px] font-black text-green-500">+<%= h.profitPercent %>%</p>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>