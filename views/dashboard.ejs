<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Dashboard | Rafinált Róka</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@800&family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        body { background: #050505; color: #fff; font-family: 'Inter', sans-serif; padding-bottom: 40px; }
        .startup-card { background: #111; border: 1px solid #27272a; border-radius: 24px; box-shadow: 0 10px 40px -10px rgba(0,0,0,0.7); }
        .gradient-text { background: linear-gradient(to right, #a855f7, #ec4899); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
        .fox-glow { box-shadow: 0 0 25px rgba(168, 85, 247, 0.2); }
        /* A VICCES TOAST STÍLUSA */
        .toast { position: fixed; bottom: -100px; left: 50%; transform: translateX(-50%); background: #7c3aed; color: white; padding: 14px 28px; border-radius: 50px; font-size: 11px; font-weight: 900; z-index: 1000; transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275); display: flex; align-items: center; gap: 10px; width: 85%; max-width: 320px; box-shadow: 0 15px 30px rgba(124, 58, 237, 0.4); text-transform: uppercase; }
        .toast.show { bottom: 30px; }
    </style>
</head>
<body class="p-5 max-w-lg mx-auto">

    <header class="flex justify-between items-center mb-10 pt-4">
        <div class="flex items-center gap-3">
            <div class="w-10 h-10 bg-gradient-to-br from-purple-600 to-pink-600 rounded-xl flex items-center justify-center text-xl shadow-lg fox-glow">🦊</div>
            <div>
                <h1 class="text-sm font-bold tracking-tight">RAFINÁLT RÓKA</h1>
                <p class="text-[9px] text-zinc-500 font-black uppercase tracking-widest">Master AI</p>
            </div>
        </div>
        <div class="flex items-center gap-2">
            <a href="/stats" class="bg-zinc-900 text-zinc-400 px-3 py-2 rounded-xl text-[10px] font-bold border border-zinc-800 flex items-center gap-1 hover:bg-zinc-800 transition">
                <span>📊</span> STATS
            </a>
            <% if (user.isAdmin) { %><a href="/admin" class="text-purple-400 text-[10px] font-bold px-2">HQ</a><% } %>
            <a href="/logout" class="bg-zinc-900 p-2 rounded-xl text-[10px] text-zinc-500">❌</a>
        </div>
    </header>

    <div class="mb-8">
        <h2 class="text-2xl font-black mb-1">Szia, <span class="text-purple-500"><%= user.fullname.split(' ')[0] %></span>! 👋</h2>
        <div class="bg-zinc-900/30 p-3 rounded-xl border border-white/5 inline-block">
            <p class="text-[11px] text-zinc-400 italic">"<%= randomQuote %>"</p>
        </div>
    </div>

    <% if (dailyTip && dailyTip.isPublished) { %>
        <div class="startup-card p-6 border-t-4 border-purple-600 relative overflow-hidden">
            <div class="absolute top-0 right-0 bg-purple-600 text-white text-[8px] font-black px-3 py-1 rounded-bl-xl uppercase tracking-tighter">Fix Profit</div>
            <div class="text-center mb-8 pt-4">
                <p class="text-[9px] text-zinc-500 font-black uppercase tracking-[4px] mb-3"><%= dailyTip.league %></p>
                <h3 class="text-xl font-black leading-tight mb-6"><%= dailyTip.match %></h3>
                <div class="bg-black/40 p-6 rounded-3xl border border-zinc-800/50 backdrop-blur-sm">
                    <p class="text-[10px] text-zinc-500 uppercase font-black mb-1">A Róka Mai Tippje</p>
                    <p class="text-3xl font-black gradient-text mb-1"><%= dailyTip.prediction %></p>
                    <p class="text-sm font-bold text-white">Odds: <span class="text-purple-500"><%= dailyTip.odds %></span></p>
                </div>
            </div>

            <div class="bg-purple-900/10 border border-purple-500/20 p-5 rounded-2xl relative mb-6">
                <span class="absolute -top-3 left-4 bg-[#111] px-2 text-xl">🦊</span>
                <p class="text-xs text-zinc-300 italic leading-relaxed">
                    "<%= dailyTip.memberMessage %>"
                </p>
            </div>

            <div class="flex justify-between items-center bg-zinc-900/50 p-5 rounded-2xl border border-zinc-800">
                <div class="text-[10px] text-zinc-500 font-black uppercase leading-none">Javasolt Tét (10%)</div>
                <div class="text-xl font-black text-green-400 tracking-tighter"><%= recommendedStake %> Ft</div>
            </div>
        </div>
    <% } else { %>
        <div class="startup-card p-12 text-center border-dashed border-2 border-zinc-800">
            <div class="w-16 h-16 bg-zinc-900 rounded-full flex items-center justify-center mx-auto mb-4 animate-bounce">⏳</div>
            <h3 class="text-lg font-bold mb-1">A Róka vadászik...</h3>
            <p class="text-xs text-zinc-500">Már szagolja a profitot, hamarosan kirakjuk!</p>
        </div>
    <% } %>

    <div id="toast" class="toast"><span>🦊</span><span id="toast-text"></span></div>
    
    <script>
        const quotes = [
            "A matek nem hazudik, tesó! 📈",
            "Dől a lé, csak figyeld! 💰",
            "A Róka sose téved, vágod? 🦊",
            "Zsebbe a profitot, ne a duma menjen! ⚡",
            "Ma is kaszálunk, nyugi van! 🔥",
            "Csúcstechnológia. Fix Profit. 💎"
        ];

        window.addEventListener('load', () => {
            const toast = document.getElementById('toast');
            const toastText = document.getElementById('toast-text');
            
            function showToast() {
                toastText.innerText = quotes[Math.floor(Math.random() * quotes.length)];
                toast.classList.add('show');
                setTimeout(() => { toast.classList.remove('show'); }, 4000);
            }

            setTimeout(showToast, 2000); // Első beszólás 2 mp után
            setInterval(showToast, 15000); // Utána 15 másodpercenként
        });
    </script>
</body>
</html>