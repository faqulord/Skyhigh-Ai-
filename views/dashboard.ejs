<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Skyhigh AI | Terminál</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { background-color: #050505; color: white; font-family: sans-serif; overflow-x: hidden; }
        .tech-card { background: #0c0c0e; border: 1px solid #1f1f22; border-radius: 20px; }
        .onboarding-overlay { background: rgba(0,0,0,0.95); backdrop-blur: 15px; }
    </style>
</head>
<body class="p-4 md:p-10 max-w-7xl mx-auto">

    <header class="flex justify-between items-center mb-10">
        <h1 class="text-2xl font-black italic tracking-tighter">Skyhigh<span class="text-purple-500">.AI</span></h1>
        <div class="flex items-center gap-3">
            <% if (isAdmin) { %>
                <a href="/admin" class="bg-red-600 text-white px-4 py-2 rounded-xl text-[10px] font-black uppercase">Admin</a>
            <% } %>
            <a href="/logout" class="text-zinc-500 hover:text-white text-xs font-bold uppercase">Kilépés</a>
        </div>
    </header>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-10">
        <div class="tech-card p-6 flex flex-col items-center">
            <p class="text-[9px] text-zinc-500 uppercase font-bold tracking-widest mb-1">Licenc</p>
            <p class="text-lg font-black <%= user.hasLicense ? 'text-green-500' : 'text-red-500 animate-pulse' %>">
                <%= user.hasLicense ? 'PRÉMIUM' : 'INAKTÍV' %>
            </p>
        </div>
        <div class="tech-card p-6 flex flex-col items-center">
            <p class="text-[9px] text-zinc-500 uppercase font-bold tracking-widest mb-1">Következő Tipp</p>
            <div id="countdown" class="text-2xl font-mono font-black">00:00:00</div>
        </div>
        <div class="tech-card p-6 flex flex-col items-center">
            <p class="text-[9px] text-zinc-500 uppercase font-bold tracking-widest mb-1">Bankroll</p>
            <p class="text-xl font-mono font-bold text-purple-500"><%= (user.startingCapital || 0).toLocaleString() %> Ft</p>
        </div>
    </div>

    <div class="mb-10">
        <div class="tech-card p-6 md:p-12 border-purple-600/30 bg-purple-600/5 relative overflow-hidden">
            <% if (!user.hasLicense) { %>
                <div class="absolute inset-0 bg-black/80 backdrop-blur-md z-10 flex flex-col items-center justify-center p-6 text-center">
                    <i class="fas fa-lock text-4xl mb-4 text-zinc-700"></i>
                    <h3 class="font-black text-xl mb-2 italic">A MAI MASTER ELEMZÉS ZÁROLT</h3>
                    <p class="text-xs text-zinc-500 mb-6 italic">A matematikai stratégia eléréséhez aktiválja licencét.</p>
                    <button class="bg-white text-black px-10 py-4 rounded-2xl font-black uppercase text-xs tracking-widest hover:bg-purple-600 hover:text-white transition">Licenc Aktiválása</button>
                </div>
            <% } %>

            <div id="tipContent" class="text-center">
                <h2 class="text-[10px] font-black text-purple-500 uppercase tracking-[10px] mb-8">Napi Stratégiai Elemzés</h2>
                <div id="waiting" class="py-12 text-zinc-600 italic text-sm">Az algoritmus a 08:00-as piaci nyitásra vár...</div>
                <div id="activeTip" class="hidden">
                    <h3 class="text-3xl md:text-5xl font-black mb-4 tracking-tighter uppercase italic"><%= dailyTip ? dailyTip.match : 'Elemzés alatt...' %></h3>
                    <div class="inline-block bg-purple-600 px-8 py-2 rounded-full mb-6 font-black text-sm tracking-widest">
                        TIP: <%= dailyTip ? dailyTip.prediction : '' %> @ <%= dailyTip ? dailyTip.odds : '' %>
                    </div>
                    <p class="text-zinc-400 text-sm leading-relaxed italic max-w-3xl mx-auto border-t border-zinc-900 pt-6">
                        "<%= dailyTip ? dailyTip.reasoning : '' %>"
                    </p>
                    <div class="mt-10 p-4 bg-black border border-zinc-800 rounded-2xl inline-block">
                        <p class="text-[9px] text-zinc-500 uppercase mb-1">Javasolt Tét (5%)</p>
                        <p class="text-xl font-mono font-bold text-green-500"><%= (user.startingCapital * 0.05).toLocaleString() %> Ft</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="tech-card flex flex-col h-[500px]">
        <div id="chatBox" class="flex-1 p-6 overflow-y-auto space-y-4 text-sm bg-[#080808]">
            <div class="bg-zinc-900 p-4 rounded-2xl rounded-tl-none border border-zinc-800 text-zinc-300 italic">
                <strong class="text-purple-500 block text-[10px] uppercase mb-1">Skyhigh Core</strong>
                Üdvözlöm az Operatív Törzsben. A 6 hónapos profit-ciklus megkezdődött.
            </div>
        </div>
        <form id="chatForm" class="p-4 border-t border-zinc-800 flex gap-2 bg-zinc-900/50">
            <input type="text" id="userInput" placeholder="Üzenet a szoftvernek..." class="flex-1 bg-black border border-zinc-800 rounded-xl px-4 text-sm outline-none focus:border-purple-600">
            <button class="bg-purple-600 w-12 h-12 rounded-xl flex items-center justify-center"><i class="fas fa-paper-plane"></i></button>
        </form>
    </div>

    <% if (!user.startingCapital || user.startingCapital == 0) { %>
    <div id="onboarding" class="onboarding-overlay fixed inset-0 flex items-center justify-center z-50 p-6 text-center">
        <div class="bg-zinc-950 p-10 border border-zinc-800 rounded-[40px] max-w-md shadow-2xl">
            <h2 class="text-3xl font-black mb-4 italic text-white uppercase">Szoftver <span class="text-purple-600">Aktiválása</span></h2>
            <p class="text-zinc-400 text-xs mb-8 leading-relaxed italic">Adja meg a kezdő tőkét, hogy a robot kiszámolja az Ön 6 hónapos profit-ciklusát.</p>
            <div class="grid grid-cols-2 gap-3 mb-6">
                <button onclick="setCap(50000)" class="bg-zinc-900 py-4 rounded-2xl font-bold hover:bg-purple-600 transition">50.000 Ft</button>
                <button onclick="setCap(100000)" class="bg-zinc-900 py-4 rounded-2xl font-bold hover:bg-purple-600 transition">100.000 Ft</button>
                <input type="number" id="customCap" placeholder="Egyéni" class="col-span-2 bg-black border border-zinc-800 p-4 rounded-2xl text-center outline-none focus:border-purple-600">
            </div>
            <button onclick="saveCap()" class="w-full bg-white text-black py-4 rounded-2xl font-black uppercase tracking-widest">Inicializálás</button>
        </div>
    </div>
    <% } %>

    <script>
        function updateUI() {
            const now = new Date();
            const h = now.getHours();
            if(h >= 8) {
                document.getElementById('activeTip').classList.remove('hidden');
                document.getElementById('waiting').classList.add('hidden');
            }
            const target = new Date();
            target.setHours(8,0,0,0);
            if(now > target) target.setDate(target.getDate()+1);
            const diff = target - now;
            const hours = Math.floor(diff/3600000).toString().padStart(2,'0');
            const mins = Math.floor((diff%3600000)/60000).toString().padStart(2,'0');
            const secs = Math.floor((diff%60000)/1000).toString().padStart(2,'0');
            document.getElementById('countdown').innerText = `${hours}:${mins}:${secs}`;
        }
        setInterval(updateUI, 1000); updateUI();

        function setCap(v) { document.getElementById('customCap').value = v; }
        async function saveCap() {
            const capital = document.getElementById('customCap').value;
            await fetch('/api/set-capital', { method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({capital}) });
            location.reload();
        }

        const chatBox = document.getElementById('chatBox');
        document.getElementById('chatForm').onsubmit = async (e) => {
            e.preventDefault();
            const msg = document.getElementById('userInput').value;
            if(!msg) return;
            document.getElementById('userInput').value = '';
            chatBox.innerHTML += `<div class="flex justify-end"><div class="bg-purple-600 p-3 rounded-2xl rounded-tr-none text-xs font-bold shadow-lg">${msg}</div></div>`;
            chatBox.scrollTop = chatBox.scrollHeight;
            const res = await fetch('/api/chat', { method:'POST', headers:{'Content-Type':'application/json'}, body:JSON.stringify({message:msg}) });
            const data = await res.json();
            chatBox.innerHTML += `<div class="flex justify-start"><div class="bg-zinc-900 p-4 rounded-2xl rounded-tl-none border border-zinc-800 text-xs italic"><strong class="text-purple-500 block text-[9px] uppercase mb-1">Skyhigh Core</strong>${data.reply}</div></div>`;
            chatBox.scrollTop = chatBox.scrollHeight;
        };
    </script>
</body>
</html>