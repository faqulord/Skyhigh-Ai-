<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Skyhigh | Az Öreg Róka</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        body { background: #000; color: #fff; font-family: 'Inter', sans-serif; }
        .fox-card { background: #0a0a0a; border-radius: 2rem; border-top: 4px solid #f97316; border-bottom: 1px solid #1f1f1f; }
    </style>
</head>
<body class="p-4 md:p-8 flex justify-center min-h-screen">
    <div class="max-w-md w-full pb-12">
        <header class="mb-10 pt-4 flex justify-between items-center">
            <div>
                <p class="text-[10px] text-orange-500 font-black uppercase tracking-[3px] mb-1">Neural AI Analyst</p>
                <h1 class="text-xl font-bold italic tracking-tighter"><%= displayDate %></h1>
            </div>
            <div class="flex items-center gap-2">
                <% if (user.isAdmin) { %><a href="/admin" class="bg-orange-600 text-white text-[9px] font-black px-3 py-2 rounded-full uppercase shadow-lg shadow-orange-600/30">Admin</a><% } %>
                <a href="/logout" class="text-zinc-600 text-[9px] font-bold border border-white/10 px-3 py-2 rounded-full uppercase">Kilépés</a>
            </div>
        </header>

        <div class="fox-card p-7 shadow-2xl relative mb-10">
            <div class="flex items-center gap-3 mb-8">
                <div class="text-3xl">👴🦊</div>
                <div>
                    <h2 class="text-[10px] font-black uppercase text-zinc-500 tracking-[2px]">Az Öreg Róka meglátása</h2>
                </div>
            </div>

            <% if (dailyTip) { %>
                <p class="text-zinc-400 text-[13px] italic mb-8 leading-relaxed">"<%= dailyTip.reasoning %>"</p>

                <div class="bg-orange-600/10 border-l-4 border-orange-500 p-6 mb-8">
                    <p class="text-[10px] text-orange-500 font-black uppercase mb-2 tracking-widest"><%= dailyTip.league %></p>
                    <h3 class="text-2xl font-black uppercase mb-2 tracking-tight text-white leading-tight"><%= dailyTip.match %></h3>
                    <p class="text-xl font-black italic text-orange-400 uppercase tracking-tighter underline underline-offset-8 decoration-orange-500/50"><%= dailyTip.prediction %></p>
                </div>

                <div class="grid grid-cols-2 gap-4 mb-8">
                    <div class="bg-white/5 p-5 rounded-2xl text-center border border-white/5 shadow-inner">
                        <p class="text-[9px] text-zinc-500 font-bold uppercase mb-1 tracking-widest">Kezdés:</p>
                        <p class="text-lg font-black italic text-white underline underline-offset-4 decoration-orange-500"><%= dailyTip.matchTime %></p>
                    </div>
                    <div class="bg-white/5 p-5 rounded-2xl text-center border border-white/5 shadow-inner">
                        <p class="text-[9px] text-zinc-500 font-bold uppercase mb-1 tracking-widest">Szorzó:</p>
                        <p class="text-lg font-black text-white italic">@<%= dailyTip.odds %></p>
                    </div>
                </div>

                <div class="bg-zinc-900/80 p-6 rounded-[2rem] border border-white/5 text-center shadow-xl">
                    <p class="text-[10px] text-zinc-500 font-bold uppercase mb-2 tracking-[2px]">Javasolt Tét a tőkédhez:</p>
                    <p class="text-3xl font-black text-green-500 italic"><%= recommendedStake.toLocaleString() %> Ft</p>
                    <div class="mt-5 pt-4 border-t border-white/5 flex justify-between items-center text-[10px]">
                        <span class="text-zinc-600 uppercase font-bold tracking-widest">Hol keresd?</span>
                        <span class="text-white font-black uppercase tracking-widest bg-zinc-800 px-3 py-1 rounded-lg"><%= dailyTip.bookmaker %></span>
                    </div>
                </div>
            <% } else { %>
                <div class="py-20 text-center text-zinc-600 text-[10px] font-black uppercase tracking-[0.5em] animate-pulse italic">Az Öreg Róka épp a Flashscore-t bújja...</div>
            <% } %>
        </div>

        <div class="px-6 flex justify-between items-center">
            <p class="text-[10px] text-zinc-500 uppercase font-bold tracking-widest italic opacity-50">Tőkéd: <%= user.startingCapital.toLocaleString() %> Ft</p>
        </div>
    </div>
</body>
</html>