<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin HQ | Skyhigh</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@800&display=swap" rel="stylesheet">
</head>
<body class="bg-[#050505] text-white p-4 font-sans">
    <div class="max-w-md mx-auto">
        <header class="flex justify-between items-center mb-10 pt-4">
            <h1 style="font-family: 'Orbitron';" class="text-xl font-bold text-purple-500 italic uppercase">Admin HQ</h1>
            <a href="/dashboard" class="bg-zinc-800 px-4 py-1 rounded-full text-[10px] font-bold uppercase text-zinc-400">Dashboard</a>
        </header>

        <div class="bg-zinc-900 p-6 rounded-3xl border border-purple-500/30 mb-8 text-center">
            <form action="/admin/run-robot" method="POST">
                <button type="submit" class="w-full bg-purple-600 py-4 rounded-2xl font-black uppercase text-xs">Elemzés Indítása (Reggel 8:00)</button>
            </form>
            <% if (status === 'success') { %><p class="text-green-500 text-[10px] mt-4 font-black uppercase">✅ A mai tipp kint van!</p><% } %>
        </div>

        <div class="bg-zinc-900 p-6 rounded-3xl border border-white/5 mb-8">
            <h3 class="text-[10px] font-black uppercase text-purple-500 mb-6 tracking-widest text-center italic">Ügyfelek Kezelése</h3>
            <div class="space-y-4">
                <% users.forEach(u => { %>
                    <div class="flex justify-between items-center border-b border-white/5 pb-3">
                        <div class="flex-1">
                            <p class="text-xs font-bold"><%= u.fullname %></p>
                            <p class="text-[9px] text-zinc-500 font-mono"><%= u.email %></p>
                            <p class="text-[9px] <%= u.hasLicense ? 'text-green-500' : 'text-red-500' %> font-bold uppercase mt-1">
                                <%= u.hasLicense ? 'Aktív' : 'Nincs licenc' %>
                            </p>
                        </div>
                        <% if (!u.hasLicense) { %>
                            <form action="/admin/activate-user" method="POST">
                                <input type="hidden" name="userId" value="<%= u._id %>">
                                <button type="submit" class="bg-green-600 text-[9px] px-3 py-1 rounded-lg font-black uppercase">Aktiválás</button>
                            </form>
                        <% } %>
                    </div>
                <% }) %>
            </div>
        </div>

        <div class="bg-zinc-900 p-6 rounded-3xl border border-white/5">
            <h3 class="text-[10px] font-black uppercase text-zinc-500 mb-6 tracking-widest text-center">Havi Profit Napló</h3>
            <table class="w-full text-[10px] text-left">
                <thead class="text-zinc-600 uppercase"><tr><th class="pb-2">Hónap</th><th class="pb-2 text-center">Tipp</th><th class="pb-2 text-right">Profit</th></tr></thead>
                <tbody class="divide-y divide-white/5">
                    <% stats.forEach(s => { %>
                        <tr><td class="py-3 font-mono"><%= s.month %></td><td class="py-3 text-center"><%= s.totalTips %></td><td class="py-3 text-right text-green-400 font-black">+<%= s.totalProfit %>%</td></tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>