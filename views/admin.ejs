<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Owner HQ | <%= brandName %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        body { background: #000; color: #fff; font-family: 'Inter', sans-serif; overflow-x: hidden; }
        #sidebar { position: fixed; top: 0; left: 0; width: 260px; height: 100vh; background: #09090b; border-right: 1px solid #222; z-index: 50; transition: transform 0.3s ease; transform: translateX(-100%); }
        #sidebar.open { transform: translateX(0); }
        @media (min-width: 1024px) { #sidebar { transform: translateX(0); } main { margin-left: 260px; } #mobile-nav { display: none; } }
        .menu-item { padding: 16px 25px; color: #666; font-size: 11px; font-weight: 800; text-transform: uppercase; cursor: pointer; border-left: 3px solid transparent; transition: 0.2s; }
        .menu-item.active { color: #fff; background: #111; border-left-color: #c084fc; }
        .hidden { display: none !important; }
        .msg-ai { background: #1e1b4b; padding: 12px; border-radius: 12px; font-size: 11px; margin-bottom: 8px; border: 1px solid #312e81; }
        #overlay { position: fixed; inset: 0; background: rgba(0,0,0,0.7); backdrop-filter: blur(4px); z-index: 40; display: none; }
        #overlay.active { display: block; }
        .tech-card { background: #0a0a0a; border: 1px solid #222; border-radius: 20px; margin-bottom: 20px; overflow: hidden; }
    </style>
    <script>
        function toggleMenu() { document.getElementById('sidebar').classList.toggle('open'); document.getElementById('overlay').classList.toggle('active'); }
        function switchTab(id) {
            document.querySelectorAll('.page-section').forEach(e => e.classList.add('hidden'));
            document.querySelectorAll('.menu-item').forEach(b => b.classList.remove('active'));
            document.getElementById('tab-' + id).classList.remove('hidden');
            document.getElementById('btn-' + id).classList.add('active');
            if (window.innerWidth < 1024) toggleMenu();
            window.scrollTo(0,0);
        }
    </script>
</head>
<body class="min-h-screen">
    <header id="mobile-nav" class="fixed top-0 left-0 w-full h-16 bg-black border-b border-zinc-800 flex items-center justify-between px-5 z-30">
        <button onclick="toggleMenu()" class="text-white text-2xl">☰</button>
        <div class="font-black text-xs tracking-widest uppercase">FOX HQ</div>
        <div class="w-8"></div> 
    </header>
    <div id="overlay" onclick="toggleMenu()"></div>

    <nav id="sidebar">
        <div class="px-6 py-8"><h2 class="text-white font-black text-xl uppercase tracking-widest">FOX HQ</h2><p class="text-[10px] text-zinc-500 font-bold uppercase tracking-tighter">Control Center v81</p></div>
        <div class="mt-4">
            <div onclick="switchTab('dashboard')" id="btn-dashboard" class="menu-item active">📊 Vezérlőpult</div>
            <div onclick="switchTab('chat')" id="btn-chat" class="menu-item">🤖 Róka Agya</div>
            <div onclick="switchTab('users')" id="btn-users" class="menu-item">👥 Tagok Kezelése</div>
            <div onclick="switchTab('finance')" id="btn-finance" class="menu-item">💰 Pénzügy</div>
            <div onclick="switchTab('insta')" id="btn-insta" class="menu-item">📸 Instagram</div>
            <div onclick="switchTab('email')" id="btn-email" class="menu-item">📩 Email</div>
        </div>
        <div class="absolute bottom-8 left-0 w-full px-6"><a href="/logout" class="block text-center py-3 border border-red-900/30 text-red-500 rounded-xl text-[10px] font-black uppercase">Kilépés</a></div>
    </nav>

    <main class="pt-20 lg:pt-10 px-5 pb-10">
        <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-8">
            <div class="bg-zinc-900 p-5 rounded-2xl border border-zinc-800"><p class="text-[9px] text-zinc-500 font-black uppercase mb-1">Átvizsgált</p><p class="text-2xl font-black text-purple-500"><%= scannedCount %></p></div>
            <div class="bg-zinc-900 p-5 rounded-2xl border border-zinc-800"><p class="text-[9px] text-zinc-500 font-black uppercase mb-1">Tagok</p><p class="text-2xl font-black text-white"><%= users.length %></p></div>
        </div>

        <div id="tab-dashboard" class="page-section">
            <div class="flex justify-between items-center mb-6"><h2 class="text-xl font-black uppercase border-l-4 border-purple-500 pl-4">Elemzések</h2><form action="/admin/run-robot" method="POST"><button class="bg-white text-black px-4 py-2 rounded-xl font-black text-[10px] uppercase">🚀 Robot</button></form></div>
            <% pendingTips.forEach(tip => { %>
                <div class="tech-card p-6">
                    <div class="flex justify-between items-center mb-2 border-b border-zinc-800 pb-2"><span class="text-[10px] text-zinc-500 font-bold uppercase">📅 <%= tip.date %></span><span class="text-[10px] text-purple-400 font-black uppercase">⏰ <%= tip.matchTime || '??:??' %></span></div>
                    <div class="flex justify-between items-start mb-4"><h3 class="text-lg font-black leading-tight"><%= tip.match %></h3></div>
                    <div class="bg-zinc-950 p-4 rounded-xl text-xs italic text-zinc-400 border border-zinc-900 mb-6">"<%= tip.reasoning %>"<div class="mt-3 flex justify-between items-center"><span class="text-white font-bold not-italic bg-zinc-800 px-2 py-1 rounded">Tipp: <%= tip.prediction %></span><span class="text-orange-500 font-bold not-italic uppercase">Odds: <%= tip.odds %></span></div></div>
                    <div class="grid grid-cols-2 gap-3"><form action="/admin/settle-tip" method="POST" class="w-full"><input type="hidden" name="tipId" value="<%= tip._id %>"><input type="hidden" name="status" value="win"><button class="w-full bg-green-600/20 text-green-500 border border-green-500/20 py-3 rounded-xl font-black text-[10px] uppercase">Nyert</button></form><form action="/admin/settle-tip" method="POST" class="w-full"><input type="hidden" name="tipId" value="<%= tip._id %>"><input type="hidden" name="status" value="loss"><button class="w-full bg-red-600/20 text-red-500 border border-red-500/20 py-3 rounded-xl font-black text-[10px] uppercase">Vesztett</button></form></div>
                    <% if(!tip.isPublished) { %><form action="/admin/publish-tip" method="POST" class="mt-3"><input type="hidden" name="tipId" value="<%= tip._id %>"><button class="w-full bg-blue-600 py-4 rounded-2xl font-black text-[10px] uppercase tracking-widest shadow-lg shadow-blue-600/20">📢 Publikálás</button></form><% } %>
                </div>
            <% }) %>
        </div>

        <div id="tab-chat" class="page-section hidden">
            <div class="tech-card p-4 h-[60vh] flex flex-col">
                <div id="chat-win" class="flex-1 overflow-y-auto mb-4 space-y-2 pr-1"><% chatHistory.forEach(msg => { %> <div class="msg-ai"><strong><%= msg.sender %>:</strong> <%= msg.text %></div> <% }) %></div>
                <div class="flex gap-2 bg-black border border-zinc-800 p-2 rounded-2xl"><input type="text" id="chat-in" class="flex-1 bg-transparent px-3 text-xs text-white outline-none" placeholder="Üzenet a Rókának..."><button onclick="talk()" class="bg-purple-600 px-4 py-3 rounded-xl font-bold">➤</button></div>
            </div>
        </div>

        <div id="tab-users" class="page-section hidden">
            <h2 class="text-xl font-black uppercase mb-6 border-l-4 border-blue-500 pl-4">Tagok & Licenszek</h2>
            <div class="tech-card">
                <div class="overflow-x-auto">
                    <table class="w-full text-left text-[10px]">
                        <thead class="bg-zinc-900 uppercase"><tr><th class="p-4">Név / Email</th><th class="p-4">Állapot</th><th class="p-4 text-right">Művelet</th></tr></thead>
                        <tbody class="divide-y divide-zinc-900">
                            <% users.forEach(u => { %> 
                                <tr>
                                    <td class="p-4"><div class="font-bold"><%= u.fullname %></div><div class="text-zinc-500 text-[9px]"><%= u.email %></div></td>
                                    <td class="p-4"><%= u.hasLicense ? '✅ AKTÍV' : '🔒 INGYENES' %></td>
                                    <td class="p-4 text-right">
                                        <form action="/admin/toggle-license" method="POST">
                                            <input type="hidden" name="userId" value="<%= u._id %>">
                                            <% if(u.hasLicense) { %>
                                                <button class="bg-red-900/30 text-red-500 border border-red-500/30 px-3 py-2 rounded-lg font-bold text-[9px] uppercase">Tiltás</button>
                                            <% } else { %>
                                                <button class="bg-green-900/30 text-green-500 border border-green-500/30 px-3 py-2 rounded-lg font-bold text-[9px] uppercase">Aktiválás</button>
                                            <% } %>
                                        </form>
                                    </td>
                                </tr> 
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <div id="tab-finance" class="page-section hidden">
            <div class="grid grid-cols-1 gap-4">
                <div class="tech-card p-10 text-center"><p class="text-zinc-500 font-bold uppercase text-[10px] mb-2 tracking-widest">Összesített tőke</p><h3 class="text-4xl font-black"><%= users.reduce((s, u) => s + (u.currentBankroll || 0), 0).toLocaleString() %> Ft</h3></div>
                <div class="tech-card p-10 text-center border-green-900/20"><p class="text-zinc-500 font-bold uppercase text-[10px] mb-2 tracking-widest">Havi Profit</p><h3 class="text-4xl font-black text-green-500">+<%= users.reduce((s, u) => s + (u.monthlyProfit || 0), 0).toLocaleString() %> Ft</h3></div>
            </div>
        </div>

        <div id="tab-insta" class="page-section hidden">
            <div class="tech-card p-8 text-center"><div class="mb-6 text-4xl">📸</div><button onclick="generateInsta()" class="w-full bg-gradient-to-r from-purple-600 to-pink-600 py-4 rounded-2xl font-black uppercase text-[10px] tracking-widest shadow-xl shadow-purple-600/20">Poszt Szöveg Generálása</button><div id="insta-res" class="mt-8 bg-zinc-950 p-5 rounded-2xl hidden text-[11px] text-zinc-300 border border-zinc-800 text-left whitespace-pre-wrap leading-relaxed"></div></div>
        </div>

        <div id="tab-email" class="page-section hidden"><div class="tech-card p-10 text-center opacity-40"><p class="font-black uppercase text-xs">Email rendszer fejlesztés alatt...</p></div></div>
    </main>

    <script>
        async function talk() {
            const i = document.getElementById('chat-in'); if(!i.value) return;
            const r = await fetch('/admin/chat', { method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({message: i.value}) });
            const d = await r.json(); const win = document.getElementById('chat-win'); win.innerHTML += `<div class="msg-ai"><strong>Róka:</strong> ${d.reply}</div>`; i.value = ''; win.scrollTop = win.scrollHeight;
        }
        async function generateInsta() { const btn = event.target; const originalText = btn.innerText; btn.innerText = "⏳ GENERÁLÁS..."; try { const r = await fetch('/admin/generate-insta', { method: 'POST' }); const d = await r.json(); const res = document.getElementById('insta-res'); res.innerText = d.caption; res.classList.remove('hidden'); } catch(e) { alert("Hiba!"); } btn.innerText = originalText; }
    </script>
</body>
</html>