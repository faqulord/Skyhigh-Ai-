<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <title>Admin Control | Skyhigh AI</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-zinc-950 text-white p-10">
    <div class="max-w-6xl mx-auto">
        <div class="flex justify-between items-center mb-10 border-b border-white/5 pb-6">
            <h1 class="text-3xl font-black italic text-red-500 uppercase italic tracking-tighter">Skyhigh <span class="text-white">Admin</span></h1>
            <form action="/admin/run-ai" method="POST">
                <button class="bg-red-600 hover:bg-red-700 text-white px-8 py-3 rounded-full font-black text-xs uppercase tracking-widest transition">
                    <i class="fas fa-robot mr-2"></i> AI Kényszerített Indítása
                </button>
            </form>
        </div>

        <div class="grid grid-cols-1 gap-6">
            <div class="bg-zinc-900 border border-white/5 rounded-3xl p-8">
                <h2 class="text-xl font-bold mb-6 italic">Felhasználók Kezelése (<%= allUsers.length %>)</h2>
                <table class="w-full text-left">
                    <tr class="text-[10px] text-zinc-500 uppercase tracking-widest border-b border-white/5">
                        <th class="pb-4">Név / Email</th>
                        <th class="pb-4">Licenc</th>
                        <th class="pb-4">Regisztrált</th>
                        <th class="pb-4 text-right">Művelet</th>
                    </tr>
                    <% allUsers.forEach(u => { %>
                    <tr class="border-b border-white/5 hover:bg-white/5 transition">
                        <td class="py-4">
                            <p class="font-bold"><%= u.fullname %></p>
                            <p class="text-[10px] text-zinc-500"><%= u.email %></p>
                        </td>
                        <td class="py-4">
                            <span class="px-3 py-1 rounded-full text-[9px] font-black uppercase <%= u.hasLicense ? 'bg-green-500/20 text-green-500' : 'bg-red-500/20 text-red-500' %>">
                                <%= u.hasLicense ? 'Aktív' : 'Nincs' %>
                            </span>
                        </td>
                        <td class="py-4 text-xs text-zinc-500"><%= u.createdAt.toLocaleDateString() %></td>
                        <td class="py-4 text-right">
                            <form action="/admin/toggle-license/<%= u._id %>" method="POST">
                                <button class="text-[10px] font-black uppercase text-purple-500 hover:text-white transition">
                                    Licenc Váltás
                                </button>
                            </form>
                        </td>
                    </tr>
                    <% }) %>
                </table>
            </div>
        </div>
        <div class="mt-10">
            <a href="/dashboard" class="text-zinc-600 uppercase text-[10px] font-bold tracking-widest hover:text-white">Vissza a Dashboardra</a>
        </div>
    </div>
</body>
</html>