<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@900&display=swap" rel="stylesheet">
</head>
<body class="bg-black text-white p-4 font-sans">
    <div class="max-w-md mx-auto">
        <h1 style="font-family: 'Orbitron';" class="text-xl text-purple-500 italic mb-8 pt-4">ADMIN HQ</h1>

        <div id="control-panel" class="bg-zinc-900 p-6 rounded-3xl border border-purple-500/30 mb-8 text-center">
            <h3 class="text-[10px] font-black uppercase mb-4 text-zinc-400 tracking-widest">Neural Robot Elemzés</h3>
            
            <form id="robot-form" action="/admin/run-robot" method="POST" onsubmit="startAnalysis()">
                <button type="submit" id="start-btn" class="w-full bg-purple-600 py-4 rounded-2xl font-black uppercase text-xs shadow-lg shadow-purple-600/20 active:scale-95 transition">
                    Elemzés Indítása (Reggel 8:00 szimuláció)
                </button>
            </form>

            <div id="loading-screen" class="hidden py-4">
                <div class="animate-spin h-8 w-8 border-4 border-purple-500 border-t-transparent rounded-full mx-auto mb-4"></div>
                <h4 class="text-purple-500 font-bold uppercase text-xs mb-2">Neural Engine Számítás folyamatban</h4>
                <div class="text-[10px] text-zinc-400 italic space-y-1 text-left px-2">
                    <p id="step1" class="opacity-50">● 10 éves H2H statisztikai mintavételezés...</p>
                    <p id="step2" class="opacity-50">● Poisson-eloszlás és gólvalószínűség számítása...</p>
                    <p id="step3" class="opacity-50">● Piaci odds-mozgások és smart money detektálása...</p>
                    <p id="step4" class="opacity-50">● Bankroll stratégia és optimális tét meghatározása...</p>
                </div>
            </div>

            <% if (status === 'success') { %>
                <p class="text-purple-400 text-[10px] mt-4 font-black animate-pulse">✅ ELEMZÉS KÉSZ, A TIPP KINT VAN!</p>
            <% } %>
        </div>

        <script>
            function startAnalysis() {
                document.getElementById('robot-form').classList.add('hidden');
                document.getElementById('loading-screen').classList.remove('hidden');
                
                // Animált szöveg szimulációja
                setTimeout(() => { document.getElementById('step1').classList.remove('opacity-50'); document.getElementById('step1').classList.add('text-green-500'); }, 3000);
                setTimeout(() => { document.getElementById('step2').classList.remove('opacity-50'); document.getElementById('step2').classList.add('text-green-500'); }, 15000);
                setTimeout(() => { document.getElementById('step3').classList.remove('opacity-50'); document.getElementById('step3').classList.add('text-green-500'); }, 30000);
                setTimeout(() => { document.getElementById('step4').classList.remove('opacity-50'); document.getElementById('step4').classList.add('text-green-500'); }, 45000);
            }
        </script>

        <div class="bg-zinc-900 p-6 rounded-3xl border border-white/5 mb-6">
            <p class="text-[9px] uppercase text-zinc-500 font-bold mb-1 italic">Napi Bevétel</p>
            <h2 class="text-3xl font-black text-green-500 italic"><%= totalRevenue.toLocaleString() %> Ft</h2>
        </div>
    </div>
</body>
</html>