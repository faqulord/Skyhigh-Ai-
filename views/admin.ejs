<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin HQ | Skyhigh</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-[#050505] text-white p-4 font-sans">
    <div class="max-w-md mx-auto">
        <header class="flex justify-between items-center mb-8"><h1 class="text-xl font-bold text-purple-500 uppercase italic">Admin HQ</h1><a href="/dashboard" class="text-[10px] text-zinc-400">Dashboard</a></header>
        <div class="bg-zinc-900 p-6 rounded-3xl mb-8 text-center">
            <% if (tipExists) { %><h2 class="text-xs text-purple-400 animate-pulse font-black uppercase tracking-widest">A Napi Fix élesítve!</h2><% } else { %>
            <form action="/admin/run-robot" method="POST"><button type="submit" class="w-full bg-purple-600 py-4 rounded-2xl font-black text-xs uppercase">Elemzés Indítása (08:00)</button></form><% } %>
        </div>
        <div class="bg-zinc-900 p-6 rounded-3xl mb-8 border border-purple-500/20">
            <h3 class="text-[10px] text-purple-500 mb-4 text-center font-black">Havi Profit Kalkulátor</h3>
            <table class="w-full text-[9px] text-left">
                <thead><tr><th>Nap</th><th>Meccs</th><th class="text-right">+/-</th><th class="text-right">Össz.</th></tr></thead>
                <tbody><% calculatorData.forEach(row => { %><tr><td><%= row.date.split('-')[2] %>.</td><td class="truncate max-w-[80px]"><%= row.match %></td><td class="text-right <%= row.dailyProfit >= 0 ? 'text-green-500' : 'text-red-500' %>"><%= row.dailyProfit %>%</td><td class="text-right font-black"><%= row.totalRunning.toFixed(1) %>%</td></tr><% }) %></tbody>
            </table>
        </div>
        <div class="bg-zinc-900 p-6 rounded-3xl mb-8">
            <h3 class="text-[10px] text-zinc-500 mb-4 text-center font-black">Tagok Aktiválása</h3>
            <% users.forEach(u => { %><div class="flex justify-between items-center py-2 border-b border-white/5"><span class="text-[10px] truncate w-32"><%= u.fullname %></span><% if (!u.hasLicense) { %><form action="/admin/activate-user" method="POST"><input type="hidden" name="userId" value="<%= u._id %>"><button class="bg-green-600 text-[8px] px-2 py-1 rounded">Aktivál</button></form><% } else { %><span class="text-green-500 text-[8px] font-bold">TAG</span><% } %></div><% }) %>
        </div>
    </div>
</body>
</html>