<main class="pt-[100px] px-4 pb-20 max-w-2xl mx-auto">
        
        <div id="tab-dashboard" class="content-tab">
            <div class="tech-card p-6 border-t-2 border-purple-600">
                <h2 class="text-lg font-bold mb-4">🤖 Mai Robot Státusz</h2>
                <% if (currentTip) { %>
                    <% } else { %>
                    <form action="/admin/run-robot" method="POST" class="text-center py-6">
                        <button class="bg-white text-black px-8 py-4 rounded-xl font-black uppercase text-xs pulse-btn">🚀 ROBOT INDÍTÁSA MOST</button>
                    </form>
                <% } %>
            </div>
        </div>

        <div id="tab-marketing" class="content-tab hidden-section">
            <h2 class="text-lg font-bold mb-4">📱 Marketing Generátor</h2>
            <div class="tech-card p-5">
                <p class="text-xs text-zinc-500 mb-4 font-bold uppercase tracking-widest">Válassz stílust:</p>
                <div class="grid grid-cols-2 gap-3 mb-4">
                    <button onclick="generateSocial('win')" class="bg-zinc-800 p-3 rounded-xl text-[10px] font-bold border border-green-900/30 text-green-400">🏆 MAI NYERŐ</button>
                    <button onclick="generateSocial('hype')" class="bg-zinc-800 p-3 rounded-xl text-[10px] font-bold border border-pink-900/30 text-pink-400">🔥 HYPE / CSATLAKOZZ</button>
                </div>
                <textarea id="social-result" class="w-full bg-black border border-zinc-800 rounded-xl p-3 text-xs text-zinc-300 mb-2 h-32" placeholder="Itt jelenik meg a szöveg..."></textarea>
                <button onclick="copyText()" class="w-full bg-zinc-800 py-3 rounded-xl text-[10px] font-bold uppercase">Másolás</button>
            </div>
        </div>

        <div id="tab-finance" class="content-tab hidden-section">
            <h2 class="text-lg font-bold mb-4">💰 Pénzügyi Áttekintés</h2>
            <div class="tech-card p-5">
                </div>
        </div>

        <div id="tab-chat" class="content-tab hidden-section">
            <h2 class="text-lg font-bold mb-4">💬 Stratégiai Chat</h2>
            <div class="tech-card p-4">
                <div id="chat-window" class="h-64 overflow-y-auto mb-4 space-y-2 p-2 bg-black/50 rounded-xl border border-zinc-800">
                    </div>
                <div class="flex gap-2">
                    <input type="text" id="chat-msg" class="flex-1 bg-black border border-zinc-800 rounded-xl p-3 text-xs" placeholder="Kérdezz a Rókától...">
                    <button onclick="sendMessage()" class="bg-purple-600 px-4 rounded-xl font-bold">➤</button>
                </div>
            </div>
        </div>

    </main>