<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <title>Skyhigh | Director Mode</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>body { background: #000; color: white; font-family: sans-serif; }</style>
</head>
<body class="p-6">

    <div class="max-w-6xl mx-auto">
        <div class="flex justify-between items-center mb-10">
            <div>
                <h1 class="text-2xl font-black uppercase text-yellow-500 italic">Vezérlőpult</h1>
                <p class="text-[10px] text-zinc-500 uppercase tracking-widest">Skyhigh AI Startup Metrics</p>
            </div>
            <a href="/dashboard" class="bg-zinc-800 text-white px-4 py-2 rounded-lg text-[10px] font-black uppercase">Vissza a Dashboardra</a>
        </div>

        <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-10">
            <div class="bg-[#0a0a0a] p-6 rounded-2xl border border-zinc-800">
                <p class="text-[10px] text-zinc-500 uppercase">Összes Regisztrált</p>
                <p class="text-3xl font-black text-white"><%= users.length %></p>
            </div>
            <div class="bg-[#0a0a0a] p-6 rounded-2xl border border-zinc-800">
                <p class="text-[10px] text-zinc-500 uppercase">Fizetett Licencek</p>
                <p class="text-3xl font-black text-green-500">
                    <%= users.filter(u => u.hasLicense).length %>
                </p>
            </div>
        </div>

        <div class="bg-[#0a0a0a] rounded-2xl border border-zinc-800 overflow-hidden">
            <div class="p-4 border-b border-zinc-800">
                <h3 class="text-sm font-black uppercase text-zinc-400">Felhasználói Adatbázis</h3>
            </div>
            <div class="overflow-x-auto">
                <table class="w-full text-left text-[11px]">
                    <thead class="bg-zinc-900 text-zinc-500 uppercase">
                        <tr>
                            <th class="p-4">Név</th>
                            <th class="p-4">Email</th>
                            <th class="p-4">Kód</th>
                            <th class="p-4">Státusz</th>
                            <th class="p-4">Regisztrált</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-zinc-800 text-zinc-300">
                        <% users.forEach(user => { %>
                        <tr class="hover:bg-zinc-900/50 transition">
                            <td class="p-4 font-bold text-white"><%= user.fullname %></td>
                            <td class="p-4"><%= user.email %></td>
                            <td class="p-4 font-mono text-purple-400"><%= user.myReferralCode %></td>
                            <td class="p-4">
                                <% if (user.hasLicense) { %>
                                    <span class="bg-green-500/20 text-green-500 px-2 py-1 rounded text-[9px] font-black uppercase">Aktív</span>
                                <% } else { %>
                                    <span class="bg-zinc-800 text-zinc-500 px-2 py-1 rounded text-[9px] font-black uppercase">Ingyenes</span>
                                <% } %>
                            </td>
                            <td class="p-4 opacity-50"><%= user.date.toISOString().substring(0,10) %></td>
                        </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

</body>
</html>