<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Owner HQ | <%= brandName %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@800&family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        body { background: #000; color: #fff; font-family: 'Inter', sans-serif; }
        .tech-card { background: #0a0a0a; border: 1px solid #222; border-radius: 20px; margin-bottom: 20px; }
        .hidden { display: none !important; }
        #sidebar { position: fixed; top: 0; left: 0; width: 260px; height: 100vh; background: #09090b; border-right: 1px solid #222; z-index: 50; padding-top: 80px; transform: translateX(-100%); transition: 0.3s; }
        #sidebar.open { transform: translateX(0); }
        @media (min-width: 1024px) { #sidebar { transform: translateX(0); } }
        .menu-item { display: flex; align-items: center; gap: 15px; width: 100%; padding: 18px 25px; color: #666; font-size: 11px; font-weight: 800; text-transform: uppercase; cursor: pointer; border-left: 3px solid transparent; }
        .menu-item.active { color: #fff; background: #111; border-left-color: #c084fc; }
        .msg { padding: 10px; border-radius: 12px; font-size: 11px; margin-bottom: 8px; border: 1px solid #111; }
        .msg-system { background: #111; color: #666; }
        .msg-ai { background: #1e1b4b; border-color: #312e81; color: #c7d2fe; }
    </style>
</head>
<body class="min-h-screen">
    <nav id="sidebar">
        <div class="px-6 mb-6"><h2 class="text-white font-black text-xl uppercase tracking-widest">HQ CONTROL</h2><p class="text-[10px] text-zinc-500 uppercase">Zsivány Róka v79</p></div>
        <div onclick="switchTab('dashboard')" id="btn-dashboard" class="menu-item active">📊 VEZÉRLŐPULT</div>
        <div onclick="switchTab('chat')" id="btn-chat" class="menu-item">🤖 RÓKA AGYA</div>
        <div onclick="switchTab('users')" id="btn-users" class="menu-item">👥 TAGOK</div>
        <div class="absolute bottom-10 left-0 w-full px-6"><a href="/logout" class="block text-center border border-red-900/50 text-red-500 py-3 rounded-xl text-[10px] font-black uppercase">Kilépés</a></div>
    </nav>

    <header class="fixed top-0 left-0 w-full bg-black/90 border-b border-zinc-800 p-4 z-40 flex justify-between items-center h-[70px] lg:hidden">
        <button onclick="document.getElementById('sidebar').classList.toggle('open')" class="text-white bg-zinc-900 px-3 py-2 rounded-lg border border-zinc-700">☰</button>
        <span class="font-black text-white uppercase tracking-widest">ZSIVÁNY HQ</span>
        <a href="/dashboard" class="text-[10px] font-bold text-purple-500 uppercase border border-purple-500 px-3 py-1 rounded-full">DASH ➤</a>
    </header>

    <main class="pt-[90px] lg:pt-[40px] px-4 pb-10 w-full lg:pl-[280px] max-w-4xl mx-auto">
        
        <div id="tab-dashboard" class="page-section">
            <div class="flex justify-between items-center mb-6">
                <h2 class="text-xl font-black text-white border-l-4 border-purple-500 pl-4 uppercase">Aktuális Elemzések</h2>
                <form action="/admin/run-robot" method="POST" onsubmit="this.querySelector('button').innerText='🦊 Szimatolás...'">
                    <button class="bg-white text-black px-6 py-3 rounded-xl font-black uppercase text-[10px] shadow-lg hover:scale-105 transition">🚀 ÚJ ELEMZÉS INDÍTÁSA</button>
                </form>
            </div>

            <% if (pendingTips && pendingTips.length > 0) { %>
                <% pendingTips.forEach(tip => { %>
                    <div class="tech-card p-6 border-b-2 border-zinc-800 mb-4 <%= tip.isPublished ? 'opacity-100' : 'border-yellow-500/50 bg-yellow-500/5' %>">
                        <div class="flex justify-between items-center mb-2">
                            <span class="text-[10px] text-zinc-500 font-bold uppercase"><%= tip.date %> | <%= tip.league %></span>
                            <% if(!tip.isPublished) { %> <span class="text-[10px] bg-yellow-600 text-white px-3 py-1 rounded-full font-black animate-pulse">JELENTÉSRE VÁR</span> <% } %>
                        </div>
                        <h3 class="text-xl font-black text-white mb-1"><%= tip.match %></h3>
                        <p class="text-purple-400 font-bold mb-4"><%= tip.prediction %> (@<%= tip.odds %>)</p>

                        <div class="grid grid-cols-2 gap-4">
                            <form action="/admin/settle-tip" method="POST"><input type="hidden" name="tipId" value="<%= tip._id %>"><input type="hidden" name="status" value="win"><button class="w-full bg-green-900/40 border border-green-500 text-green-500 py-3 rounded-xl font-black text-xs uppercase hover:bg-green-600 hover:text-white transition">WIN ✅</button></form>
                            <form action="/admin/settle-tip" method="POST"><input type="hidden" name="tipId" value="<%= tip._id %>"><input type="hidden" name="status" value="loss"><button class="w-full bg-red-900/40 border border-red-500 text-red-500 py-3 rounded-xl font-black text-xs uppercase hover:bg-red-600 hover:text-white transition">LOSS ❌</button></form>
                        </div>

                        <% if(!tip.isPublished) { %>
                            <form action="/admin/publish-tip" method="POST" class="mt-4"><input type="hidden" name="tipId" value="<%= tip._id %>"><button class="w-full bg-blue-600 text-white py-4 rounded-xl font-black text-xs uppercase shadow-xl hover:bg-blue-500 transition">📢 PUBLIKÁLÁS TAGOKNAK</button></form>
                        <% } %>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="text-center py-20 tech-card opacity-50"><p class="text-xs uppercase font-black tracking-widest text-zinc-500">Nincs aktív jelentés a listán.</p></div>
            <% } %>
        </div>

        <div id="tab-chat" class="page-section hidden">
            <h2 class="text-xl font-black mb-6 text-white border-l-4 border-blue-500 pl-4 uppercase">Róka Jelentései</h2>
            <div class="tech-card p-4 h-[500px] flex flex-col">
                <div id="chat-win" class="flex-1 overflow-y-auto custom-scrollbar pr-2">
                    <% chatHistory.forEach(msg => { %> 
                        <div class="msg <%= msg.sender === 'System' ? 'msg-system' : 'msg-ai' %>">
                            <strong class="block mb-1 opacity-50 uppercase text-[9px]"><%= msg.sender %></strong> <%= msg.text %>
                        </div> 
                    <% }) %>
                </div>
            </div>
        </div>

        <div id="tab-users" class="page-section hidden">
            <h2 class="text-xl font-black mb-6 text-white border-l-4 border-green-500 pl-4 uppercase">Falka Tagok</h2>
            <% users.forEach(u => { 
                let bank = (u.currentBankroll > 0) ? u.currentBankroll : (u.startingCapital || 0);
            %> 
                <div class="tech-card p-4 flex justify-between items-center mb-2">
                    <div><p class="text-sm font-bold"><%= u.fullname %> <span class="text-[10px] text-zinc-500 font-normal ml-2"><%= u.email %></span></p><p class="text-[10px] text-zinc-400 mt-1">BANKROLL: <span class="text-white font-bold"><%= bank.toLocaleString() %> Ft</span></p></div>
                    <form action="/admin/manage-sub" method="POST" class="flex gap-2"><input type="hidden" name="userId" value="<%= u._id %>"><button name="action" value="add30" class="bg-green-900/30 text-green-400 border border-green-600 px-3 py-2 rounded-lg text-[9px] font-black uppercase">+30 NAP</button></form>
                </div> 
            <% }) %>
        </div>
    </main>

    <script>
        function switchTab(id) { document.querySelectorAll('.page-section').forEach(e => e.classList.add('hidden')); document.querySelectorAll('.menu-item').forEach(b => b.classList.remove('active')); document.getElementById('tab-' + id).classList.remove('hidden'); document.getElementById('btn-' + id).classList.add('active'); }
        const win = document.getElementById('chat-win'); if(win) win.scrollTop = win.scrollHeight;
    </script>
</body>
</html>