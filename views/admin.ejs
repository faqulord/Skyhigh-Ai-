<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <title>Skyhigh | Cégvezetés</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>body { background-color: #000; color: white; font-family: sans-serif; }</style>
</head>
<body class="p-6 md:p-12 max-w-6xl mx-auto">

    <header class="flex justify-between items-center mb-12">
        <div>
            <h1 class="text-4xl font-black italic tracking-tighter">Skyhigh <span class="text-red-600">Executive</span></h1>
            <p class="text-zinc-500 text-[10px] uppercase tracking-[4px]">Vállalati Pénzügyi Áttekintés</p>
        </div>
        <a href="/dashboard" class="text-xs bg-zinc-900 px-4 py-2 rounded-lg font-bold">VISSZA A TERMINÁLHOZ</a>
    </header>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12">
        <div class="bg-zinc-900/50 border border-zinc-800 p-8 rounded-[24px] text-center">
            <p class="text-[10px] text-zinc-500 uppercase font-bold mb-2">Összes Regisztrált Tag</p>
            <p class="text-4xl font-black text-white"><%= stats.totalUsers %></p>
        </div>
        <div class="bg-zinc-900/50 border border-zinc-800 p-8 rounded-[24px] text-center border-blue-500/20 shadow-[0_0_30px_rgba(59,130,246,0.1)]">
            <p class="text-[10px] text-zinc-500 uppercase font-bold mb-2">Aktív Licenc (Premium)</p>
            <p class="text-4xl font-black text-blue-500"><%= stats.activeSubscribers %></p>
        </div>
        <div class="bg-zinc-900/50 border border-zinc-800 p-8 rounded-[24px] text-center border-green-500/20 shadow-[0_0_30px_rgba(34,197,94,0.1)]">
            <p class="text-[10px] text-zinc-500 uppercase font-bold mb-2">Havi Becsült Bevétel</p>
            <p class="text-4xl font-black text-green-500"><%= stats.monthlyRevenue.toLocaleString() %> Ft</p>
        </div>
    </div>

    <div class="bg-zinc-900/30 border border-zinc-800 p-8 rounded-[24px] mb-12 flex flex-col md:flex-row gap-6 items-center">
        <div class="flex-1">
            <h3 class="text-sm font-bold">Adminisztrátor Hozzáadása</h3>
            <p class="text-xs text-zinc-500">Adjon teljes tulajdonosi jogot a társ-tulajdonosoknak.</p>
        </div>
        <form action="/admin/make-admin" method="POST" class="flex gap-2 w-full md:w-auto">
            <input type="email" name="email" placeholder="Email cím" required class="bg-black border border-zinc-800 p-3 rounded-xl outline-none focus:border-red-600 text-sm">
            <button class="bg-white text-black px-6 py-3 rounded-xl text-xs font-black uppercase">Felhatalmazás</button>
        </form>
    </div>

    <div class="space-y-4 text-left">
        <h3 class="text-[10px] font-bold text-zinc-600 uppercase tracking-widest ml-4">Operátori Lista</h3>
        <% users.forEach(u => { %>
            <div class="bg-zinc-900 border border-zinc-800 p-6 rounded-2xl flex justify-between items-center transition hover:bg-zinc-800/50">
                <div>
                    <div class="flex items-center gap-3">
                        <p class="font-bold text-lg"><%= u.fullname %></p>
                        <% if (u.isAdmin) { %>
                            <span class="text-red-500 text-[8px] font-black border border-red-500/30 px-2 py-0.5 rounded uppercase tracking-tighter">Owner</span>
                        <% } %>
                    </div>
                    <p class="text-xs text-zinc-500 font-mono"><%= u.email %></p>
                </div>
                <div class="text-right">
                    <p class="text-green-500 font-mono font-black text-xl"><%= u.startingCapital || 0 %> Ft</p>
                    <p class="text-[10px] <%= u.hasLicense ? 'text-blue-500' : 'text-zinc-600' %> font-bold uppercase tracking-widest">
                        <%= u.hasLicense ? 'Premium Client' : 'Demo User' %>
                    </p>
                </div>
            </div>
        <% }) %>
    </div>

</body>
</html>