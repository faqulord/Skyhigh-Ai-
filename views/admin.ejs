<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Owner HQ | <%= brandName %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@800&family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
    
    <style>
        body { background: #000000; color: #fff; font-family: 'Inter', sans-serif; overflow-x: hidden; }
        .tech-card { background: #0a0a0a; border: 1px solid #222; border-radius: 20px; margin-bottom: 20px; box-shadow: 0 4px 20px rgba(0,0,0,0.5); }
        .hidden { display: none !important; }
        
        /* OLDALSÁV */
        #sidebar { position: fixed; top: 0; left: 0; width: 260px; height: 100vh; background: #09090b; border-right: 1px solid #222; z-index: 50; transform: translateX(-100%); transition: 0.3s cubic-bezier(0.4, 0, 0.2, 1); padding-top: 80px; }
        #sidebar.open { transform: translateX(0); }
        
        /* OVERLAY MOBILON */
        #overlay { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.8); z-index: 40; display: none; backdrop-filter: blur(5px); }
        #overlay.show { display: block; }
        
        /* MENÜ GOMBOK */
        .menu-item { display: flex; align-items: center; gap: 15px; width: 100%; padding: 18px 25px; color: #666; font-size: 11px; font-weight: 800; text-transform: uppercase; letter-spacing: 1px; transition: all 0.2s; border-left: 3px solid transparent; }
        .menu-item:hover { color: #fff; background: #111; }
        .menu-item.active { color: #c084fc; border-left: 3px solid #c084fc; background: linear-gradient(to right, #1a1a1a, transparent); }
        
        /* CHAT */
        .chat-container { height: 400px; overflow-y: auto; display: flex; flex-direction: column; gap: 10px; padding: 10px; background: #050505; border-radius: 12px; border: 1px solid #222; }
        .msg { padding: 10px 15px; border-radius: 12px; font-size: 12px; max-width: 80%; line-height: 1.4; }
        .msg-me { align-self: flex-end; background: #2563eb; color: white; border-bottom-right-radius: 2px; }
        .msg-ai { align-self: flex-start; background: #222; color: #ccc; border: 1px solid #333; border-bottom-left-radius: 2px; }
        
        .orange-neon { color: #FF9F43; text-shadow: 0 0 10px rgba(255, 159, 67, 0.4); }
    </style>

    <script>
        function toggleSidebar() {
            const sb = document.getElementById('sidebar');
            const ov = document.getElementById('overlay');
            if (sb.classList.contains('open')) { sb.classList.remove('open'); ov.classList.remove('show'); } 
            else { sb.classList.add('open'); ov.classList.add('show'); }
        }

        function switchTab(id) {
            document.querySelectorAll('.page-section').forEach(e => e.classList.add('hidden'));
            document.querySelectorAll('.menu-item').forEach(b => b.classList.remove('active'));
            const targetSection = document.getElementById('tab-' + id);
            const targetBtn = document.getElementById('btn-' + id);
            if(targetSection) targetSection.classList.remove('hidden');
            if(targetBtn) targetBtn.classList.add('active');
            if(window.innerWidth < 1024) { toggleSidebar(); }
            window.scrollTo(0,0);
        }
    </script>
</head>
<body class="min-h-screen">

    <div id="overlay" onclick="toggleSidebar()"></div>

    <nav id="sidebar">
        <div class="px-6 mb-6">
            <h2 class="text-white font-black text-xl tracking-widest uppercase">HQ <span class="text-purple-500">CONTROL</span></h2>
            <p class="text-[10px] text-zinc-600 font-bold">Adminisztrációs Központ</p>
        </div>
        <button onclick="switchTab('dashboard')" id="btn-dashboard" class="menu-item active"><span>📊</span> VEZÉRLŐPULT</button>
        <button onclick="switchTab('chat')" id="btn-chat" class="menu-item"><span>🤖</span> RÓKA AGYA (CHAT)</button>
        <button onclick="switchTab('marketing')" id="btn-marketing" class="menu-item"><span>📢</span> MARKETING</button>
        <button onclick="switchTab('users')" id="btn-users" class="menu-item"><span>👥</span> TAGOK & LICENSZ</button>
        <button onclick="switchTab('finance')" id="btn-finance" class="menu-item"><span>💰</span> PÉNZÜGY</button>
        <div class="absolute bottom-10 left-0 w-full px-6">
            <a href="/logout" class="block w-full text-center border border-red-900/50 text-red-500 py-3 rounded-xl text-[10px] font-black uppercase hover:bg-red-900/20 transition">Kijelentkezés</a>
        </div>
    </nav>

    <header class="fixed top-0 left-0 w-full bg-black/90 border-b border-zinc-900 p-4 z-40 flex justify-between items-center h-[70px] backdrop-blur-md">
        <button onclick="toggleSidebar()" class="text-white bg-zinc-900 p-2 rounded-lg border border-zinc-800 lg:hidden">☰ MENÜ</button>
        <div class="flex items-center gap-3">
            <span class="text-2xl">🦊</span>
            <span class="font-black text-white tracking-widest hidden sm:block">ZSIVÁNY HQ</span>
        </div>
        <a href="/dashboard" class="bg-purple-600 text-white text-[10px] font-black px-4 py-2 rounded-full shadow-[0_0_15px_rgba(168,85,247,0.4)]">VISSZA A DASHBOARDRA ➤</a>
    </header>

    <main class="pt-[90px] px-4 pb-10 w-full lg:pl-[280px] max-w-6xl mx-auto">
        
        <div id="tab-dashboard" class="page-section">
            <h2 class="text-xl font-black mb-6 text-white border-l-4 border-purple-500 pl-4">Stratégia Központ</h2>
            
            <div class="tech-card p-6 border-t-2 border-t-orange-500">
                <div class="flex justify-between items-center mb-5 text-[11px] font-bold text-zinc-500 uppercase tracking-widest">
                    <span>Aktuális Mód</span>
                    <span class="orange-neon text-sm"><%= strategyMode.toUpperCase() %></span>
                </div>
                <form action="/admin/update-settings" method="POST" class="grid grid-cols-3 gap-3">
                    <button name="mode" value="recovery" class="py-4 rounded-xl text-[10px] font-black border transition hover:scale-105 <%= strategyMode === 'recovery' ? 'bg-blue-600 border-blue-400 text-white shadow-lg' : 'bg-zinc-900 border-zinc-800 text-zinc-500' %>">🛡️ ÓVATOS (1.5%)</button>
                    <button name="mode" value="normal" class="py-4 rounded-xl text-[10px] font-black border transition hover:scale-105 <%= strategyMode === 'normal' ? 'bg-purple-600 border-purple-400 text-white shadow-lg' : 'bg-zinc-900 border-zinc-800 text-zinc-500' %>">🦊 NORMAL (3%)</button>
                    <button name="mode" value="aggressive" class="py-4 rounded-xl text-[10px] font-black border transition hover:scale-105 <%= strategyMode === 'aggressive' ? 'bg-orange-600 border-orange-400 text-white shadow-lg' : 'bg-zinc-900 border-zinc-800 text-zinc-500' %>">🔥 ZSIVÁNY (6%)</button>
                </form>
            </div>

            <div class="tech-card p-6">
                <% if (currentTip) { %>
                    <div class="mb-6">
                        <span class="bg-zinc-800 text-zinc-400 px-2 py-1 rounded text-[10px] font-bold uppercase"><%= currentTip.league %></span>
                        <h3 class="text-2xl font-black mt-2 mb-1"><%= currentTip.match %></h3>
                        <p class="text-sm text-purple-400 font-mono font-bold"><%= currentTip.prediction %> <span class="text-white">(@<%= currentTip.odds %>)</span></p>
                        <% if(!currentTip.isPublished) { %>
                             <form action="/admin/publish-tip" method="POST" class="mt-4"><input type="hidden" name="tipId" value="<%= currentTip._id %>"><button class="w-full bg-blue-600 hover:bg-blue-500 text-white py-3 rounded-xl font-black text-xs uppercase shadow-lg animate-pulse">📢 PUBLIKÁLÁS A TAGOKNAK</button></form>
                        <% } else { %><p class="text-green-500 text-[10px] font-bold uppercase mt-2">✅ Publikálva a Dashboardon</p><% } %>
                    </div>
                    <div class="border-t border-zinc-800 pt-4 mb-4">
                         <p class="text-[10px] text-zinc-500 font-bold uppercase mb-2">Szöveg AI Finomítása:</p>
                        <form action="/admin/refine-text" method="POST"><button class="w-full bg-zinc-800 hover:bg-zinc-700 text-white py-2 rounded-lg text-[10px] font-bold uppercase border border-zinc-700">📝 Szöveg Újragenerálása</button></form>
                    </div>
                    <div class="grid grid-cols-2 gap-4 mt-6">
                        <form action="/admin/settle-tip" method="POST" onsubmit="return confirm('Biztos NYERŐ a tipp?')"><input type="hidden" name="status" value="win"><button class="w-full bg-green-900/40 hover:bg-green-600 border border-green-500 text-green-500 hover:text-white py-4 rounded-xl text-xs font-black uppercase transition">WIN ✅</button></form>
                        <form action="/admin/settle-tip" method="POST" onsubmit="return confirm('Biztos VESZTES?')"><input type="hidden" name="status" value="loss">