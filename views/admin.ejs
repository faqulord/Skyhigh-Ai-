<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <title>Skyhigh | Admin Terminal</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>body { background-color: #000; color: white; }</style>
</head>
<body class="p-6 md:p-12 max-w-6xl mx-auto">
    <header class="flex flex-col md:flex-row justify-between items-center gap-6 mb-16">
        <div class="text-center md:text-left">
            <h1 class="text-4xl font-black italic tracking-tighter uppercase italic">Skyhigh <span class="text-red-600">Executive</span></h1>
            <p class="text-zinc-600 text-[10px] uppercase tracking-[4px]">Vállalati Pénzügyi Monitoring</p>
        </div>
        <a href="/dashboard" class="bg-zinc-900 px-6 py-3 rounded-xl text-xs font-bold uppercase tracking-widest">Terminál</a>
    </header>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12">
        <div class="bg-zinc-900/50 border border-zinc-800 p-8 rounded-3xl text-center">
            <p class="text-[10px] text-zinc-500 uppercase font-bold mb-2">Összes Operátor</p>
            <p class="text-4xl font-black"><%= stats.totalUsers %></p>
        </div>
        <div class="bg-zinc-900/50 border border-blue-500/20 p-8 rounded-3xl text-center shadow-[0_0_30px_rgba(59,130,246,0.1)]">
            <p class="text-[10px] text-zinc-500 uppercase font-bold mb-2 text-blue-500">Aktív Prémium</p>
            <p class="text-4xl font-black text-blue-500"><%= stats.activeSubs %></p>
        </div>
        <div class="bg-zinc-900/50 border border-green-500/20 p-8 rounded-3xl text-center shadow-[0_0_30px_rgba(34,197,94,0.1)]">
            <p class="text-[10px] text-zinc-500 uppercase font-bold mb-2 text-green-500">Havi Bevétel</p>
            <p class="text-4xl font-black text-green-500"><%= stats.revenue.toLocaleString() %> Ft</p>
        </div>
    </div>

    <div class="bg-zinc-900/30 border border-zinc-800 p-8 rounded-3xl mb-12">
        <h3 class="text-sm font-bold mb-4 italic uppercase">Társ-tulajdonos felhatalmazása</h3>
        <form action="/admin/make-admin" method="POST" class="flex flex-col md:flex-row gap-2">
            <input type="email" name="email" placeholder="Email cím" class="flex-1 bg-black border border-zinc-800 p-4 rounded-2xl outline-none focus:border-red-600">
            <button class="bg-white text-black px-10 py-4 rounded-2xl font-black uppercase text-xs">Admin Jog</button>
        </form>
    </div>

    <div class="space-y-4">
        <% users.forEach(u => { %>
        <div class="bg-zinc-900 p-6 rounded-2xl flex justify-between items-center border border-zinc-900 hover:border-zinc-700 transition">
            <div>
                <div class="flex items-center gap-2">
                    <p class="font-bold text-lg"><%= u.fullname %></p>
                    <% if(u.isAdmin) { %><span class="bg-red-600/10 text-red-600 border border-red-600/30 text-[8px] px-2 py-0.5 rounded font-black uppercase italic">Tulaj</span><% } %>
                </div>
                <p class="text-xs text-zinc-600 font-mono"><%= u.email %></p>
            </div>
            <div class="text-right">
                <p class="text-green-500 font-mono font-bold text-xl"><%= (u.startingCapital || 0).toLocaleString() %> Ft</p>
                <p class="text-[9px] uppercase font-bold <%= u.hasLicense ? 'text-blue-500' : 'text-zinc-600' %>">
                    <%= u.hasLicense ? 'Premium Client' : 'Demo User' %>
                </p>
            </div>
        </div>
        <% }) %>
    </div>
</body>
</html>