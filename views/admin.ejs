<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Owner HQ | <%= brandName %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@800&family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        body { background: #000000; color: #fff; font-family: 'Inter', sans-serif; }
        .tech-card { background: #0a0a0a; border: 1px solid #222; border-radius: 20px; margin-bottom: 20px; }
        .hidden { display: none !important; }
        #sidebar { position: fixed; top: 0; left: 0; width: 260px; height: 100vh; background: #09090b; border-right: 1px solid #222; z-index: 50; padding-top: 80px; transform: translateX(-100%); transition: 0.3s; }
        #sidebar.open { transform: translateX(0); }
        @media (min-width: 1024px) { #sidebar { transform: translateX(0); } }
        .menu-item { display: flex; align-items: center; gap: 15px; width: 100%; padding: 18px 25px; color: #666; font-size: 11px; font-weight: 800; text-transform: uppercase; border-left: 3px solid transparent; cursor: pointer; }
        .menu-item.active { color: #fff; background: #111; border-left-color: #c084fc; }
    </style>
</head>
<body class="min-h-screen">
    <nav id="sidebar">
        <div class="px-6 mb-6"><h2 class="text-white font-black text-xl tracking-widest uppercase">HQ CONTROL</h2><p class="text-[10px] text-zinc-500 font-bold uppercase">v75 Fixed</p></div>
        <div onclick="switchTab('dashboard')" id="btn-dashboard" class="menu-item active"><span>📊</span> VEZÉRLŐPULT</div>
        <div onclick="switchTab('users')" id="btn-users" class="menu-item"><span>👥</span> TAGOK</div>
        <div class="absolute bottom-10 left-0 w-full px-6"><a href="/logout" class="block w-full text-center border border-red-900/50 text-red-500 py-3 rounded-xl text-[10px] font-black uppercase">Kijelentkezés</a></div>
    </nav>

    <main class="pt-[40px] px-4 pb-10 w-full lg:pl-[280px] max-w-6xl mx-auto">
        <div id="tab-dashboard" class="page-section">
            <h2 class="text-xl font-black mb-6 text-white border-l-4 border-purple-500 pl-4">Aktuális és Függő meccsek</h2>
            <div class="tech-card p-6 border-t-2 border-t-orange-500">
                <div class="flex justify-between items-center mb-5 text-[11px] font-bold text-zinc-500 uppercase tracking-widest">
                    <span>Mód:</span> <span class="text-orange-500 text-sm"><%= strategyMode.toUpperCase() %></span>
                </div>
                <form action="/admin/update-settings" method="POST" class="grid grid-cols-3 gap-3">
                    <button name="mode" value="recovery" class="py-4 rounded-xl text-[10px] font-black border <%= strategyMode === 'recovery' ? 'bg-blue-600 text-white' : 'bg-zinc-900 text-zinc-500' %>">ÓVATOS</button>
                    <button name="mode" value="normal" class="py-4 rounded-xl text-[10px] font-black border <%= strategyMode === 'normal' ? 'bg-purple-600 text-white' : 'bg-zinc-900 text-zinc-500' %>">NORMÁL</button>
                    <button name="mode" value="aggressive" class="py-4 rounded-xl text-[10px] font-black border <%= strategyMode === 'aggressive' ? 'bg-orange-600 text-white' : 'bg-zinc-900 text-zinc-500' %>">ZSIVÁNY</button>
                </form>
            </div>

            <% if (pendingTips && pendingTips.length > 0) { %>
                <% pendingTips.forEach(tip => { %>
                    <div class="tech-card p-6 bg-zinc-900/10">
                        <p class="text-[10px] text-zinc-500 uppercase font-bold"><%= tip.date %> | <%= tip.league %></p>
                        <h3 class="text-xl font-black text-white"><%= tip.match %></h3>
                        <p class="text-purple-400 font-bold mb-4"><%= tip.prediction %> (@<%= tip.odds %>)</p>
                        <div class="grid grid-cols-2 gap-4">
                            <form action="/admin/settle-tip" method="POST"><input type="hidden" name="tipId" value="<%= tip._id %>"><input type="hidden" name="status" value="win"><button class="w-full bg-green-900/40 border border-green-500 text-green-500 py-4 rounded-xl font-black text-xs">WIN ✅</button></form>
                            <form action="/admin/settle-tip" method="POST"><input type="hidden" name="tipId" value="<%= tip._id %>"><input type="hidden" name="status" value="loss"><button class="w-full bg-red-900/40 border border-red-500 text-red-500 py-4 rounded-xl font-black text-xs">LOSS ❌</button></form>
                        </div>
                        <% if(!tip.isPublished) { %><form action="/admin/publish-tip" method="POST" class="mt-4"><input type="hidden" name="tipId" value="<%= tip._id %>"><button class="w-full bg-blue-600 text-white py-3 rounded-xl font-black text-xs">PUBLIKÁLÁS</button></form><% } %>
                    </div>
                <% }) %>
            <% } else { %>
                <form action="/admin/run-robot" method="POST" class="text-center py-10 tech-card"><button class="bg-white text-black px-10 py-4 rounded-xl font-black text-xs uppercase">🚀 Elemzés Indítása</button></form>
            <% } %>
        </div>

        <div id="tab-users" class="page-section hidden">
            <h2 class="text-xl font-black mb-6 text-white border-l-4 border-green-500 pl-4">Tagok</h2>
            <% users.forEach(u => { 
                let bank = (u.currentBankroll > 0) ? u.currentBankroll : (u.startingCapital || 0);
            %> 
                <div class="tech-card p-5 flex justify-between items-center">
                    <div><p class="text-sm font-bold text-white"><%= u.fullname %> <span class="text-[10px] text-zinc-500"><%= u.email %></span></p><p class="text-[10px] text-zinc-400">VAGYON: <%= bank.toLocaleString() %> Ft</p></div>
                    <div class="flex gap-2">
                        <form action="/admin/manage-sub" method="POST"><input type="hidden" name="userId" value="<%= u._id %>"><input type="hidden" name="action" value="add30"><button class="px-4 py-2 rounded-lg bg-green-900 text-green-400 text-[10px] font-black">+30 NAP</button></form>
                        <form action="/admin/manage-sub" method="POST"><input type="hidden" name="userId" value="<%= u._id %>"><input type="hidden" name="action" value="revoke"><button class="px-4 py-2 rounded-lg bg-red-900 text-red-400 text-[10px] font-black">TILTÁS</button></form>
                    </div>
                </div> 
            <% }) %>
        </div>
    </main>

    <script>
        function switchTab(id) { document.querySelectorAll('.page-section').forEach(e => e.classList.add('hidden')); document.querySelectorAll('.menu-item').forEach(b => b.classList.remove('active')); document.getElementById('tab-' + id).classList.remove('hidden'); document.getElementById('btn-' + id).classList.add('active'); }
    </script>
</body>
</html>