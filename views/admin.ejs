<div id="tab-dashboard" class="page-section">
    <h2 class="text-xl font-black mb-6 text-white border-l-4 border-purple-500 pl-4">Függőben lévő meccsek</h2>
    
    <% if (pendingTips && pendingTips.length > 0) { %>
        <% pendingTips.forEach(tip => { %>
            <div class="tech-card p-6 border-b-2 border-zinc-800 mb-4">
                <div class="flex justify-between items-center mb-2">
                    <span class="text-[10px] text-zinc-500 font-bold uppercase"><%= tip.date %> | <%= tip.league %></span>
                </div>
                <h3 class="text-lg font-black text-white"><%= tip.match %></h3>
                <p class="text-purple-400 font-bold"><%= tip.prediction %> (@<%= tip.odds %>)</p>

                <div class="grid grid-cols-2 gap-4 mt-6">
                    <form action="/admin/settle-tip" method="POST">
                        <input type="hidden" name="tipId" value="<%= tip._id %>">
                        <input type="hidden" name="status" value="win">
                        <button class="w-full bg-green-900/40 border border-green-500 text-green-500 py-3 rounded-xl font-black text-xs">WIN ✅</button>
                    </form>
                    <form action="/admin/settle-tip" method="POST">
                        <input type="hidden" name="tipId" value="<%= tip._id %>">
                        <input type="hidden" name="status" value="loss">
                        <button class="w-full bg-red-900/40 border border-red-500 text-red-500 py-3 rounded-xl font-black text-xs">LOSS ❌</button>
                    </form>
                </div>
                
                <% if(!tip.isPublished) { %>
                    <form action="/admin/publish-tip" method="POST" class="mt-4">
                        <input type="hidden" name="tipId" value="<%= tip._id %>">
                        <button class="w-full bg-blue-600 text-white py-2 rounded-lg font-bold text-[10px] uppercase">Publikálás a Tagoknak</button>
                    </form>
                <% } %>
            </div>
        <% }) %>
    <% } else { %>
        <div class="text-center py-10">
            <h3 class="text-zinc-500 font-bold uppercase text-xs">Nincs lezáratlan meccs.</h3>
            <form action="/admin/run-robot" method="POST" class="mt-4">
                <button class="bg-white text-black px-8 py-3 rounded-xl font-black text-xs">🚀 ÚJ TIPP GENERÁLÁSA</button>
            </form>
        </div>
    <% } %>
</div>