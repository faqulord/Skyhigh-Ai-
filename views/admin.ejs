<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Skyhigh | Biztonságos Admin</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black text-white font-sans p-4">
    <div class="max-w-md mx-auto">
        <div class="flex justify-between items-center mb-8">
            <h1 class="text-xl font-black italic text-purple-500 uppercase">Master Panel</h1>
            <a href="/dashboard" class="text-[10px] bg-zinc-800 px-3 py-1 rounded-full uppercase">Vissza</a>
        </div>

        <div class="bg-gradient-to-br from-purple-900/40 to-black p-6 rounded-3xl border border-purple-500/30 mb-6 shadow-xl shadow-purple-500/10">
            <p class="text-[10px] uppercase text-zinc-400 font-bold mb-1">Havi Bevétel</p>
            <h2 class="text-4xl font-black text-green-400 italic">€<%= totalRevenue %></h2>
            <p class="text-[10px] text-zinc-500 mt-2 uppercase font-bold"><%= licensedCount %> aktív előfizető</p>
        </div>

        <div class="bg-zinc-900 p-6 rounded-3xl border border-white/5 mb-6">
            <h3 class="text-xs font-black uppercase mb-4 text-center">Neural Bot Trigger</h3>
            <form action="/admin/run-robot" method="POST">
                <button type="submit" class="w-full bg-purple-600 hover:bg-purple-700 py-4 rounded-2xl font-black uppercase text-xs transition active:scale-95 shadow-lg shadow-purple-600/20">
                    Mai Elemzés Indítása Most
                </button>
            </form>
            <% if (status === 'success') { %>
                <p class="text-center text-purple-400 text-[10px] mt-3 font-bold animate-pulse uppercase tracking-widest">✅ Robot sikeresen frissítve!</p>
            <% } %>
            <% if (status === 'error') { %>
                <p class="text-center text-red-500 text-[10px] mt-3 font-bold uppercase">❌ Hiba! Ellenőrizd az API kulcsokat!</p>
            <% } %>
        </div>

        <div class="bg-zinc-900 p-6 rounded-3xl border border-white/5">
            <h3 class="text-xs font-black uppercase text-purple-500 mb-4">Ügyfelek & Tőke</h3>
            <div class="space-y-4">
                <% users.forEach(u => { %>
                    <div class="flex justify-between items-center border-b border-white/5 pb-2">
                        <div>
                            <p class="text-sm font-bold"><%= u.fullname %></p>
                            <p class="text-[9px] text-zinc-500"><%= u.email %></p>
                        </div>
                        <div class="text-right">
                            <p class="text-sm font-mono text-purple-400"><%= u.startingCapital %> €</p>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>