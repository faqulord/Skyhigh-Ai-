<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <title>Skyhigh Admin | Control</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black text-white p-10 font-sans">
    <div class="max-w-6xl mx-auto">
        <h1 class="text-4xl font-black italic mb-10 tracking-tighter text-purple-600">Admin_Terminal v1.0</h1>
        
        <div class="grid gap-4">
            <h2 class="text-xs font-bold text-zinc-500 uppercase tracking-widest mb-2">Felhasználók Kezelése</h2>
            <% users.forEach(u => { %>
            <div class="bg-zinc-900 border border-zinc-800 p-6 rounded-2xl flex justify-between items-center">
                <div>
                    <h3 class="text-lg font-bold"><%= u.fullname %></h3>
                    <p class="text-xs text-zinc-500"><%= u.email %></p>
                </div>
                <div class="text-right">
                    <p class="text-[10px] text-zinc-500 uppercase">Bankroll</p>
                    <p class="text-green-500 font-mono font-bold"><%= u.startingCapital %> Ft</p>
                    <span class="text-[9px] px-2 py-1 bg-zinc-800 rounded font-black uppercase <%= u.hasLicense ? 'text-blue-500' : 'text-zinc-600' %>">
                        <%= u.hasLicense ? 'Premium' : 'Demo' %>
                    </span>
                </div>
            </div>
            <% }) %>
        </div>
    </div>
</body>
</html>