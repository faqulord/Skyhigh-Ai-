<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Skyhigh AI Analyst</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        body { background: #000; color: #fff; font-family: 'Inter', sans-serif; }
        .message-bubble { background: #0a0a0a; border: 1px solid #1f1f1f; border-radius: 24px; }
        .highlight-box { background: rgba(139, 92, 246, 0.1); border-left: 4px solid #8b5cf6; }
    </style>
</head>
<body class="p-4 md:p-8 flex justify-center min-h-screen">
    <div class="max-w-md w-full">
        <div class="flex justify-between items-center mb-10">
            <h1 class="text-xl font-black italic text-purple-500 uppercase tracking-tighter">Skyhigh AI</h1>
            <% if (user.isAdmin) { %><a href="/admin" class="text-[10px] bg-zinc-900 px-3 py-1 rounded-full uppercase font-bold text-zinc-500">Admin</a><% } %>
        </div>

        <div class="flex items-center gap-4 mb-6">
            <div class="w-12 h-12 bg-purple-600 rounded-full flex items-center justify-center shadow-lg shadow-purple-600/20 text-xl">🧠</div>
            <div>
                <h2 class="font-bold text-sm uppercase tracking-widest">Neural Analyst v10.0</h2>
                <p class="text-[9px] text-green-500 font-bold uppercase animate-pulse">Online és Elemzésre kész</p>
            </div>
        </div>

        <div class="message-bubble p-6 shadow-2xl relative">
            <p class="text-zinc-500 text-[10px] mb-4 uppercase font-bold tracking-widest border-b border-white/5 pb-2">Üzenet érkezett: <%= new Date().toLocaleTimeString('hu-HU', {hour:'2-digit', minute:'2-digit'}) %></p>
            
            <% if (dailyTip) { %>
                <p class="text-zinc-300 text-sm leading-relaxed mb-6 italic">"A mai piaci adatok és az utolsó 10 év statisztikai korrelációi alapján a következő Master Tippet állítottam össze számodra:"</p>

                <div class="highlight-box p-5 mb-8">
                    <p class="text-[9px] text-purple-400 font-black uppercase mb-2">Mérkőzés és Tipp</p>
                    <h3 class="text-2xl font-black uppercase mb-1 leading-tight"><%= dailyTip.match %></h3>
                    <p class="text-xl font-black text-white italic underline decoration-purple-500 decoration-4 underline-offset-4"><%= dailyTip.prediction %></p>
                </div>

                <div class="grid grid-cols-2 gap-4 mb-8">
                    <div class="bg-black border border-white/5 p-4 rounded-2xl">
                        <p class="text-[8px] text-zinc-500 uppercase font-black mb-1 italic tracking-widest">Javasolt Tét (Bankroll 10%)</p>
                        <p class="text-2xl font-black text-green-500"><%= recommendedStake.toLocaleString() %> Ft</p>
                    </div>
                    <div class="bg-black border border-white/5 p-4 rounded-2xl">
                        <p class="text-[8px] text-zinc-500 uppercase font-black mb-1 italic tracking-widest">Szorzó (Odds)</p>
                        <p class="text-2xl font-black"><%= dailyTip.odds %></p>
                    </div>
                </div>

                <div class="flex justify-between text-[10px] text-zinc-500 mb-8 border-t border-white/5 pt-4 uppercase font-bold">
                    <span>Kezdés: <b class="text-white"><%= dailyTip.matchTime %></b></span>
                    <span>Iroda: <b class="text-white"><%= dailyTip.bookmaker %></b></span>
                </div>

                <div class="bg-zinc-900/50 p-5 rounded-2xl">
                    <h4 class="text-[10px] font-black text-purple-500 uppercase mb-3 tracking-widest">Matematikai Érvelés</h4>
                    <p class="text-[11px] text-zinc-400 leading-relaxed"><%= dailyTip.reasoning %></p>
                </div>

            <% } else { %>
                <div class="py-10 text-center">
                    <p class="text-zinc-600 text-[10px] font-black uppercase tracking-widest animate-pulse">A neurális hálózat jelenleg a Poisson-eloszlást számítja...</p>
                </div>
            <% } %>
        </div>

        <div class="mt-8 flex justify-between items-center px-4">
            <p class="text-[10px] text-zinc-600 uppercase font-bold tracking-widest">Aktuális Tőke: <b><%= user.startingCapital.toLocaleString() %> Ft</b></p>
            <a href="/logout" class="text-[10px] text-red-500 uppercase font-black tracking-widest">Kijelentkezés</a>
        </div>
    </div>
</body>
</html>