<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Skyhigh Admin | Live Preview</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-[#050505] text-white p-4 font-sans">
    <div class="max-w-md mx-auto">
        <header class="flex justify-between items-center mb-8 pt-4">
            <h1 class="text-xl font-black italic text-purple-500 uppercase tracking-tighter">Admin Control</h1>
            <a href="/dashboard" class="text-[10px] bg-zinc-800 px-3 py-1 rounded-full uppercase font-bold">Dashboard</a>
        </header>

        <div class="bg-zinc-900 p-6 rounded-3xl border border-purple-500/30 mb-8 text-center shadow-xl">
            <h3 class="text-[10px] font-black uppercase mb-4 text-zinc-400 tracking-widest">Reggel 8:00 Szimuláció</h3>
            <form action="/admin/run-robot" method="POST" onsubmit="this.querySelector('button').disabled=true; this.querySelector('button').innerText='Neural Elemzés... (60mp)';">
                <button type="submit" class="w-full bg-purple-600 py-4 rounded-2xl font-black uppercase text-xs shadow-lg shadow-purple-600/20 transition active:scale-95">Elemzés Futtatása Most</button>
            </form>
            <% if (status === 'success') { %><p class="text-purple-400 text-[10px] mt-4 font-black animate-pulse uppercase">✅ Elemzés Sikerült!</p><% } %>
        </div>

        <div class="mb-10">
            <h3 class="text-[10px] font-black uppercase text-zinc-500 mb-4 tracking-widest text-center italic">Aktuális Felhasználói Előnézet</h3>
            <% if (currentTip) { %>
                <div class="bg-[#111] border-l-4 border-purple-600 p-5 rounded-r-2xl shadow-xl">
                    <p class="text-[8px] text-zinc-500 mb-2 uppercase font-bold"><%= currentTip.matchTime %> | <%= currentTip.bookmaker %></p>
                    <p class="text-sm font-black uppercase mb-1"><%= currentTip.match %></p>
                    <p class="text-purple-500 text-xs font-bold italic mb-4"><%= currentTip.prediction %></p>
                    <div class="flex justify-between text-[10px] font-mono border-t border-white/5 pt-3">
                        <span>ODDS: <b class="text-white"><%= currentTip.odds %></b></span>
                        <span class="text-green-500">PROFIT: <b>+<%= currentTip.profitPercent %>%</b></span>
                    </div>
                </div>
            <% } else { %>
                <div class="bg-zinc-900/50 p-8 rounded-2xl border border-white/5 text-center italic text-zinc-600 text-xs">Nincs aktív tipp az adatbázisban.</div>
            <% } %>
        </div>

        <div class="bg-zinc-900 p-6 rounded-3xl border border-white/5">
            <h3 class="text-[10px] font-black uppercase text-purple-500 mb-4 tracking-widest text-center italic">Ügyfelek Adatbázisa</h3>
            <div class="space-y-4">
                <% users.forEach(u => { %>
                    <div class="flex justify-between items-center border-b border-white/5 pb-2">
                        <span class="text-[11px] font-bold truncate w-32"><%= u.fullname %></span>
                        <span class="text-[11px] text-purple-400 font-mono"><%= u.startingCapital.toLocaleString() %> Ft</span>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>