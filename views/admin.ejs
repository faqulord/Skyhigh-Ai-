<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Skyhigh Admin HQ</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black text-white p-4 font-sans">
    <div class="max-w-md mx-auto">
        <h1 class="text-xl font-black italic text-purple-500 uppercase mb-6">Master HQ</h1>

        <% if (error) { %>
            <div class="bg-red-600/20 border border-red-500 text-red-500 p-4 rounded-xl text-xs mb-6 font-bold">
                ⚠️ HIBA: <%= error %>
            </div>
        <% } %>

        <% if (success) { %>
            <div class="bg-green-600/20 border border-green-500 text-green-500 p-4 rounded-xl text-xs mb-6 font-bold">
                ✅ ROBOT SIKERESEN LEFUTOTT!
            </div>
        <% } %>

        <div class="bg-zinc-900 p-6 rounded-3xl border border-white/5 mb-6 text-center">
            <p class="text-[10px] text-zinc-500 uppercase mb-4 tracking-widest">Neural Command</p>
            <form action="/admin/run-robot" method="POST">
                <button type="submit" class="w-full bg-purple-600 py-4 rounded-2xl font-black uppercase text-xs active:scale-95 transition">
                    Elemzés Indítása Most
                </button>
            </form>
        </div>

        <div class="grid grid-cols-2 gap-4 mb-6">
            <div class="bg-zinc-900 p-4 rounded-2xl border border-white/5">
                <p class="text-[9px] text-zinc-500 uppercase">Bevétel</p>
                <p class="text-xl font-black text-green-500">€<%= totalRevenue %></p>
            </div>
            <div class="bg-zinc-900 p-4 rounded-2xl border border-white/5">
                <p class="text-[9px] text-zinc-500 uppercase">Aktív Tag</p>
                <p class="text-xl font-black"><%= licensedCount %></p>
            </div>
        </div>

        <div class="bg-zinc-900 p-6 rounded-3xl border border-white/5">
            <h3 class="text-xs font-black text-purple-500 uppercase mb-4 tracking-tighter">Client Base</h3>
            <% users.forEach(u => { %>
                <div class="flex justify-between items-center border-b border-white/5 py-2">
                    <span class="text-xs font-bold"><%= u.fullname %></span>
                    <span class="text-xs text-purple-400 font-mono"><%= u.startingCapital %> €</span>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>