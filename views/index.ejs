<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Skyhigh AI | Személyi Bookmaker</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;900&display=swap');
        body { font-family: 'Inter', sans-serif; background-color: #000; color: #fff; -webkit-font-smoothing: antialiased; }
        .bento-card { background: #0a0a0a; border: 1px solid #1a1a1a; border-radius: 24px; }
        
        /* Aktív gombok stílusa */
        .btn-active { background: #22c55e !important; color: #000 !important; border-color: #22c55e !important; }
        .btn-inactive { background: transparent; border: 1px solid #222; color: #666; }
        
        .term-active { border: 2px solid #a855f7 !important; background: rgba(168, 85, 247, 0.1); color: #fff !important; }
        
        .green-glow { color: #22c55e; text-shadow: 0 0 20px rgba(34, 197, 94, 0.4); }
        .purple-glow { color: #a855f7; filter: drop-shadow(0 0 8px rgba(168, 85, 247, 0.6)); }
        
        input[type="range"] { accent-color: #22c55e; }
        .buy-button { background: linear-gradient(90deg, #22c55e, #16a34a); color: #000; font-weight: 900; }
    </style>
</head>
<body class="px-4 pb-10">

    <nav class="py-6 flex justify-between items-center border-b border-white/5 mb-8">
        <div class="flex items-center gap-2">
            <i class="fas fa-robot purple-glow"></i>
            <span class="font-black tracking-tighter text-lg uppercase">Skyhigh<span class="text-green-500">.Ai</span></span>
        </div>
        <div class="text-[8px] font-bold text-zinc-500 uppercase tracking-widest">v2.0 Beta</div>
    </nav>

    <header class="text-center mb-10">
        <div class="inline-block px-3 py-1 rounded-full border border-purple-500/20 bg-purple-500/5 text-[9px] font-bold text-purple-400 uppercase tracking-widest mb-4">
            Személyi AI Asszisztens
        </div>
        <h1 class="text-4xl font-black uppercase tracking-tighter leading-none mb-6">
            A jövő <br> <span class="green-glow italic text-5xl">pénzgyára.</span>
        </h1>
        <p class="text-zinc-500 text-xs leading-relaxed px-4">
            A Skyhigh AI több mint 1 másodperc alatt elemzi a világ összes adatát, hogy matematikai precizitással építse a vagyonod.
        </p>
    </header>

    <section class="mb-10 space-y-3">
        <div class="bento-card p-5 flex items-center gap-4">
            <i class="fas fa-microchip text-purple-500 text-lg"></i>
            <p class="text-[10px] text-zinc-400 font-medium leading-tight">0-24 órás globális piacfigyelés és matematikai szelekció.</p>
        </div>
        <div class="bento-card p-5 flex items-center gap-4">
            <i class="fas fa-shield-alt text-green-500 text-lg"></i>
            <p class="text-[10px] text-zinc-400 font-medium leading-tight">Dinamikus tőkevédelem: a szoftver vigyáz a pénzedre.</p>
        </div>
    </section>

    <section class="bento-card p-6 mb-10">
        <h3 class="text-[10px] font-black text-zinc-600 uppercase tracking-widest mb-6 text-center italic">Tőkekezelő Konzol</h3>
        
        <div class="space-y-8">
            <div>
                <label class="text-[9px] text-zinc-500 uppercase font-bold block mb-3">Válaszd ki az induló tőkéd:</label>
                <div class="grid grid-cols-3 gap-2">
                    <button onclick="setCap(50000)" id="cap50" class="btn-inactive py-3 rounded-xl text-[10px] font-black transition-all">50K</button>
                    <button onclick="setCap(100000)" id="cap100" class="btn-active py-3 rounded-xl text-[10px] font-black transition-all">100K</button>
                    <button onclick="setCap(250000)" id="cap250" class="btn-inactive py-3 rounded-xl text-[10px] font-black transition-all">250K</button>
                </div>
            </div>

            <div class="space-y-4">
                <label class="text-[9px] text-zinc-500 uppercase font-bold block mb-1">Válassz stratégiát:</label>
                <div class="grid grid-cols-2 gap-3">
                    <button onclick="setTerm(6)" id="term6" class="term-active py-4 rounded-2xl text-[10px] font-black uppercase tracking-widest transition-all">6 Hónap</button>
                    <button onclick="setTerm(12)" id="term12" class="btn-inactive py-4 rounded-2xl text-[10px] font-black uppercase tracking-widest transition-all">12 Hónap</button>
                </div>
                
                <div class="pt-4">
                    <button class="buy-button w-full py-5 rounded-2xl text-xs uppercase tracking-[0.2em] shadow-lg shadow-green-500/20 active:scale-95 transition">
                        Licenc Aktiválása (20.000 Ft/hó)
                    </button>
                    <p class="text-center text-[8px] text-zinc-600 mt-3 uppercase tracking-widest">Biztonságos Stripe fizetés • Azonnali hozzáférés</p>
                </div>
            </div>

            <div class="pt-6 border-t border-white/5 text-center">
                <div class="flex justify-between items-center px-2 mb-4">
                    <span class="text-[9px] text-zinc-500 uppercase font-bold italic">30 napos AI cél (+30%)</span>
                    <span id="haviProfit" class="text-sm font-bold text-green-500">~130.000 Ft</span>
                </div>
                <div class="bg-zinc-900/50 py-6 rounded-2xl">
                    <span class="text-[9px] text-purple-400 uppercase font-black tracking-widest block mb-1">Várható Vagyon a ciklus végén:</span>
                    <div id="eredmeny" class="text-4xl font-black tracking-tighter italic">450.000 <span class="text-green-500 text-lg">Ft</span></div>
                    <p class="text-[7px] text-zinc-700 mt-2 uppercase px-4 italic">*Változó oddsok miatt az összeg eltérhet, de az AI a maximumot hozza ki belőle.</p>
                </div>
            </div>
        </div>
    </section>

    <section class="px-4 mb-20">
        <h4 class="text-[10px] font-black text-center text-zinc-600 uppercase tracking-widest mb-6">A csomag tartalma:</h4>
        <div class="grid grid-cols-2 gap-4">
            <div class="text-center">
                <i class="fas fa-robot text-purple-500 mb-2"></i>
                <p class="text-[8px] text-zinc-400 font-bold uppercase tracking-tight">AI Asszisztens</p>
            </div>
            <div class="text-center">
                <i class="fas fa-chart-pie text-green-500 mb-2"></i>
                <p class="text-[8px] text-zinc-400 font-bold uppercase tracking-tight">Tőkemenedzsment</p>
            </div>
            <div class="text-center">
                <i class="fas fa-save text-blue-500 mb-2"></i>
                <p class="text-[8px] text-zinc-400 font-bold uppercase tracking-tight">Folyamat Mentés</p>
            </div>
            <div class="text-center">
                <i class="fas fa-bolt text-yellow-500 mb-2"></i>
                <p class="text-[8px] text-zinc-400 font-bold uppercase tracking-tight">Master Tippek</p>
            </div>
        </div>
    </section>

    <footer class="text-center opacity-30 pt-10 border-t border-white/5">
        <p class="text-[8px] font-black tracking-[0.6em] uppercase mb-4 text-green-500">SKYHIGH AI • Architect: faqubelord</p>
        <p class="text-[7px] text-zinc-700 uppercase mb-4 px-10">A szoftver matematikai alapokon dolgozik. Minden jog fenntartva.</p>
    </footer>

    <script>
        let currentCap = 100000;
        let currentTerm = 6;

        function setCap(amount) {
            currentCap = amount;
            const ids = [50000, 100000, 250000];
            ids.forEach(id => {
                const el = document.getElementById('cap' + id);
                el.className = id === amount ? 'btn-active py-3 rounded-xl text-[10px] font-black transition-all' : 'btn-inactive py-3 rounded-xl text-[10px] font-black transition-all';
            });
            updateCalc();
        }

        function setTerm(months) {
            currentTerm = months;
            document.getElementById('term6').className = months === 6 ? 'term-active py-4 rounded-2xl text-[10px] font-black uppercase tracking-widest transition-all' : 'btn-inactive py-4 rounded-2xl text-[10px] font-black uppercase tracking-widest transition-all';
            document.getElementById('term12').className = months === 12 ? 'term-active py-4 rounded-2xl text-[10px] font-black uppercase tracking-widest transition-all' : 'btn-inactive py-4 rounded-2xl text-[10px] font-black uppercase tracking-widest transition-all';
            updateCalc();
        }

        function updateCalc() {
            const havi = document.getElementById('haviProfit');
            const veg = document.getElementById('eredmeny');
            
            // Havi profit kalkuláció (kb 30%)
            const monthlyProfit = Math.floor(currentCap * 1.3);
            havi.innerText = '~' + monthlyProfit.toLocaleString() + ' Ft';
            
            // Ciklus végi (6 hó: 4.5x, 12 hó: 14x)
            const multiplier = currentTerm === 6 ? 4.5 : 14;
            const finalValue = Math.floor(currentCap * multiplier);
            veg.innerHTML = finalValue.toLocaleString() + ' <span class="text-green-500 text-lg">Ft</span>';
        }

        updateCalc();
    </script>
</body>
</html>