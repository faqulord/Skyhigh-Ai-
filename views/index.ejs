<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Skyhigh AI | Quantum Capital Management</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;700;900&display=swap');
        body { font-family: 'Inter', sans-serif; background-color: #000; color: #fff; scroll-behavior: smooth; }
        
        .bento-card { background: #080808; border: 1px solid #141414; border-radius: 28px; }
        
        /* Időtartam Választó - Lila Kiemelés */
        .term-active { background: #a855f7; color: #fff; border-color: #a855f7; box-shadow: 0 0 20px rgba(168, 85, 247, 0.4); }
        .term-inactive { background: transparent; border: 1px solid #1a1a1a; color: #555; }
        
        /* Tőkeválasztó Chips */
        .chip-active { background: #22c55e; color: #000; border-color: #22c55e; }
        .chip-inactive { background: transparent; border: 1px solid #1a1a1a; color: #555; }
        
        .purple-glow { filter: drop-shadow(0 0 15px rgba(168, 85, 247, 0.6)); }
        .green-glow { text-shadow: 0 0 20px rgba(34, 197, 94, 0.5); }
        
        .cta-button { background: linear-gradient(90deg, #a855f7, #7c3aed); color: #fff; font-weight: 900; }
        .tech-tag { background: rgba(168, 85, 247, 0.1); border: 1px solid rgba(168, 85, 247, 0.2); color: #a855f7; }
    </style>
</head>
<body class="selection:bg-purple-500 selection:text-white">

    <nav class="fixed top-0 w-full z-50 bg-black/60 backdrop-blur-xl border-b border-white/5 px-8 py-5 flex justify-between items-center">
        <div class="flex items-center gap-3">
            <i class="fas fa-robot text-purple-500 text-2xl purple-glow"></i>
            <span class="font-black tracking-tighter text-2xl uppercase italic">SKYHIGH<span class="text-green-500">.AI</span></span>
        </div>
        <button class="text-[9px] font-black tracking-[0.2em] bg-white text-black px-5 py-2.5 rounded-full uppercase">Login</button>
    </nav>

    <header class="pt-32 pb-12 px-8 max-w-4xl mx-auto text-center">
        <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full tech-tag mb-8">
            <i class="fas fa-bolt text-[10px]"></i>
            <span class="text-[9px] font-black tracking-widest uppercase text-purple-400">Adatfeldolgozás < 1.0s</span>
        </div>
        <h1 class="text-5xl md:text-7xl font-black tracking-tighter mb-8 leading-[0.9] uppercase">
            Csúcstechnológia. <br><span class="text-purple-500 italic">Matematikai profit.</span>
        </h1>
        <p class="text-zinc-500 text-sm md:text-lg leading-relaxed mb-10">
            A Skyhigh AI nem tippel. A szoftver több mint <span class="text-white">1 másodperc alatt</span> elemzi a globális odds-okat és statisztikákat, hogy matematikai precizitással számolja ki az utat a tőkéid gyarapításához.
        </p>
    </header>

    <section class="px-6 py-8 max-w-5xl mx-auto">
        <div class="bento-card p-8 bg-[#050505] border-zinc-800">
            <div class="space-y-10">
                <div>
                    <span class="text-[10px] font-bold text-zinc-600 uppercase tracking-[0.3em] mb-4 block">Válaszd ki az induló tőkéd</span>
                    <div class="grid grid-cols-2 md:grid-cols-4 gap-3">
                        <button onclick="setCapital(100000)" id="cap100" class="chip-active py-4 rounded-2xl text-xs font-black transition-all">100.000 Ft</button>
                        <button onclick="setCapital(250000)" id="cap250" class="chip-inactive py-4 rounded-2xl text-xs font-black transition-all">250.000 Ft</button>
                        <button onclick="setCapital(500000)" id="cap500" class="chip-inactive py-4 rounded-2xl text-xs font-black transition-all">500.000 Ft</button>
                        <button onclick="setCapital(1000000)" id="cap1m" class="chip-inactive py-4 rounded-2xl text-xs font-black transition-all">1.000.000 Ft</button>
                    </div>
                </div>

                <div>
                    <span class="text-[10px] font-bold text-zinc-600 uppercase tracking-[0.3em] mb-4 block">Vagyonépítési ciklus (Lila Kiemelés)</span>
                    <div class="grid grid-cols-2 gap-4">
                        <button onclick="setTerm(6)" id="term6" class="term-active py-5 rounded-2xl text-xs font-black uppercase tracking-widest transition-all">6 Hónapos Terv</button>
                        <button onclick="setTerm(12)" id="term12" class="term-inactive py-5 rounded-2xl text-xs font-black uppercase tracking-widest transition-all">12 Hónapos Terv</button>
                    </div>
                </div>

                <div class="pt-10 border-t border-zinc-900 grid md:grid-cols-2 gap-8 items-center">
                    <div>
                        <div class="text-[10px] text-zinc-500 uppercase font-bold tracking-widest mb-2">30 napos AI cél (+25-30%)</div>
                        <div id="haviProfit" class="text-3xl font-bold text-green-500 italic tracking-tighter">~130.000 Ft</div>
                    </div>
                    <div class="text-right">
                        <div class="text-[10px] text-purple-400 uppercase font-black tracking-widest mb-2">Várható Vagyonérték a ciklus végén</div>
                        <div id="eredmeny" class="text-5xl md:text-6xl font-black text-white tracking-tighter italic leading-none">450.000 <span class="text-purple-500 text-xl">Ft</span></div>
                    </div>
                </div>
                <p class="text-[9px] text-center text-zinc-700 uppercase tracking-widest italic">
                    *Tájékoztató jellegű adat. A végső összeg a változó odds-ok és piaci mozgások miatt eltérhet.
                </p>
            </div>
        </div>
    </section>

    <section class="px-6 py-12 max-w-6xl mx-auto grid md:grid-cols-2 gap-6">
        <div class="bento-card p-10">
            <i class="fas fa-microchip text-purple-500 text-2xl mb-6"></i>
            <h4 class="text-xl font-black uppercase tracking-tighter mb-4">Ultra-Gyors Analízis</h4>
            <p class="text-zinc-500 text-sm leading-relaxed">Míg egy ember percekig elemez egy meccset, a Skyhigh AI <span class="text-white font-bold">kevesebb mint 1 másodperc</span> alatt veti össze a világ összes fogadóirodájának kínálatát a valós matematikai valószínűséggel.</p>
        </div>
        <div class="bento-card p-10">
            <i class="fas fa-calculator text-green-500 text-2xl mb-6"></i>
            <h4 class="text-xl font-black uppercase tracking-tighter mb-4">Matematikai Pajzs</h4>
            <p class="text-zinc-500 text-sm leading-relaxed">A szoftver dinamikus algoritmusokat használ. Minden hónapban fix profitra optimalizál, és azonnal korrigál, ha a piaci odds-ok elmozdulnak. Ez nem szerencse, ez tiszta számítás.</p>
        </div>
    </section>

    <section id="licenc" class="px-6 py-20 pb-32 max-w-3xl mx-auto">
        <div class="bento-card p-12 relative overflow-hidden text-center border-purple-500/30 bg-gradient-to-b from-[#0a0a0a] to-black">
            <div class="mb-8">
                <i class="fas fa-robot text-purple-500 text-5xl purple-glow"></i>
            </div>
            <h2 class="font-black text-4xl mb-2 uppercase italic tracking-tighter">Skyhigh AI Licenc</h2>
            <p class="text-[10px] text-zinc-500 uppercase tracking-[0.4em] mb-12">Teljes technológiai hozzáférés</p>
            
            <div class="bg-zinc-900/30 p-8 rounded-[2rem] mb-12 text-left space-y-4">
                <h4 class="text-[10px] font-black text-purple-500 uppercase tracking-widest mb-4">Amit a csomaggal kapsz:</h4>
                <ul class="space-y-4">
                    <li class="flex items-center gap-4 text-xs font-medium text-zinc-300">
                        <i class="fas fa-check text-green-500"></i> Skyhigh Dashboard: Személyi Bookmaker Asszisztens
                    </li>
                    <li class="flex items-center gap-4 text-xs font-medium text-zinc-300">
                        <i class="fas fa-check text-green-500"></i> Napi 1 Master AI Szelekció (A legfrissebb adatokból)
                    </li>
                    <li class="flex items-center gap-4 text-xs font-medium text-zinc-300">
                        <i class="fas fa-check text-green-500"></i> Dinamikus Kamatos-Kamat Algoritmus
                    </li>
                    <li class="flex items-center gap-4 text-xs font-medium text-zinc-300">
                        <i class="fas fa-check text-green-500"></i> Progress Save: Mentett tőkeépítési folyamat
                    </li>
                </ul>
            </div>

            <div class="text-6xl font-black mb-12 tracking-tighter">20.000<span class="text-2xl text-zinc-700 ml-2">Ft/hó</span></div>
            
            <button class="cta-button w-full py-6 rounded-3xl text-sm uppercase tracking-[0.3em] shadow-[0_0_40px_rgba(168,85,247,0.3)] transition-all active:scale-95">
                Licenc Aktiválása
            </button>
        </div>
    </section>

    <div class="text-center pb-12 opacity-20">
        <p class="text-[8px] font-bold tracking-[0.8em] uppercase">Skyhigh AI • Architect: faqubelord</p>
    </div>

    <script>
        let currentCapital = 100000;
        let currentTerm = 6;

        const eredmeny = document.getElementById('eredmeny');
        const haviProfit = document.getElementById('haviProfit');

        function setCapital(amount) {
            currentCapital = amount;
            const ids = ['cap100', 'cap250', 'cap500', 'cap1m'];
            const targetId = amount === 100000 ? 'cap100' : amount === 250000 ? 'cap250' : amount === 500000 ? 'cap500' : 'cap1m';
            ids.forEach(id => {
                const el = document.getElementById(id);
                if(id === targetId) { el.classList.add('chip-active'); el.classList.remove('chip-inactive'); }
                else { el.classList.remove('chip-active'); el.classList.add('chip-inactive'); }
            });
            updateCalc();
        }

        function setTerm(months) {
            currentTerm = months;
            document.getElementById('term6').className = months === 6 ? 'term-active py-5 rounded-2xl text-xs font-black uppercase tracking-widest transition-all' : 'term-inactive py-5 rounded-2xl text-xs font-black uppercase tracking-widest transition-all';
            document.getElementById('term12').className = months === 12 ? 'term-active py-5 rounded-2xl text-xs font-black uppercase tracking-widest transition-all' : 'term-inactive py-5 rounded-2xl text-xs font-black uppercase tracking-widest transition-all';
            updateCalc();
        }

        function updateCalc() {
            // Havi profit kb 30%
            const monthlyGrowth = Math.floor(currentCapital * 1.3);
            haviProfit.innerHTML = `~${monthlyGrowth.toLocaleString()} Ft`;

            // Projekció szorzók
            const multiplier = currentTerm === 6 ? 4.5 : 14;
            const final = Math.floor(currentCapital * multiplier);
            eredmeny.innerHTML = `${final.toLocaleString()} <span class="text-purple-500 text-2xl">Ft</span>`;
        }

        updateCalc();
    </script>
</body>
</html>