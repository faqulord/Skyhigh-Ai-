<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Skyhigh AI | Személyi Bookmaker Szoftver</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@900&family=Inter:wght@400;700;800&display=swap');
        body { font-family: 'Inter', sans-serif; background-color: #050505; color: #fff; }
        .font-robot { font-family: 'Orbitron', sans-serif; }
        .green-glow { text-shadow: 0 0 15px rgba(34, 197, 94, 0.6); }
        .purple-robot { color: #a855f7; filter: drop-shadow(0 0 10px rgba(168, 85, 247, 0.8)); }
        .glass-dark { background: rgba(10, 10, 10, 0.8); backdrop-filter: blur(10px); border: 1px solid rgba(34, 197, 94, 0.1); }
        input[type="range"] { accent-color: #22c55e; }
    </style>
</head>
<body class="overflow-x-hidden">

    <div class="fixed bottom-10 -left-6 text-[8px] text-green-500/30 uppercase tracking-[0.5em] z-50 transform -rotate-90 font-bold">
        Dev by faqu
    </div>

    <nav class="sticky top-0 z-40 glass-dark px-6 py-4 flex justify-between items-center border-b border-green-500/20">
        <div class="flex items-center gap-2">
            <i class="fas fa-robot purple-robot text-2xl"></i>
            <span class="font-robot text-lg tracking-tighter text-white">SKYHIGH<span class="text-green-500">.AI</span></span>
        </div>
        <button class="bg-green-600 text-[10px] font-black px-5 py-2 rounded-full tracking-widest uppercase text-black hover:bg-green-400 transition">Belépés</button>
    </nav>

    <header class="pt-12 pb-10 px-6 text-center">
        <div class="mb-6 relative inline-block">
            <div class="w-32 h-32 bg-black border-4 border-purple-500 rounded-full flex items-center justify-center mx-auto shadow-[0_0_30px_rgba(168, 85, 247, 0.4)]">
                <i class="fas fa-robot text-6xl purple-robot"></i>
            </div>
            <div class="absolute -bottom-2 -right-2 bg-green-500 text-black text-[8px] font-black px-2 py-1 rounded shadow-lg">ACTIVE</div>
        </div>

        <h1 class="font-robot text-3xl font-black mb-4 uppercase tracking-tighter leading-none">
            Személyi <span class="text-green-500 green-glow">Bookmaker</span> <br> Asszisztens
        </h1>
        <p class="text-gray-400 text-xs leading-relaxed max-w-xs mx-auto italic">
            "A nevem Skyhigh AI. Egy matematikai alapú pénzgyárat építettünk neked. Én adom a számítást, te adod a fegyelmet."
        </p>
    </header>

    <section class="px-6 py-10">
        <div class="glass-dark p-8 rounded-[2rem] border-green-500/30">
            <h3 class="font-robot text-xs text-green-500 mb-6 uppercase text-center tracking-[0.2em]">Számold ki a jövőd</h3>
            
            <div class="space-y-6">
                <div>
                    <div class="flex justify-between items-end mb-2">
                        <label class="text-[10px] uppercase text-gray-500 font-bold tracking-widest">Kezdőtőke</label>
                        <span id="tokeValue" class="text-green-500 text-lg font-black tracking-tighter">100.000 Ft</span>
                    </div>
                    <input type="range" min="50000" max="5000000" step="10000" value="100000" class="w-full h-1.5 bg-zinc-800 rounded-lg appearance-none cursor-pointer" id="tokeRange">
                </div>

                <div>
                    <label class="text-[10px] uppercase text-gray-500 font-bold mb-3 block tracking-widest">Időtartam kiválasztása</label>
                    <div class="grid grid-cols-2 gap-4">
                        <button onclick="setTerm(6)" id="term6" class="py-4 rounded-2xl border border-green-500/50 bg-green-500/10 text-[10px] font-black tracking-widest transition uppercase">6 Hónap</button>
                        <button onclick="setTerm(12)" id="term12" class="py-4 rounded-2xl border border-zinc-800 bg-transparent text-[10px] font-black tracking-widest transition text-gray-500 uppercase">12 Hónap</button>
                    </div>
                </div>

                <div class="pt-8 border-t border-white/5 text-center">
                    <span class="text-[10px] text-gray-500 uppercase block mb-2 tracking-widest">Garantált tőke-iránycél:</span>
                    <div id="eredmeny" class="text-5xl font-black text-green-500 tracking-tighter green-glow">450.000 Ft</div>
                </div>
            </div>
        </div>
    </section>

    <section class="py-12 px-6">
        <div class="bg-gradient-to-br from-green-600 to-green-800 text-black p-10 rounded-[3rem] shadow-[0_20px_50px_rgba(34,197,94,0.3)] relative overflow-hidden">
            <div class="absolute -right-5 -top-5 opacity-10">
                <i class="fas fa-robot text-[8rem]"></i>
            </div>
            
            <h3 class="font-robot text-3xl font-black mb-1 uppercase tracking-tighter">HAVI LICENC</h3>
            <p class="text-[10px] font-bold uppercase mb-8 opacity-80 tracking-widest italic font-black">Technológiai Hozzáférés</p>
            
            <div class="flex items-baseline gap-2 mb-10">
                <span class="text-6xl font-black tracking-tighter">20.000 Ft</span>
                <span class="text-sm font-bold opacity-60">/hó</span>
            </div>

            <ul class="space-y-4 text-xs font-black mb-12 uppercase tracking-wide">
                <li class="flex items-center gap-3"><i class="fas fa-check-circle opacity-50"></i> Személyi AI Asszisztens (24/7)</li>
                <li class="flex items-center gap-3"><i class="fas fa-check-circle opacity-50"></i> Napi 1 Master AI Szelekció</li>
                <li class="flex items-center gap-3"><i class="fas fa-check-circle opacity-50"></i> Dinamikus Tőkemenedzsment</li>
                <li class="flex items-center gap-3"><i class="fas fa-check-circle opacity-50"></i> Automata Haladás Mentés</li>
            </ul>

            <button class="w-full bg-black text-white py-6 rounded-[2rem] font-black text-xs uppercase tracking-[0.3em] shadow-2xl active:scale-95 transition">
                Licenc Aktiválása
            </button>
        </div>
    </section>

    <section class="py-10 px-10 text-center space-y-12">
        <div>
            <i class="fas fa-save text-purple-500 mb-4 text-2xl"></i>
            <h4 class="font-robot text-[10px] mb-2 uppercase tracking-[0.2em] font-bold">Progress Save technológia</h4>
            <p class="text-[10px] text-gray-500 leading-relaxed uppercase tracking-wider">Ha a licenc lejár, a Robot megáll és menti a tőkéd állapotát. Aktiválás után pontosan onnan folytatódik a növekedés.</p>
        </div>
    </section>

    <footer class="py-12 text-center opacity-30 border-t border-white/5">
        <p class="text-[7px] tracking-[0.6em] font-bold uppercase text-green-500">Skyhigh AI System • Owner: faqu</p>
    </footer>

    <script>
        const range = document.getElementById('tokeRange');
        const tokeDisplay = document.getElementById('tokeValue');
        const eredmeny = document.getElementById('eredmeny');
        let currentTerm = 6;

        function updateCalc() {
            const val = parseInt(range.value);
            tokeDisplay.innerText = val.toLocaleString() + ' Ft';
            const multiplier = currentTerm === 6 ? 4.5 : 14;
            const final = Math.floor(val * multiplier);
            eredmeny.innerText = final.toLocaleString() + ' Ft';
        }

        function setTerm(months) {
            currentTerm = months;
            document.getElementById('term6').className = months === 6 ? 'py-4 rounded-2xl border border-green-500/50 bg-green-500/10 text-[10px] font-black tracking-widest transition uppercase' : 'py-4 rounded-2xl border border-zinc-800 bg-transparent text-[10px] font-black tracking-widest transition text-gray-500 uppercase';
            document.getElementById('term12').className = months === 12 ? 'py-4 rounded-2xl border border-green-500/50 bg-green-500/10 text-[10px] font-black tracking-widest transition uppercase' : 'py-4 rounded-2xl border border-zinc-800 bg-transparent text-[10px] font-black tracking-widest transition text-gray-500 uppercase';
            updateCalc();
        }

        range.addEventListener('input', updateCalc);
        updateCalc();
    </script>
</body>
</html>