<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Skyhigh AI | Az Igazgatói Rendszer</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;900&display=swap');
        body { font-family: 'Inter', sans-serif; background-color: #000; color: #fff; -webkit-font-smoothing: antialiased; }
        .bento-card { background: #0a0a0a; border: 1px solid #1a1a1a; border-radius: 28px; }
        .btn-active { background: #22c55e !important; color: #000 !important; border-color: #22c55e !important; }
        .btn-inactive { background: transparent; border: 1px solid #222; color: #666; }
        .term-active { border: 2px solid #a855f7 !important; background: rgba(168, 85, 247, 0.1); color: #fff !important; }
        .green-glow { color: #22c55e; text-shadow: 0 0 20px rgba(34, 197, 94, 0.4); }
        .purple-glow { color: #a855f7; filter: drop-shadow(0 0 8px rgba(168, 85, 247, 0.6)); }
        .buy-button { background: linear-gradient(90deg, #22c55e, #16a34a); color: #000; font-weight: 900; }
    </style>
</head>
<body class="px-4 pb-10">

    <nav class="py-6 flex justify-between items-center border-b border-white/5 mb-8">
        <div class="flex items-center gap-2">
            <i class="fas fa-robot purple-glow text-xl"></i>
            <span class="font-black tracking-tighter text-lg uppercase italic">Skyhigh<span class="text-green-500">.Ai</span></span>
        </div>
        <div class="text-[8px] font-bold text-zinc-500 uppercase tracking-widest">Director Mode: ON</div>
    </nav>

    <header class="text-center mb-10">
        <h1 class="text-3xl font-black uppercase tracking-tighter leading-none mb-4">
            A Napi 1 Tipp <br> <span class="green-glow text-5xl italic">Birodalma.</span>
        </h1>
        <p class="text-zinc-400 text-[11px] leading-relaxed px-6 uppercase tracking-wide font-bold">
            Indítsd el a saját sportfogadó vállalkozásod! <br> 
            <span class="text-white font-black italic">1 nap = 1 tipp. 30 nap = Fix profit.</span>
        </p>
    </header>

    <section class="mb-10 space-y-4">
        <div class="bento-card p-6 border-l-4 border-green-500">
            <div class="flex items-start gap-4">
                <div class="text-green-500 text-xl pt-1"><i class="fas fa-calendar-check"></i></div>
                <div>
                    <h4 class="text-xs font-black uppercase mb-1">A "Napi 1" Protokoll</h4>
                    <p class="text-[10px] text-zinc-500 leading-relaxed">Minden nap pontosan <span class="text-white font-bold">egy darab</span> mester tippet kapsz. Semmi zavaró tényező, csak a színtiszta matematika. A feladatod: minden nap megtenni.</p>
                </div>
            </div>
        </div>

        <div class="bento-card p-6">
            <div class="flex items-start gap-4">
                <div class="text-purple-500 text-xl pt-1"><i class="fas fa-chart-line"></i></div>
                <div>
                    <h4 class="text-xs font-black uppercase mb-1">Profit Visszaforgatás</h4>
                    <p class="text-[10px] text-zinc-500 leading-relaxed">A titok a 30 napos ciklus végén van. Ha a profitot visszaforgatod, a 6. hónap végére a szoftver <span class="text-white font-bold">brutális vagyongyarapodást</span> generál a tőkédre emlékezve.</p>
                </div>
            </div>
        </div>

        <div class="bento-card p-6 bg-zinc-900/20">
            <div class="flex items-start gap-4">
                <div class="text-white text-xl pt-1"><i class="fas fa-desktop"></i></div>
                <div>
                    <h4 class="text-xs font-black uppercase mb-1">Profi Admin Vezérlőpult</h4>
                    <p class="text-[10px] text-zinc-500 leading-relaxed">Egy átlátható felületen látod az összes statisztikát: napi profit, havi egyenleg, százalékos növekedés. Pontosan látod, hol tartasz az éves tervedben.</p>
                </div>
            </div>
        </div>
    </section>

    <section class="bento-card p-6 mb-10">
        <h3 class="text-[10px] font-black text-zinc-600 uppercase tracking-widest mb-6 text-center italic">Vállalkozási Kalkulátor</h3>
        
        <div class="space-y-8">
            <div>
                <label class="text-[9px] text-zinc-500 uppercase font-bold block mb-3">Válaszd ki az induló tőkéd:</label>
                <div class="grid grid-cols-3 gap-2">
                    <button onclick="setCap(50000)" id="cap50000" class="btn-inactive py-3 rounded-xl text-[10px] font-black transition-all">50.000</button>
                    <button onclick="setCap(100000)" id="cap100000" class="btn-active py-3 rounded-xl text-[10px] font-black transition-all">100.000</button>
                    <button onclick="setCap(250000)" id="cap250000" class="btn-inactive py-3 rounded-xl text-[10px] font-black transition-all">250.000</button>
                </div>
            </div>

            <div class="space-y-4">
                <label class="text-[9px] text-zinc-500 uppercase font-bold block mb-1">Válassz ciklust (Lila Kiemelés):</label>
                <div class="grid grid-cols-2 gap-3">
                    <button onclick="setTerm(6)" id="term6" class="term-active py-4 rounded-2xl text-[10px] font-black uppercase tracking-widest transition-all">6 Hónapos Terv</button>
                    <button onclick="setTerm(12)" id="term12" class="btn-inactive py-4 rounded-2xl text-[10px] font-black uppercase tracking-widest transition-all">12 Hónapos Terv</button>
                </div>
                
                <div class="pt-4">
                    <button class="buy-button w-full py-5 rounded-2xl text-xs uppercase tracking-[0.2em] shadow-lg shadow-green-500/20 active:scale-95 transition">
                        Licenc Aktiválása (20.000 Ft)
                    </button>
                </div>
            </div>

            <div class="pt-6 border-t border-white/5 text-center">
                <div class="flex justify-between items-center px-2 mb-4">
                    <span class="text-[9px] text-zinc-500 uppercase font-bold italic">30 napos AI iránycél:</span>
                    <span id="haviProfit" class="text-sm font-bold text-green-500">~130.000 Ft</span>
                </div>
                <div class="bg-zinc-900 py-6 rounded-2xl border border-white/5">
                    <span class="text-[9px] text-purple-400 uppercase font-black tracking-widest block mb-1">Várható Vagyon a terv végén:</span>
                    <div id="eredmeny" class="text-4xl font-black tracking-tighter italic">450.000 <span class="text-green-500 text-lg">Ft</span></div>
                    <p class="text-[7px] text-zinc-700 mt-2 uppercase px-6 italic font-bold">100% Matematikai modell • 365 napos AI memória</p>
                </div>
            </div>
        </div>
    </section>

    <footer class="text-center pt-10 border-t border-white/5 opacity-40">
        <p class="text-[8px] font-black tracking-[0.6em] uppercase mb-2 text-green-500">SKYHIGH AI SYSTEM</p>
        <p class="text-[7px] text-zinc-700 uppercase mb-4 italic">Minden jog fenntartva. Faqubelord Engineering.</p>
    </footer>

    <script>
        let currentCap = 100000;
        let currentTerm = 6;

        function setCap(amount) {
            currentCap = amount;
            const caps = [50000, 100000, 250000];
            caps.forEach(c => {
                document.getElementById('cap' + c).className = c === amount ? 'btn-active py-3 rounded-xl text-[10px] font-black transition-all' : 'btn-inactive py-3 rounded-xl text-[10px] font-black transition-all';
            });
            updateCalc();
        }

        function setTerm(months) {
            currentTerm = months;
            document.getElementById('term6').className = months === 6 ? 'term-active py-4 rounded-2xl text-[10px] font-black uppercase tracking-widest transition-all' : 'btn-inactive py-4 rounded-2xl text-[10px] font-black uppercase tracking-widest transition-all';
            document.getElementById('term12').className = months === 12 ? 'term-active py-4 rounded-2xl text-[10px] font-black uppercase tracking-widest transition-all' : 'btn-inactive py-4 rounded-2xl text-[10px] font-black uppercase tracking-widest transition-all';
            updateCalc();
        }

        function updateCalc() {
            const havi = document.getElementById('haviProfit');
            const veg = document.getElementById('eredmeny');
            const monthlyProfit = Math.floor(currentCap * 1.3);
            havi.innerText = '~' + monthlyProfit.toLocaleString() + ' Ft';
            const multiplier = currentTerm === 6 ? 4.5 : 14;
            const finalValue = Math.floor(currentCap * multiplier);
            veg.innerHTML = finalValue.toLocaleString() + ' <span class="text-green-500 text-lg">Ft</span>';
        }
        updateCalc();
    </script>
</body>
</html>