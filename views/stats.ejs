<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Statisztika | Rafinált Róka</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@800&family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        body { background: #050505; color: #fff; font-family: 'Inter', sans-serif; padding-bottom: 40px; }
        .startup-card { background: #111; border: 1px solid #27272a; border-radius: 20px; box-shadow: 0 10px 30px -10px rgba(0,0,0,0.5); }
        .gradient-text { background: linear-gradient(to right, #a855f7, #ec4899); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
        .fox-glow { box-shadow: 0 0 25px rgba(168, 85, 247, 0.2); }
    </style>
</head>
<body class="p-5 max-w-lg mx-auto">

    <header class="flex justify-between items-center mb-8 pt-2">
        <div class="flex items-center gap-3">
            <div class="w-10 h-10 bg-gradient-to-br from-purple-600 to-pink-600 rounded-xl flex items-center justify-center text-xl shadow-lg fox-glow">🦊</div>
            <div>
                <h1 class="text-sm font-bold text-white tracking-wide">RAFINÁLT RÓKA</h1>
                <p class="text-[10px] text-zinc-400 font-medium">Statisztika Központ</p>
            </div>
        </div>
        
        <div class="flex gap-2">
            <a href="/dashboard" class="bg-zinc-900 hover:bg-zinc-800 text-zinc-400 px-3 py-2 rounded-lg text-[10px] font-bold transition">🏠 HOME</a>
            <a href="/logout" class="bg-zinc-900 hover:bg-zinc-800 text-zinc-400 px-4 py-2 rounded-full text-[10px] font-bold transition">EXIT</a>
        </div>
    </header>

    <div class="mb-8 text-center">
        <h2 class="text-2xl font-black text-white mb-2">A számok <span class="text-purple-400">nem hazudnak</span>.</h2>
        <div class="bg-zinc-900/50 p-3 rounded-xl border border-white/5 inline-block">
            <p class="text-xs text-zinc-300 italic">"<%= randomQuote %>"</p>
        </div>
    </div>

    <h3 class="text-xs font-bold text-zinc-500 uppercase mb-4">Havi Teljesítmény (Zárt)</h3>
    
    <% if (monthlyStats && monthlyStats.length > 0) { %>
        <div class="space-y-4 mb-10">
            <% monthlyStats.forEach(stat => { %>
                <div class="startup-card p-5 border-l-4 border-l-purple-500">
                    <div class="flex justify-between items-center mb-3">
                        <h4 class="text-lg font-black text-white"><%= stat.month %></h4>
                        <span class="text-[10px] font-bold bg-purple-900/30 text-purple-400 px-2 py-1 rounded">HIVATALOS</span>
                    </div>
                    <div class="grid grid-cols-3 gap-2 text-center">
                        <div class="bg-zinc-900/50 p-2 rounded-lg">
                            <p class="text-[9px] text-zinc-500 uppercase">Profit</p>
                            <p class="text-sm font-bold <%= stat.totalProfit > 0 ? 'text-green-500' : 'text-red-500' %>"><%= stat.totalProfit %>%</p>
                        </div>
                        <div class="bg-zinc-900/50 p-2 rounded-lg">
                            <p class="text-[9px] text-zinc-500 uppercase">Win Rate</p>
                            <p class="text-sm font-bold text-white"><%= stat.totalTips > 0 ? Math.round((stat.winCount/stat.totalTips)*100) : 0 %>%</p>
                        </div>
                        <div class="bg-zinc-900/50 p-2 rounded-lg">
                            <p class="text-[9px] text-zinc-500 uppercase">Tippek</p>
                            <p class="text-sm font-bold text-white"><%= stat.totalTips %> db</p>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    <% } else { %>
        <div class="text-center py-6 border border-dashed border-zinc-800 rounded-xl mb-10">
            <p class="text-xs text-zinc-500">Még nincs lezárt/publikált hónap.</p>
        </div>
    <% } %>

    <h3 class="text-xs font-bold text-zinc-500 uppercase mb-4 flex items-center gap-2">
        <span>Részletes Történet (Napló)</span>
        <span class="h-px bg-zinc-800 flex-1"></span>
    </h3>

    <div class="space-y-3">
        <% historyTips.forEach(tip => { %>
            <div class="bg-zinc-900/50 p-4 rounded-xl flex justify-between items-center border border-white/5 hover:bg-zinc-800 transition">
                <div>
                    <p class="text-[10px] text-zinc-500 mb-0.5"><%= tip.date %></p>
                    <p class="text-xs font-bold text-white"><%= tip.match %></p>
                </div>
                <% if(tip.status === 'win') { %> 
                    <div class="w-8 h-8 rounded-full bg-green-500/10 flex items-center justify-center text-green-500 border border-green-500/20 font-bold">✓</div> 
                <% } else { %> 
                    <div class="w-8 h-8 rounded-full bg-red-500/10 flex items-center justify-center text-red-500 border border-red-500/20 font-bold">✕</div> 
                <% } %>
            </div>
        <% }) %>
    </div>

</body>
</html>