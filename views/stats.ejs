<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Statisztika | Rafinált Róka</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body { background: #050505; color: #fff; font-family: 'Inter', sans-serif; }
        .stat-card { background: #111; border: 1px solid #27272a; border-radius: 15px; }
        .win { border-left: 4px solid #22c55e; }
        .loss { border-left: 4px solid #ef4444; }
    </style>
</head>
<body class="p-5 max-w-lg mx-auto">
    <div class="flex items-center gap-4 mb-8">
        <a href="/dashboard" class="text-zinc-500 text-sm">← Vissza</a>
        <h1 class="text-xl font-bold">Napi Statisztika</h1>
    </div>

    <% if (monthClosed) { %>
        <div class="stat-card p-5 mb-6 bg-gradient-to-br from-purple-900/20 to-transparent border-purple-500/30">
            <p class="text-[10px] text-purple-400 font-bold uppercase">Havi Zárás Eredménye</p>
            <div class="flex justify-between items-end mt-2">
                <h2 class="text-3xl font-black text-white">+<%= monthlyProfit %>%</h2>
                <p class="text-xs text-zinc-500">Profit ebben a hónapban</p>
            </div>
        </div>
    <% } %>

    <div class="space-y-3">
        <% history.forEach(tip => { %>
            <div class="stat-card p-4 flex justify-between items-center <%= tip.status === 'win' ? 'win' : 'loss' %>">
                <div>
                    <p class="text-[10px] text-zinc-500 uppercase font-bold"><%= tip.date %></p>
                    <h4 class="text-sm font-bold text-white"><%= tip.match %></h4>
                    <p class="text-xs text-zinc-400"><%= tip.prediction %> (<%= tip.odds %>)</p>
                </div>
                <div class="text-right">
                    <span class="<%= tip.status === 'win' ? 'text-green-500' : 'text-red-500' %> font-black text-xs uppercase">
                        <%= tip.status === 'win' ? 'NYERT ✅' : 'VESZTETT ❌' %>
                    </span>
                </div>
            </div>
        <% }) %>
    </div>
</body>
</html>