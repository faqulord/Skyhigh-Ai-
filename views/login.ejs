<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Skyhigh AI | Bejelentkezés</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body { background-color: #000; color: white; -webkit-tap-highlight-color: transparent; }
        input { font-size: 16px !important; } /* Megakadályozza az iPhone automatikus zoomját */
    </style>
</head>
<body class="flex items-center justify-center min-h-screen p-6 bg-[url('https://www.transparenttextures.com/patterns/dark-matter.png')]">

    <div class="w-full max-w-sm space-y-8 bg-zinc-950/50 p-8 rounded-[32px] border border-zinc-900 backdrop-blur-xl">
        <div class="text-center">
            <h1 class="text-3xl font-black italic tracking-tighter uppercase">Skyhigh<span class="text-purple-600">.AI</span></h1>
            <p class="text-[9px] text-zinc-500 uppercase tracking-[4px] mt-2 font-bold italic">Rendszer Belépés</p>
        </div>

        <form action="/auth/login" method="POST" class="space-y-5">
            <div>
                <label class="text-[10px] text-zinc-600 uppercase font-black ml-2 mb-1 block">Azonosító</label>
                <input type="email" name="email" required placeholder="email@skyhigh.ai" 
                    class="w-full bg-black border border-zinc-800 rounded-2xl p-4 text-sm focus:border-purple-600 outline-none transition duration-300">
            </div>
            <div>
                <label class="text-[10px] text-zinc-600 uppercase font-black ml-2 mb-1 block">Jelszó</label>
                <input type="password" name="password" required placeholder="••••••••" 
                    class="w-full bg-black border border-zinc-800 rounded-2xl p-4 text-sm focus:border-purple-600 outline-none transition duration-300">
            </div>
            <button type="submit" class="w-full bg-white text-black py-4 rounded-2xl font-black uppercase text-xs tracking-widest hover:bg-purple-600 hover:text-white transition duration-500 active:scale-95">
                Belépés a Terminálba
            </button>
        </form>

        <div class="text-center pt-4">
            <a href="/regisztracio" class="text-[10px] text-zinc-500 hover:text-purple-400 transition uppercase font-bold tracking-widest">
                Új operátor regisztrációja
            </a>
        </div>
    </div>

</body>
</html>