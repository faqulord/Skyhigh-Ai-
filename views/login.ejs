<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Belépés | Skyhigh AI</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@800&family=Inter:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        body { background: #000; color: #fff; font-family: 'Inter', sans-serif; }
        .login-card { background: #0a0a0a; border: 1px solid #1a1a1a; border-top: 3px solid #8b5cf6; box-shadow: 0 10px 30px rgba(0,0,0,0.5); }
    </style>
</head>
<body class="flex items-center justify-center min-h-[100dvh] p-6">
    <div class="max-w-xs w-full">
        <div class="login-card p-8 rounded-[2rem] text-center">
            <div class="text-5xl mb-6">🦊</div>
            <h2 style="font-family: 'Orbitron';" class="text-lg text-purple-500 font-black uppercase italic mb-8 tracking-tighter">Neural Login</h2>
            
            <form action="/auth/login" method="POST" class="space-y-4">
                <input type="email" name="email" placeholder="Email címed" class="w-full bg-zinc-900 border-none rounded-2xl p-4 text-sm focus:ring-2 focus:ring-purple-600 outline-none" required>
                <input type="password" name="password" placeholder="Jelszavad" class="w-full bg-zinc-900 border-none rounded-2xl p-4 text-sm focus:ring-2 focus:ring-purple-600 outline-none" required>
                
                <button type="submit" class="w-full bg-purple-600 hover:bg-purple-700 py-4 rounded-2xl font-black uppercase text-[11px] tracking-widest shadow-lg shadow-purple-600/20 transition-all active:scale-95">
                    Belépés a Rendszerbe
                </button>
            </form>

            <p class="mt-8 text-[10px] text-zinc-600 font-bold uppercase tracking-widest">
                Nincs fiókod? <a href="/register" class="text-purple-500 underline">Regisztrálj</a>
            </p>
        </div>
    </div>
</body>
</html>